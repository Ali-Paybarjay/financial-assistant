# بریف طراحی: دستیار مالی (MVP) — فقط جهت‌گیری بصری و Design Token System

> این فایل مکمل `financial-assistant-claude-code-prompt.md` است (که جدا اضافه می‌شود). آن فایل مرجع کامل محصول/معماری/دیتاست است؛ این فایل فقط روی کار طراحی بصری تمرکز دارد که قرار است اینجا (Claude Design) انجام شود و بعداً نتیجه‌اش به `PLAN.md` پروژه منتقل شود.

---

## نقش تو در این گفتگو

تو یک senior product designer هستی، نه یک illustrator. خروجی نهایی‌ات باید مستقیماً قابل کپی در یک `PLAN.md` فنی باشد: تصمیم‌های مشخص با دلیل، نه moodboard مبهم. هر انتخاب باید نقد شود — اگر توجیه نداری چرا این رنگ/فونت/لهجه به‌جای ده‌ها گزینه‌ی دیگر انتخاب شده، دوباره فکر کن.

---

## محصول در یک پاراگراف

«دستیار مالی» یک وب‌اپ مدیریت مالی شخصی برای **فارسی‌زبانان مقیم خارج از ایران** (کانادا/آمریکا/اروپا) است. کاربر هزینه‌های روزمره‌اش را با فرم، متن آزاد، صدا یا عکس فاکتور ثبت می‌کند و داشبورد ماهانه‌ی درآمد/هزینه/مانده/نرخ پس‌انداز را می‌بیند.

**جمله‌ی محک محصول:** کاربر باید در کمتر از ۱۰ ثانیه یک خرید ثبت کند و در کمتر از ۵ ثانیه بفهمد این ماه چقدر پول برایش مانده. هر تصمیم طراحی که این دو عدد را بدتر کند، اشتباه است. یعنی: سرعت و وضوح بر تزئین اولویت دارند.

---

## قیدهای غیرقابل‌مذاکره

1. **RTL کامل.** `dir="rtl"`. جهت آیکون‌های فلش‌دار باید mirror شود. الگوهای spacing باید منطقی باشند (start/end نه left/right).
2. **فونت:**
   - بدنه: **Vazirmatn**
   - نقش display (تیتر/اعداد کلیدی): یک فونت فارسی متمایز — پیشنهاد خودت را با دلیل بده (مثلاً Morabba یا Estedad)، با خساست استفاده شود.
   - فونت‌ها باید self-hostable باشند (نه CDN‌های تحریم‌خورده مثل Google Fonts مستقیم برای کاربر ایرانی — فایل باید قابل دانلود/self-host باشد).
3. **اعداد پول:** همیشه ارقام لاتین، `font-variant-numeric: tabular-nums`، و در متن فارسی با `dir="ltr"` + `unicode-bidi: isolate` ایزوله می‌شوند تا جای نماد ارز به هم نریزد. مثال درست را در موکاپ نشان بده.
4. **پالت آرام و مالی، نه شاد و بازی‌گونه.** رنگ فقط جایی معنا دارد که پیام می‌دهد: مثبت/منفی (مانده)، وضعیت هدف. نمودارها رنگین‌کمانی نباشند.
5. **موبایل-اول.** اکثر ثبت‌های هزینه روی گوشی اتفاق می‌افتد؛ هر موکاپ باید عرض ۳۷۵px را هم پوشش دهد.
6. **کنتراست AA، focus قابل‌مشاهده، `prefers-reduced-motion`** — این‌ها کف کیفیت‌اند، حتی اگر گفته نشوند.
7. **فقط لایت‌مود در MVP.** دارک‌مود خارج از دامنه است.

### سه لوکی که ممنوع‌اند (چون امضای «طراحی نشده، تولید شده» هستند)

- پس‌زمینه‌ی کرم گرم + سریف پرکنتراست + لهجه‌ی خاکی-نارنجی
- پس‌زمینه‌ی تقریباً مشکی + یک سبز اسیدی تک‌رنگ
- لی‌اوت روزنامه‌ای با خط‌های مویی نازک و صفر گردی گوشه (border-radius: 0)

اگر پیش‌نویس اول به یکی از این‌ها نزدیک شد، دوباره طراحی کن.

---

## کار خواسته‌شده (خروجی مورد انتظار)

### ۱. Design Token System

- ۴ تا ۶ رنگ نام‌گذاری‌شده با hex دقیق (پس‌زمینه، متن، برند/اصلی، مثبت، منفی، خنثی/کارتی) + دلیل انتخاب هرکدام
- مقیاس تایپوگرافی (سایزها، وزن‌ها، line-height) برای بدنه و display، هماهنگ با اعداد بزرگ داشبورد
- **«عنصر امضا»**: یک چیز مشخص که این اپ با آن به خاطر سپرده می‌شود (مثلاً یک شکل خاص برای نمودار دونات، یک نوع کارت تأیید تراکنش، یک الگوی حرکت). باید توضیح دهی چرا این عنصر برای «دستیار مالی فارسی» معنادار است، نه یک انتخاب عمومی که برای هر اپ مالی دیگری هم می‌ساختی.
- **نقد خودت**: بعد از پیشنهاد اول، آن را زیر سؤال ببر — هر بخشی که ژنریک/کلیشه‌ای است را مشخص کن و اصلاح کن.

### ۲. موکاپ‌های تصویری (اولویت‌بندی‌شده)

اگر امکان تولید تصویر/HTML preview داری، این صفحات را به ترتیب اولویت بساز (موبایل ۳۷۵px):

1. **داشبورد** — چهار کارت بالا (درآمد/هزینه/مانده/نرخ پس‌انداز با رنگ مثبت/منفی)، نمودار دونات دسته‌بندی هزینه، دکمه‌ی شناور «ثبت هزینه» (پایین‌راست چون RTL)
2. **Modal ثبت تراکنش با ۴ تب** (فرم/متن/صدا/عکس) — به‌خصوص حالت «کارت تأیید» که خروجی AI را نشان می‌دهد و فیلدهای حدسی/کم‌اطمینان را بصری متمایز می‌کند
3. **یک گام از Onboarding** (مثلاً گام ریسک‌پذیری با سؤال سناریومحور) — برای نشان‌دادن لحن و progress bar
4. **حالت خالی داشبورد** برای کاربر تازه‌وارد (نه نمودار خالی؛ یک دعوت به عمل)

### ۳. متن رابط کاربری (نمونه)

چند نمونه از لحن دکمه/خطا/حالت خالی به فارسی بنویس: فعل فعال، بدون تعارف، بدون عذرخواهی در خطاها (خطا می‌گوید چه شد و کاربر چه کند).

---

## خروجی نهایی چطور باید باشد

یک سند (یا چند بخش) شامل:
- جدول رنگ‌ها با hex + نام semantic + دلیل
- مقیاس تایپوگرافی به‌صورت جدول/لیست
- توضیح عنصر امضا + نقد
- موکاپ‌ها (تصویر یا HTML)
- نمونه‌های متن رابط

این خروجی مستقیماً در `PLAN.md` پروژه (بخش design token system) کپی می‌شود، پس ساختارش را طوری بده که copy-paste-پذیر باشد، نه یک گزارش روایی بلند.
