# DECISIONS.md

هر ردیف: تصمیم · دلیل · جایگزین رد شده.

## M0 — پایه

### معماری و استک

- **Next.js 15.5.25 و نه 16.** سند استک صریحاً «Next.js 15» گفته و پروژه به همان پایبند می‌ماند؛ ۱۶ پایدار است ولی انحراف از مشخصات بدون دلیل فنی ارزش ندارد و مسیر ارتقا با codemod باز است. *رد شده:* `create-next-app@latest` (نسخه‌ی ۱۶).
- **pnpm از طریق corepack و نه نصب global.** نسخه در خود پروژه قفل می‌شود و محیط کاربر آلوده نمی‌شود. *رد شده:* `npm i -g pnpm`.
- **خواندن داده: prefetch در Server Component + `HydrationBoundary` برای TanStack Query.** یک منبع حقیقت؛ اگر نیمی از صفحه RSC مستقیم بخواند و نیمی Query، بعد از هر mutation نیمی تازه و نیمی کهنه می‌ماند. *رد شده:* RSC خالص با `revalidatePath` (undo پنج‌ثانیه‌ای و فیلترهای لیست را سخت می‌کند).
- **نوشتن داده: Server Action برای CRUD، Route Handler فقط برای سه مسیر AI.** مسیر AI به کنترل صریح status code، تایم‌اوت و `maxDuration` نیاز دارد؛ CRUD ندارد. *رد شده:* همه‌چیز Route Handler.
- **سه کارخانه‌ی جدا برای Supabase با `import "server-only"` روی `admin.ts`.** نشت service role به باندل کلاینت به جای یک باگ زمان‌اجرا، خطای build می‌شود. *رد شده:* یک کلاینت واحد با سوییچ.

### مدل داده

- **`text` + `check` به‌جای `pg enum`.** افزودن یک مقدار به enum یک migration قفل‌کننده می‌خواهد؛ تغییر check ساده است. *رد شده:* enum بومی Postgres.
- **RLS با `(select auth.uid())` و نه `auth.uid()`.** فرم بدون `select` به ازای هر ردیف اجرا می‌شود؛ با آن یک‌بار ارزیابی و کش می‌شود. *رد شده:* نگارش لفظی سند.
- **policy به تفکیک عملیات و `to authenticated`.** `for all` باعث می‌شود `with check` روی insert/update عملاً بی‌اثر بماند. *رد شده:* یک policy واحد `for all`.
- **`categories` تنها جدول با policy خواندن متفاوت** (`user_id is null or user_id = ...`). ردیف‌های سیستمی `user_id = null` دارند؛ با شرط لفظی سند هیچ‌کس آن‌ها را نمی‌دید و ثبت تراکنش از کار می‌افتاد. *رد شده:* اعمال لفظی «`auth.uid() = user_id` روی همه‌ی جدول‌ها».
- **افزودن `recurring_expense_id` و `posted_month` به `transactions`.** خود سند unique constraint روی `(recurring_expense_id, month)` خواسته ولی ستون‌ها در schemaاش نبودند. *رد شده:* چک «قبلاً ساخته شده؟» در کد اپ (با دو تب باز همزمان می‌شکند).
- **افزودن `is_confirmed` و `needs_review[]` به `transactions`.** عنصر امضای طراحی per-field است؛ `ai_confidence` یک عدد کلی است و نمی‌گوید کدام فیلد حدسی بوده. *رد شده:* اتکا به `ai_confidence` تنها.
- **جدول `variable_expense_baselines`.** گام ۴ onboarding تخمین ماهانه در پنج دسته می‌گیرد و جایی برای ذخیره نداشت. *رد شده:* jsonb روی `profiles` (کوئری و اعتبارسنجی را سخت می‌کرد).
- **ستون `savings_rate_estimate` روی `profiles`.** گام ۷ این عدد را می‌پرسد و ستونی نداشت.
- **`birth_year` در CHECK فقط ۱۹۳۰..۲۱۰۰.** قاعده‌ی واقعی («حداقل ۱۳ سال») به سال جاری وابسته است و CHECK در Postgres باید immutable باشد؛ قاعده در Zod اعمال می‌شود. *رد شده:* CHECK با `extract(year from now())`.
- **تولید تراکنش‌های تکرارشونده با تابع Postgres و `on conflict do nothing`.** تضمین idempotency در دیتابیس است نه در کد، پس دو اجرای همزمان هم ردیف تکراری نمی‌سازد. *رد شده:* pg_cron در MVP (بدون تغییر منطق بعداً قابل اضافه‌شدن است).
- **دسته‌های درآمدی به seed اضافه شد** (حقوق، درآمد آزاد، سایر درآمد). لیست سند فقط هزینه‌ای بود، ولی تراکنش `type = 'income'` هم به دسته نیاز دارد.
- **عکس هرگز از Route Handler عبور نمی‌کند.** سقف body در Vercel ۴.۵ مگابایت است و سند تا ۵ مگابایت فاکتور اجازه می‌دهد؛ آپلود مستقیم به Storage هم سقف را حذف می‌کند و هم تأخیر را. *رد شده:* ارسال base64 به route.

### طراحی

- **مانده از کارت بیرون کشیده شد و تیتر صفحه شد** (سه کارت KPI زیرش، نه چهار کارت هم‌وزن). انحراف عمدی از «ردیف اول — چهار کارت» سند، در خدمت جمله‌ی محک «در کمتر از ۵ ثانیه بفهمد چقدر مانده». منبع: نقد خودِ فاز طراحی.
- **`display-xl` با `clamp(1.75rem, 11vw, 2.5rem)` و نه ۴۰px ثابت.** موکاپ با `+$1,131.60` طراحی شده؛ مانده‌ی پنج‌رقمی در عرض ۳۷۵px سرریز می‌کرد. *رد شده:* سایز ثابت + وصله در M5.
- **فونت Estedad به‌جای Morabba.** مربع لایسنس تجاری دارد و قید self-host را با یک قید حقوقی عوض می‌کند. منبع: فاز طراحی.
- **فقط `Vazirmatn-Regular` و `Estedad-ExtraBold` پیش‌بارگذاری می‌شوند.** با `font-display: swap` عدد مانده اول با فونت بدنه رندر و بعد جابه‌جا می‌شد — دقیقاً روی عنصری که باید در ۵ ثانیه خوانده شود.
- **`Direction.Provider` از radix-ui در Providers.** کامپوننت‌های Radix جهت را از این provider می‌خوانند نه از `dir` روی `<html>`؛ بدون آن dropdown و select از سمت غلط باز می‌شوند.
- **اسکریپت `check:rtl` به‌جای lint rule.** ابزار lint تیلویند هنوز v4 را کامل پوشش نمی‌دهد؛ یک اسکریپت گرپ‌محور `ml-/mr-/left-/right-` را قطعی می‌گیرد و با کامنت `rtl-ok` قابل استثناست.
- **متغیرهای semantic شadcn به توکن‌های این پروژه نگاشت شدند** و بلوک `.dark` حذف شد. شadcn پیش‌فرض یک پالت خاکستری oklch و دارک‌مود می‌آورد؛ دارک‌مود در MVP خارج از دامنه است و نگه‌داشتنش یعنی یک تم که بی‌سروصدا از تم اصلی جدا می‌افتد. *رد شده:* نگه‌داشتن خروجی پیش‌فرض `shadcn init`.
- **فونت Geist که `shadcn init` اضافه کرده بود حذف شد.** از Google Fonts بارگذاری می‌شد که قید «CDN تحریم‌خورده ممنوع» را نقض می‌کند.
- **`lucide-react` فعلاً نصب می‌ماند.** کامپوننت‌های shadcn آیکون‌هایشان را از آن import می‌کنند؛ آیکون‌های رابط ما از Phosphor است و هنگام افزودن هر کامپوننت آیکونش جایگزین می‌شود. اگر در پایان هیچ ارجاعی نماند، حذف می‌شود.

## M1 — احراز هویت

- **کلید anon به شکل JWT قدیمی، نه کلید `sb_publishable_…`.** با کلید publishable جدید، middleware در edge runtime با `Cannot read properties of undefined (reading 'indexOf')` می‌شکند — `@supabase/ssr` نسخه‌ی ۰.۱۲ کلید را به‌عنوان JWT پارس می‌کند. وقتی کتابخانه پشتیبانی کرد قابل مهاجرت است. *رد شده:* کلید publishable (توصیه‌ی عمومی Supabase، ولی اینجا کار نمی‌کند).
- **پیام خطای Supabase ترجمه می‌شود، نه نمایش مستقیم.** پیام‌های انگلیسی کتابخانه به کاربر فارسی‌زبان نشان داده نمی‌شوند؛ هر شاخه می‌گوید چه شد و کاربر چه کند. *رد شده:* نمایش `error.message`.
- **درخواست بازیابی رمز برای ایمیل ناموجود هم «موفق» جواب می‌دهد.** در غیر این صورت این endpoint ابزاری می‌شود برای فهمیدن اینکه کدام ایمیل‌ها حساب دارند.
- **`next` در callback فقط مسیر نسبی می‌پذیرد.** وگرنه یک لینک دستکاری‌شده می‌تواند کاربر را با نشست تازه به دامنه‌ی دیگری ببرد.
- **اندازه‌ی دکمه‌ها و ورودی‌های shadcn بازنویسی شد** (۵۲px کنش اصلی، ۴۴px حداقل هدف لمسی، ۴۸px ورودی با متن ۱۶px تا سافاری iOS روی focus زوم نکند). پیش‌فرض shadcn ۳۲px است که هم از حداقل هدف لمسی کمتر است و هم با طراحی نمی‌خواند.

## M3 — درآمد و هزینه‌ی ثابت

- **یک صفحه با کنترل دوتایی، نه دو مسیر `/income` و `/recurring`.** ساختار ردیف‌ها یکسان است و تقسیم‌کردنش ارزش یک مسیر جدا را ندارد. منبع: خود فاز طراحی (صفحه‌ی ۶).
- **غیرفعال‌کردن ردیف، حذفش نمی‌کند** و ردیف با ۵۵٪ کدری دیده می‌شود. خاموش‌کردن یک هزینه نباید تاریخچه را پاک کند. *رد شده:* حذف نرم با مخفی‌کردن کامل.
- **تضمین idempotency در UI گفته می‌شود** («دو بار اجرا شدن، تراکنش تکراری نمی‌سازد»). کاربر باید بداند چرا بستن و باز کردن اپ اول ماه، اجاره‌اش را دوبار ثبت نمی‌کند.
- **RPC تولید تراکنش‌های تکرارشونده در هر بار بارگذاری داشبورد صدا زده می‌شود.** تضمین در ایندکس یکتای دیتابیس است نه در کد، پس فراخوانی مکرر بی‌هزینه و امن است. تست‌شده: سه اجرای پشت‌سرهم فقط دو ردیف ساخت.
- **`due_day = 31` در ماه‌های کوتاه به آخرین روز همان ماه می‌افتد**، نه ماه بعد. تست‌شده روی فوریه‌ی ۲۰۲۶ → ۲۸ فوریه.
- **خطای RPC داشبورد را از کار نمی‌اندازد.** تراکنش‌های خودِ کاربر درست‌اند؛ فقط ردیف‌های تولیدشده غایب می‌مانند.

## M4 — تراکنش‌ها

- **همه‌ی خواندن‌ها از `lib/queries/transactions.ts` عبور می‌کنند** و فیلتر `deleted_at is null` فقط همان‌جا نوشته شده. اگر هر کامپوننتی مستقیم کوئری بزند، یک روز فیلتر را فراموش می‌کند و پول حذف‌شده را نشان می‌دهد.
- **جمعِ فیلترشده کنار تعداد نتایج نشان داده می‌شود.** بدون آن، فیلتر یک اسباب‌بازی است: کاربر نمی‌بیند مجموعه‌ی محدودشده چقدر می‌ارزد. منبع: فاز طراحی.
- **undo به‌صورت ردیف درون همان کارت، نه toast گوشه‌ی صفحه.** در پنج ثانیه، انگشت به گوشه‌ی صفحه نمی‌رسد.
- **پیام تأیید بعد از ثبت، مانده‌ی تازه را می‌گوید** («ثبت شد. $۱٬۰۶۹٫۲۰ برایت مانده»). همان عددی که کاربر برایش آمده بود.
- **مودال فعلاً فقط تب فرم دارد.** سه تب هوشمند با مسیرهای پردازش در M6 تا M8 اضافه می‌شوند؛ نمایش تب‌های غیرفعال فقط کاربر را سردرگم می‌کند.

## M5 — داشبورد

- **`monthTotals` یک‌بار روی ماه عبور می‌کند** و کارت‌های KPI و دونات از همان نتیجه تغذیه می‌شوند. دو کوئری جدا یعنی دو عددی که یک روز با هم نمی‌خوانند.
- **محور زمانی نمودار میله‌ای چپ‌به‌راست است** (قدیمی‌ترین ماه سمت چپ)، مطابق طراحی. SVG با `dir` سند برنمی‌گردد و زمان در نمودار قراردادی چپ‌به‌راست است. *رد شده:* `reversed` روی محور.
- **دونات یک جدول معادل مخفی برای screen reader دارد.** خودِ نمودار برای صفحه‌خوان بی‌معناست؛ این همان مورد بازِ بخش دسترس‌پذیری در handoff بود.
- **یک breakpoint در ۹۶۰px، نه سه.** زیر آن چیدمان موبایل با نوار پایین؛ بالای آن سایدبار سمت راست، حذف FAB، انتقال کنش اصلی به هدر، و محتوا تا ۱۱۲۰px و بعد وسط‌چین. سایز فونت‌ها عوض نمی‌شود.
- **`/goals` و `/settings` زودتر از برنامه ساخته شدند** چون نوار پایین به آن‌ها لینک می‌دهد و ۴۰۴ دادن بدتر از یک صفحه‌ی ساده است. «خروج از حساب» هم تا اینجا هیچ UI ای نداشت. صیقل کامل تنظیمات در M9 می‌ماند.
- **`queryClient.clear()` پیش از خروج از حساب.** وگرنه حساب بعدی روی همان دستگاه، اعداد حساب قبلی را در کش می‌بیند.

### هوش مصنوعی

- **مدل `claude-sonnet-5` به‌جای `claude-sonnet-4-6` سند.** نسل جاری است و ارزان‌تر هم هست ($2/$10 در برابر $3/$15 به ازای هر میلیون توکن). تأییدشده توسط کارفرما.
- **Claude از طریق OpenRouter (`anthropic/claude-sonnet-5`)، نه SDK مستقیم Anthropic.** یک کلید و یک صورتحساب برای همه‌ی مسیرهای مدل، و تعویض مدل بعداً فقط عوض‌کردن یک رشته است. بررسی شد که روی OpenRouter همان نرخ مستقیم Anthropic را دارد (بدون مارک‌آپ توکن)، ورودی تصویری می‌پذیرد و `structured_outputs` را پشتیبانی می‌کند. *رد شده:* `@anthropic-ai/sdk` — از وابستگی‌ها حذف شد چون هیچ‌وقت صدا زده نمی‌شود.
- **ضبط صدا در اپ ساخته نمی‌شود؛ کاربر با کیبورد خودش دیکته می‌کند.** تصمیم کارفرما، و از نظر فنی هم درست‌ترین کار: ریسک شماره‌یک پلن (`MediaRecorder` روی iOS Safari و تکه‌تکه‌بودن فرمت‌ها) کامل حذف می‌شود، هزینه صفر است، یک رفت‌وبرگشت شبکه کم می‌شود، و اصل طراحی «متن پیش از پارس دیده شود» بهتر رعایت می‌گردد چون متن از اول در textarea و قابل ویرایش است. *رد شده:* Whisper با کلید مستقیم OpenAI؛ و رونویسی با Gemini روی OpenRouter.
- **اسم هیچ اپ خاصی در رابط نمی‌آید.** هر گوشی دیکته‌ی داخلی دارد (میکروفون کیبورد iOS، voice typing در Gboard) — رایگان و بدون ثبت‌نام. گره‌زدن محصول به پلن رایگان یک شرکت ثالث، وابستگی‌ای است که کنترلش دست ما نیست. تب متن فقط دعوت به دیکته می‌کند و ابزارش را به کاربر می‌سپارد.
- **باکت `voice-notes`، ستون `media_assets.transcript` و مقدار `audio` در `media_assets.kind` حذف شدند.** چیزی که استفاده نمی‌شود فقط ابهام می‌سازد؛ برگرداندنش هر وقت لازم شد یک migration کوچک است. مقدار `voice` در `transactions.source` سر جایش ماند چون حذفش از CHECK هزینه‌ی churn دارد بدون فایده.
- **`fetch` خام به‌جای SDK برای OpenRouter.** یک POST ساده‌ی JSON است، و در‌اختیارداشتن `AbortController` همان چیزی است که بودجه‌ی ۳۰ ثانیه را واقعی نگه می‌دارد. *رد شده:* نصب `openai` SDK فقط برای یک فراخوانی، آن هم به سرویسی که OpenAI نیست.
- **`require_parameters: true` روی مسیردهی OpenRouter.** تضمین schema بین providerها یکسان نیست؛ بدون این پرچم، درخواست ممکن است به endpointی برود که `response_format` را فقط راهنمایی تلقی می‌کند. اعتبارسنجی Zod همچنان لایه‌ی دوم است.
- **Structured Outputs به‌جای «در پرامپت بگو فقط JSON بنویس».** اعتبارسنجی Zod به‌عنوان لایه‌ی دوم می‌ماند، ولی retry باید به‌ندرت لازم شود. *رد شده:* اتکا به دستور متنی در پرامپت.
- **`effort: "low"` برای مسیرهای استخراج.** کار کوتاه و حساس به تأخیر است با سقف ۳۰ ثانیه. اگر کیفیت افت کرد به `medium` می‌رود.

## پیش‌پرکردن گام یک ثبت‌نام

- **کشور و ارز حدس زده می‌شوند، نه اینکه روی کانادا قفل باشند.** پیش‌تر `?? "CA"` در فرم و `default 'CAD'` روی ستون یعنی هر کاربری، هر جای دنیا، با کانادا شروع می‌کرد. حالا کشور از سه منبع به‌ترتیب حدس زده می‌شود و ارز از روی کشور می‌آید.
- **منطقه‌ی زمانیِ مرورگر بر geo-IP اولویت دارد.** بخش بزرگی از مخاطب این محصول پشت VPN است؛ VPN آدرس IP را جابه‌جا می‌کند ولی ساعت دستگاه را نه. ترتیب: منطقه‌ی زمانی → منطقه‌ی locale مرورگر (`en-CA` → `CA`) → هدر `x-vercel-ip-country`.
- **حدس مرورگر در `useEffect` اعمال می‌شود، نه در render.** `Intl` روی سرور همان جواب مرورگر را نمی‌دهد و استفاده از آن حین render یعنی hydration mismatch. پس اولین رنگ‌آمیزی حدسِ سرور را نشان می‌دهد و یک فریم بعد اصلاح می‌شود.
- **کشورِ حدس‌زده با همان عنصر امضای محصول علامت می‌خورد** («حدس زدم» کنار برچسب + حاشیه‌ی خط‌چین برنجی)، و با اولین دست‌زدن کاربر solid می‌شود. همان قاعده‌ای که در پرامپت مدل نوشته شده: خواندن یک مقدار حدس نیست، ولی استنتاج‌کردنش هست — نام از حساب گوگل *خوانده* می‌شود پس علامت نمی‌خورد، کشور *استنتاج* می‌شود پس می‌خورد.
- **`country_code` (nullable) نشانه‌ی «کاربر جواب داده» است، نه `base_currency`.** ستون ارز از لحظه‌ی ساخته‌شدن ردیف مقدار دارد (`default 'CAD'`) و نمی‌تواند بین «کاربر انتخاب کرد» و «پیش‌فرض دیتابیس» فرق بگذارد.
- **کشورِ پشتیبانی‌نشده حدس زده نمی‌شود.** منطقه‌ی زمانی‌ای که به فهرست کشورها نمی‌خورد (مثل `Europe/Dublin`) به `null` می‌رسد و فرم روی پیش‌فرض می‌ماند؛ پیش‌پرکردن با کدی که در enum فرم نیست، هم اعتبارسنجی را می‌شکند هم وانمود می‌کند اپ چیزی می‌داند که نمی‌داند.
- **ایمیل هرگز به نام تبدیل نمی‌شود.** زنجیره‌ی نام: `full_name` → `name` → `given_name + family_name` و بعد خالی. پیشوندِ یک ایمیل اسم کسی نیست، و پیش‌پرکردن غلط در اولین سؤال اپ از خالی‌بودن بدتر است. همین زنجیره در trigger پایگاه‌داده هم تکرار شد (`0004`) تا providerهایی که فقط `name` می‌فرستند، کاربر را با فیلد خالی رها نکنند.

## ایران، تومان و ریال

- **ایران به فهرست کشورها اضافه شد و اولِ فهرست نشست.** اپ کاملاً فارسی است؛ کاربر داخل ایران نباید برای پیدا‌کردن کشور خودش از دوازده کشور دیاسپورا رد شود. `Asia/Tehran` (و نام قدیمی‌اش `Iran`) حالا به `IR` می‌رسد، و `fa-IR` در `navigator.languages` هم همین را می‌دهد. *رد شده:* افزودن به ته فهرست با حفظ ترتیب قبلی.
- **دو ارز، نه یکی: `IRT` برای تومان و `IRR` برای ریال.** مردم به تومان حساب می‌کنند ولی قبض و سند رسمی به ریال نوشته می‌شود؛ با یک ارز، یکی از این دو باید هر بار در ذهن کاربر ضرب یا تقسیم شود — دقیقاً کاری که این اپ قرار است از دوشش بردارد. کشور ایران پیش‌فرض تومان می‌گیرد. *رد شده:* فقط تومان.
- **`IRT` کد ISO 4217 نیست و این پذیرفته شد.** برای تومان هیچ کد استانداردی وجود ندارد و `IRT` همان چیزی است که صرافی‌ها روی آن توافق کرده‌اند. ستون `base_currency` هم هیچ‌وقت «فهرست ISO» نبوده، «فهرستِ آنچه این اپ می‌تواند فرمت کند» بوده. *رد شده:* ذخیره‌ی ریال با ضریب ۱۰ و نمایش تومان (یعنی یک واحد نامرئی که هیچ‌جا نوشته نمی‌شود).
- **توان واحد خُرد برای هر دو صفر است.** نه تومان اعشار دارد نه ریالِ در گردش؛ `toMinor("12.6", "IRT")` گرد می‌شود به ۱۳ و عددِ ذخیره‌شده خودِ تومان است. *رد شده:* دو رقم اعشار برای همسانی با بقیه‌ی ارزها (یعنی ضرب همه‌ی مبالغ در ۱۰۰ بدون اینکه کسی آن دو رقم را ببیند یا وارد کند).
- **نماد از `Intl` نمی‌آید، از یک جدول کوچک می‌آید، و بعدِ عدد می‌نشیند.** `Intl` برای `IRR` خودِ «IRR» را چاپ می‌کند و `IRT` را اصلاً نمی‌شناسد. «45,000 تومان» یعنی واحد بعد از عدد است، برخلاف `$45`؛ `AmountInput` هم به همین دلیل نماد را برای این دو ارز آن‌سوی فیلد می‌گذارد. *رد شده:* نمایش «IRR 45,000».
- **قاعده‌ی اعشار در پرامپت مدل از روی ارزِ کاربر ساخته می‌شود.** پیش‌تر در `SHARED_RULES` ثابت نوشته بود «این ارز دو رقم اعشار دارد»؛ برای کاربر تومانی یعنی مدل «۴۵٬۰۰۰ تومان» را ۴٬۵۰۰٬۰۰۰ برمی‌گرداند — صدبرابر، روی همان فیلدی که خودِ پرامپت می‌گوید کاربر نباید مجبور باشد غلط‌بودنش را بفهمد. *رد شده:* یک متن ثابت برای همه‌ی ارزها.
- **`0005` فقط CHECK را باز می‌کند و هیچ مبلغی را دست نمی‌زند.** تغییر ارزِ پایه هیچ‌وقت چیزی را تبدیل نکرده (همان هشداری که در تنظیمات پیش از تغییر نشان داده می‌شود) و این migration ارزِ پایه‌ی هیچ‌کس را عوض نمی‌کند.

## ارز سوئد

- **سوئد از یورو به کرون (`SEK`) اصلاح شد.** سوئد عضو یورو نیست و هیچ‌وقت نبوده؛ نگاشت قبلی یک خطای داده بود، نه یک ساده‌سازی عمدی.
- **`kr` بعد از عدد می‌آید، نه قبلش.** `Intl` با قالب‌بندی en-US که این ماژول رویش استاندارد شده، `kr 1,234.50` می‌دهد؛ سوئدی واحد را آخر می‌نویسد. پس `SEK` در همان `SYMBOL_OVERRIDE`ی می‌نشیند که تومان و ریال در آن هستند — با این تفاوت که اینجا *جای* نماد بازنویسی می‌شود نه خودِ نماد.
- **هیچ ردیفی بازنویسی نشد.** الان هیچ کاربری `country_code = 'SE'` ندارد، ولی اگر داشت، عوض‌کردن ارزش به SEK بدون تبدیل، هر عددی که می‌بیند را حدود یازده برابر می‌کرد — و تبدیل‌کردن بدتر بود، چون معلوم نیست کاربری که فیلدِ برچسب‌خورده به «€» را پر کرده، کرون تایپ کرده یا یورو. تغییر ارز در تنظیمات هشدار دارد؛ جای این تصمیم همان‌جاست.
- **باقی‌مانده و خارج از دامنه:** `toMinor` کاما را جداکننده‌ی هزارگان می‌گیرد، پس `1 234,50` می‌شود ۱۲۳۴۵۰۰۰. این برای آلمان، فرانسه، اسپانیا و ایتالیا هم از قبل صادق بوده و سوئد چیز تازه‌ای به آن اضافه نمی‌کند؛ ورودی مبلغ `dir="ltr"` است و نقطه انتظار می‌رود.

## راه خروج از ثبت‌نام

- **هر صفحه‌ی ثبت‌نام یک کلید «خروج» در هدر دارد** — هر هفت گام و صفحه‌ی خلاصه. تا پیش از این، تنها راه بیرون‌آمدن از یک فلوی هفت‌گامی، دکمه‌ی back مرورگر یا بستن اپ بود؛ «خروج از حساب» هم فقط در تنظیمات وجود داشت، یعنی جایی که کاربرِ نیمه‌ثبت‌نام‌کرده اصلاً به آن راه ندارد. *رد شده:* گذاشتن کلید خروج کنار «ادامه» در فوتر — فوتر بعضی گام‌ها همین حالا سه عنصر دارد (ادامه، «فعلاً رد کن»، جمله‌ی اطمینان) و کنشِ اصلی باید بی‌رقیب بماند.
- **شمارنده‌ی گام به وسط هدر رفت.** هدر حالا سه بخش دارد: بازگشت (راست)، «گام ۲ از ۷» (وسط)، خروج (چپ).
- **خروج یعنی پایانِ نشست، نه پاک‌شدن حساب.** پاسخ‌های داده‌شده سر جایشان می‌مانند و ورود بعدی از همان گام ادامه پیدا می‌کند — همان قولی که جمله‌ی «هر گام را همان لحظه ذخیره می‌کنم» در پای هر گام می‌دهد. متن شیت هم دقیقاً همین را می‌گوید تا کاربر نترسد که با زدنش چیزی را از دست می‌دهد.
- **یک شیت تأیید در میان است، نه خروج با یک ضربه.** کلید در هدر است و کنار دکمه‌ی بازگشت می‌نشیند؛ خروجِ ناخواسته‌ی یک‌ضربه‌ای از فلویی که کاربر نیمه‌کاره رهایش می‌کند، بدترین حالت است. کنش پیش‌فرض در شیت «ادامه‌ی ثبت‌نام» است و خروج دکمه‌ی ثانویه.
- **`queryClient.clear()` پیش از خروج، همان‌طور که در تنظیمات.** وگرنه حساب بعدی روی همان دستگاه، اعداد حساب قبلی را در کش می‌بیند.
- **`type="button"` روی کلید خروج لازم است.** `StepShell` درون `<form>` هر گام رندر می‌شود و دکمه‌ی بی‌`type` در HTML یعنی submit؛ بدون آن، زدن «خروج» گام را ثبت می‌کرد.

## جداکننده‌ی اعشار

- **کاما دیگر همیشه جداکننده‌ی هزارگان نیست.** هفت کشور از سیزده کشور فهرست — سوئد، آلمان، فرانسه، اسپانیا، ایتالیا، اتریش، بلژیک — اعشار را با کاما می‌نویسند، و `toMinor` هر کاما را حذف می‌کرد؛ یعنی `1234,50` می‌شد ۱۲۳٬۴۵۰. خطای صدبرابری، بی‌سروصدا.
- **قاعده:** کاما اعشار است اگر (الف) نقطه‌ای پیش از آن آمده باشد — شکل آلمانی `1.234,50` — یا (ب) تنها کامای رشته باشد و حداکثر دو رقم بعدش بیاید — شکل `1234,50` و `0,5`. در غیر این صورت هزارگان است، پس `1,234` هنوز هزار و دویست و سی‌وچهار می‌ماند.
- **رشته‌ی بدون کاما دست‌نخورده می‌ماند.** نقطه همان جداکننده‌ی اعشار بی‌ابهامی می‌ماند که همیشه در این ماژول بوده: `1.005` یعنی یک و نیم سنت که رُند می‌شود، نه هزار و پنج. *رد شده:* قاعده‌ی متقارن «سه رقم پشت جداکننده یعنی هزارگان» — درست‌تر به‌نظر می‌رسید ولی `45.555` و `1.005` را می‌شکست، و هر دو خطا هزاربرابری‌اند. وقتی هر دو خوانش ممکن است، خوانشی که این ماژول همیشه داشته برنده است.
- **گروه‌بندی راستی‌آزمایی می‌شود، نه فرض.** `1,23,456` (شکل هندی) و `1.2,3` خطا می‌دهند به‌جای اینکه بی‌صدا به عددی دیگر تبدیل شوند. `12.3.4` مثل قبل خطا می‌دهد.
- **`،` (U+060C) به کاما نگاشت می‌شود، نه به نقطه.** این کاراکتر کاماست نه علامت اعشار — کلیدی است که فارسی‌زبان برای هزارگان به آن می‌رسد. قبلاً به نقطه تبدیل می‌شد، پس `۱،۲۳۴` می‌شد `1.234` یعنی یک و ربع. حالا می‌شود ۱۲۳۴. علامت اعشار فارسی `٫` (U+066B) است و همچنان به نقطه نگاشت می‌شود.

## آپلود صورت‌حساب بانکی

### تطبیق با چیزی که قبلاً ثبت شده

- **تشخیص تکراری‌بودن حساب‌وکتاب است، نه قضاوت؛ پس در کد انجام می‌شود نه در مدل.** `lib/import/reconcile.ts` یک تابع خالص و تست‌شده است. مدلی که از او بپرسی «این ردیف قبلاً ثبت شده؟» بار دوم روی همان فایل جواب دیگری می‌دهد، و بدترین اتفاق ممکن در این قابلیت این است که پولی سه‌شنبه دوباره شمرده شود و چهارشنبه نه. *رد شده:* دادن فهرست تراکنش‌های موجود به مدل و پرسیدن کدام‌ها تازه‌اند.
- **مبلغ و جهت باید دقیقاً یکی باشند؛ فقط تاریخ اجازه‌ی لغزش دارد، تا سه روز.** خرید با کارت روز جمعه، دوشنبه در حساب می‌نشیند؛ سه روز کوتاه‌ترین پنجره‌ای است که از یک آخر هفته رد می‌شود. مبلغ همان چیزی است که هویت تراکنش را می‌سازد.
- **شرح بانک هیچ‌وقت در تطبیق دخالت نمی‌کند.** بانک می‌نویسد «POS-472913 HYPERSTAR» و کاربر نوشته بود «خرید». اگر این دو را مجبور به توافق کنیم، پولی که قبلاً ثبت شده «تازه» گزارش می‌شود — و دفتر دوباره‌شماری می‌کند.
- **تطبیق یک‌به‌یک است.** سه قهوه‌ی هم‌مبلغ در صورت‌حساب در برابر یک قهوه در دفتر، دو تای تطبیق‌نیافته باقی می‌گذارد. همین جواب درست است.
- **پاس‌ها از تاریخ دقیق به بیرون حرکت می‌کنند** (اختلاف صفر، بعد یک، بعد دو، بعد سه). بدون این، ردیفی که سه روز فاصله دارد و زودتر در فایل آمده، تراکنش را می‌قاپد و ردیفی که دقیقاً همان روز است «تازه» اعلام می‌شود. تست‌شده، و ترتیب فایل هم نتیجه را عوض نمی‌کند.
- **آپلود دوباره‌ی همان صورت‌حساب هیچ مسیر ویژه‌ای ندارد.** ردیف‌هایی که بار اول نوشته شدند حالا در دفترند، پس همان قاعده‌ای که بقیه را می‌شناسد آن‌ها را هم می‌شناسد. *رد شده:* اثر انگشت (fingerprint) روی هر ردیف و جدول جداگانه برای آن — یک مکانیزم دوم برای کاری که مکانیزم اول از قبل انجام می‌دهد.
- **ردیف‌های «قبلاً ثبت شده» هم در گزارش نشان داده می‌شوند، جمع‌شده.** تطبیق می‌تواند در هر دو جهت اشتباه کند، ولی فقط یکی از این دو خودش را نشان می‌دهد: ردیفی که اشتباهاً «تازه» خوانده شده در فهرست است و کاربر تیکش را برمی‌دارد؛ ردیفی که اشتباهاً «قبلاً ثبت شده» خوانده شده، بی‌صدا غیب می‌شد. پس هر دو روی صفحه‌اند.

### تاریخ، ارز، و چیزهایی که جواب درست دارند

- **مدل تاریخ را همان‌طور که چاپ شده برمی‌گرداند و می‌گوید کدام تقویم است؛ تبدیل در `lib/jalali.ts` انجام می‌شود.** صورت‌حساب بانک ایرانی `1404/06/26` می‌نویسد. مدلی که تبدیل را اشتباه کند، تراکنش را در ماه اشتباه می‌نشاند و هیچ لایه‌ی بعدی نمی‌تواند بفهمد. الگوریتم بروکوفسکی (همان که `jalaali-js` منتشر کرده) با ۱۲ تاریخِ لنگر تست شده: اول فروردین، روز کبیسه‌ی ۱۳۹۹، مرز ماه‌های ۳۱ و ۳۰ روزه، و سال‌های شکست چرخه.
- **برچسب تقویمِ مدل با بزرگیِ خودِ سال تصحیح می‌شود.** سال جلالی نمی‌تواند ۲۰۲۵ باشد و سال میلادی نمی‌تواند ۱۴۰۴؛ هیچ هم‌پوشانی‌ای در کار نیست، پس عدد سال حرف آخر را می‌زند حتی وقتی مدل برچسب را غلط زده.
- **واحد صورت‌حساب را کاربر انتخاب می‌کند، و پیش‌فرضش برای کاربر تومانی «ریال» است.** بانک ایرانی به ریال چاپ می‌کند و کاربر به تومان حساب می‌کند؛ خواندن یکی به‌جای دیگری یعنی خطای ده‌برابری روی هر ردیف. `convertMinor` فقط همین یک جفت را می‌شناسد و هر جفت دیگری استثنا می‌دهد — تبدیل ارز واقعی نرخِ روزِ هر تراکنش را می‌خواهد که این اپ ندارد.
- **دسته‌ای که پیدا نشود ردیف را حذف نمی‌کند، به «متفرقه» می‌رود.** این تفاوت عمدی با مسیر فاکتور است: آنجا دسته‌ی ناشناس یعنی کل تراکنش دور انداخته می‌شود. خروجی‌ای که نمی‌دانیم بابت چه بوده، هنوز خروجی است — و ثبتش به‌عنوان «متفرقه» دقیقاً همان چیزی است که خواسته شده بود.
- **دسته در صورت‌حساب همیشه «حدس زدم» علامت می‌خورد.** هیچ‌کس دسته را از روی گردش حساب *نمی‌خواند*؛ همیشه از روی شرح *استنتاج* می‌شود. همان قاعده‌ای که در کل محصول برقرار است، اینجا در کد اعمال می‌شود نه به یادِ مدل سپرده می‌شود.
- **ردیفی که تاریخش یا مبلغش قابل اعتماد نیست کلاً حذف می‌شود.** ردیفِ حذف‌شده در شمارشِ گزارش دیده می‌شود؛ ردیفِ حدس‌زده‌شده نه.

### خواندن فایل

- **مانده‌ی حساب مهم‌ترین تله است و صریح در پرامپت آمده.** هر سطر صورت‌حساب یک ستون مانده دارد که معمولاً بزرگ‌ترین عدد آن سطر است. تست E2E عمداً فیکسچری دارد که مانده‌اش چند برابر مبلغ است.
- **PDF مستقیم به مدل می‌رود، متنش استخراج نمی‌شود.** استخراج متن از PDF فارسی رشته‌ها را برعکس و حروف را جدا می‌کند؛ مدلی که خودِ صفحه را می‌بیند قابل‌اعتمادتر است. `plugins: [{ id: "file-parser", pdf: { engine: "native" } }]` روی OpenRouter تا یک پاس OCR جداگانه‌ی گران‌تر و بدتر صورت‌حساب نخورد. *رد شده:* `unpdf` یا `pdf-parse` روی سرور.
- **CSV در خودِ کد تکه‌تکه می‌شود، هشتاد ردیف در هر فراخوانی، با تکرار سربرگ.** دقیق است چون هیچ ردیفی نمی‌تواند در دو تکه بیفتد، پس هیچ ردیفی دو بار خوانده نمی‌شود. PDF و عکس یک‌جا می‌روند: تکه‌کردنشان حدس است و ردیفی که دو بار خوانده شود بدتر از ردیفی است که دیر خوانده شود.
- **چند فایل در هر آپلود (تا ۱۲).** صورت‌حساب سه‌ماهه به‌همان اندازه که یک فایل بلند است، سه خروجی ماهانه هم هست، و صورت‌حسابِ عکس‌گرفته‌شده صفحه‌به‌صفحه می‌آید. همه با هم و به‌عنوان یک دوره تطبیق داده می‌شوند.
- **مهلت مدل برای صورت‌حساب ۱۵۰ ثانیه است، نه ۳۰.** سقف ۳۰ ثانیه برای یک فاکتور درست است؛ دویست ردیف JSON به‌تنهایی یک دقیقه تولید طول می‌کشد. `TIMEOUT_MS` ثابت به پارامتر با همان پیش‌فرض قبلی تبدیل شد تا مسیرهای موجود دست‌نخورده بمانند.
- **مسیر خواندن route handler است نه server action، و نتیجه‌اش در دیتابیس می‌نشیند.** کاربری که وسط خواندن تب را ببندد، برمی‌گردد و گزارشِ کامل را می‌بیند، نه هیچ.

### نوشتن

- **هیچ چیزی بدون تأیید کاربر نوشته نمی‌شود، ولی تأیید یک ضربه است.** همه‌ی ردیف‌های تازه از اول تیک‌خورده‌اند و یک دکمه همه را ثبت می‌کند. اصل محصول («خروجی مدل بدون دیده‌شدن نوشته نمی‌شود») سر جایش می‌ماند بدون اینکه کاربر مجبور شود چهل بار تیک بزند.
- **مبلغ از ردیفِ ذخیره‌شده خوانده می‌شود، هرگز از درخواست.** کاربر می‌تواند دسته، تاریخ و فروشنده را عوض کند و انتخاب کند کدام ردیف‌ها ثبت شوند — ولی نمی‌تواند بگوید یک ردیف چقدر می‌ارزد.
- **قفلْ تغییر وضعیت ردیف است، نه یک flag.** `match_status` از `new` به `imported` با یک update شرطی می‌رود و همان ردیف‌هایی که واقعاً عوض شدند برمی‌گردند؛ درخواست دوم چیزی برای برداشتن پیدا نمی‌کند. ایندکس یکتای `transactions.statement_line_id` زیرِ آن است، همان تضمینی که کار تکرارشونده‌ها دارد.
- **اگر ارز پایه بین خواندن و ثبت عوض شده باشد، ثبت رد می‌شود.** همه‌ی مبالغ ذخیره‌شده به ارز قبلی‌اند؛ نوشتنشان با برچسب ارز جدید، آشغالِ بی‌صدا است.
- **فقط یک import می‌تواند باز باشد.** دو گزارش نیمه‌کاره با دوره‌های هم‌پوشان، راهی است برای ثبت دوباره‌ی یک پول.
- **ردیف‌ها بعد از ثبت هم نگه داشته می‌شوند.** گزارش از روی همان‌ها رندر می‌شود، سندِ پشت هر تراکنشِ نوشته‌شده‌اند، و شرح بانک عیناً در `note` تراکنش می‌نشیند تا هر ردیف تا خودِ صورت‌حساب قابل ردیابی باشد.
- **تیکِ «دسته‌ها را خودم نگاه کردم» پیش‌فرض خاموش است.** روشن‌کردنش یعنی پذیرشِ آگاهانه‌ی همه‌ی دسته‌ها، که دقیقاً معنی تأییدکردن یک فیلد است. خاموش بماند، ردیف‌ها با خط‌چین «حدس زدم» ثبت می‌شوند و در شمارنده‌ی «نیاز به بررسی» می‌مانند.

### راه ورود روی موبایل — تصحیح

- **فیچر اول بار جایی گذاشته شد که کاربر اصلی‌اش پیدایش نمی‌کرد.** استدلال اولیه این بود که «تب‌بار چهار قلم دارد و حساب‌ها هم از قبل زیر تنظیمات است، پس الگو را به هم نزن». آن استدلال از داخل کد درست بود و یک چیز را نادیده گرفت: اپ عمدتاً روی گوشی استفاده می‌شود، و نوار کناری — تنها جایی که این بخش‌ها دیده می‌شدند — زیر ۹۶۰ پیکسل اصلاً وجود ندارد. فیچری که دو لایه زیر «تنظیمات» است، برای کاربرش اضافه نشده. **این را کاربر گزارش کرد، نه تست و نه بازبینی کد.**
- **راه‌حل: کارت روی داشبورد، نه دست‌بردن در تب‌بار.** تب‌بار دست‌نخورده ماند چون چهارتا بودنش تصمیم طراحیِ مستقلی است که هنوز معتبر است؛ چیزی که کم بود یک نقطه‌ی ورود دیده‌شدنی بود، نه یک تب پنجم. *رد شده:* جایگزینی «هدف‌ها» در تب‌بار (یک فیچر را به قیمت فیچر دیگر پیدا می‌کرد) و تب‌بار پنج‌تایی (عرض هر قلم روی ۳۷۵ پیکسل از ۹۴ به ۷۵ پیکسل می‌افتاد).
- **کارت حتی وقتی هیچ دوره‌ای نیست هم رندر می‌شود.** نقطه‌ی ورودی که فقط بعد از پیداکردنِ مقصد ظاهر شود، نقطه‌ی ورود نیست. همین برای کارت حساب‌ها هم اعمال شد: تا قبل از این، `AccountBalances` وقتی حسابی نبود `null` برمی‌گرداند — یعنی تنها راهِ رسیدن به صفحه‌ی حساب‌ها از روی گوشی، دقیقاً در لحظه‌ای که کاربر می‌خواست اولین حساب را بسازد، وجود نداشت.
- **حالت خالی یک کارت کامل است، نه یک نوار یک‌خطی.** نسخه‌ی اول یک نوار نازکِ خط‌چین بود که کنار کارت‌های اطرافش مثل پانویس خوانده می‌شد. کارت با آیکون، عنوان، یک جمله توضیح و دکمه‌ی تمام‌عرض، همان وزنی را دارد که بقیه‌ی داشبورد دارد. منبع: گزارش کاربر («در باکس خیلی کوچکی می‌بینم»).
- **کارت دنگ و دونگ آخرین بخش داشبورد است.** داشبورد درباره‌ی *این ماهِ* تو است؛ دنگ و دونگ فیچری در کنار آن است، نه بخشی از آن. پس بعد از تراکنش‌های اخیر می‌آید، نه وسط کارت‌های ماه.
- **تستِ راه ورود با `href` بسته می‌شود، نه با متن لینک، و به `<main>` محدود است.** متن در دو حالت فرق می‌کند («دنگ و دونگ» در برابر «ساختن اولین دوره»)، و نوار کناری لینک خودش را به همان مقصد دارد که زیر ۹۶۰ پیکسل فقط *مخفی* است نه غایب — تستی که آن را ببیند، در حالی پاس می‌شود که گوشی همچنان هیچ راهی ندارد. هر دو مورد را خودِ تست موقع نوشتن گرفت.
- **کارت‌های حساب‌ها و دنگ و دونگ از شرط «ماه خالی» بیرون آمدند.** قبلاً ماهی بدون تراکنش کل پایین داشبورد را با دعوت به ثبت اولین هزینه جایگزین می‌کرد — یعنی دقیقاً در وضعیتی که کاربر تازه‌وارد دارد و بیشتر از همیشه دنبال این صفحه‌ها می‌گردد، هیچ راهی به آن‌ها نبود. تست e2e روی یک ماه خالی این را می‌بندد.
- **هیچ جمعی بین دوره‌ها زده نمی‌شود.** هر دوره ارز خودش را دارد و این اپ تبدیل نمی‌کند، پس کارت به‌جای یک عدد کل، هر دوره را جدا نشان می‌دهد. `dong_my_balances()` تراز خودِ کاربر را در همه‌ی دوره‌ها با یک round trip می‌دهد؛ صدازدنِ `dong_balances()` به ازای هر دوره یعنی یک رفت‌وبرگشت به ازای هر سفری که کاربر تا حالا رفته.

### باقی‌مانده و خارج از دامنه

- **اکسل (`.xlsx`/`.xls`) پشتیبانی نمی‌شود.** PDF، CSV و عکس کافی‌اند و هر بانکی حداقل یکی از این سه را می‌دهد؛ افزودن یک کتابخانه‌ی اکسل برای همین یک مسیر، وابستگیِ بزرگی است که بعداً هم می‌شود اضافه کرد.
- **انتقال بین حساب‌های خودِ کاربر تشخیص داده نمی‌شود** و مثل هر ورودی/خروجی دیگری ثبت می‌شود. برای تشخیصش باید اپ از وجود حساب دوم خبر داشته باشد، که ندارد.
- **سقف ۵۰ پردازش هوشمند در روز مشترک است** بین متن، فاکتور و صورت‌حساب. صورت‌حسابی که از سقف رد شود، هرچه تا آن لحظه خوانده را نگه می‌دارد و گزارشِ ناقص را با توضیح نشان می‌دهد، به‌جای اینکه همه‌چیز را دور بیندازد.

## حساب‌های بانکی

### موجودی، و اینکه چرا هیچ‌جا ذخیره نمی‌شود

- **موجودی یک ستون نیست؛ هر بار از روی دفتر حساب می‌شود.** هر حساب یک عدد دارد که کاربر نوشته (`opening_balance`) و یک تاریخ که آن عدد مالِ آن است (`opening_balance_on`)؛ موجودی یعنی همان عدد به‌علاوه‌ی هر تراکنشی که از آن تاریخ به بعد به آن حساب خورده. تابع `account_balances()` این را در یک رفت‌وبرگشت برای همه‌ی حساب‌ها می‌دهد. *رد شده:* ستون `current_balance` که با trigger کم و زیاد شود — آن ستون باید در هشت مسیر درست بماند (درج، ویرایش مبلغ، ویرایش نوع، ویرایش تاریخ، عوض‌کردن حساب، حذف نرم، برگرداندن، حذف حساب) و یک مسیرِ جاافتاده یعنی عددی که بی‌صدا غلط است و دیگر هم قابل بازسازی نیست. چیزی که مشتق می‌شود، نمی‌تواند از چیزی که ازش مشتق شده فاصله بگیرد.
- **تراکنشِ قبل از تاریخِ موجودی، موجودی را تکان نمی‌دهد.** کاربر عددی را می‌نویسد که همین الان در گوشی‌اش دیده؛ خرید سه‌شنبه‌ی قبل از قبل، از قبل داخل آن عدد هست. بدون این قاعده، آپلود صورت‌حساب سه‌ماهه در روزِ ساختنِ حساب، موجودیِ درست را سه ماه عقب می‌برد.
- **«بانک عدد دیگری می‌گوید» با نوشتنِ دوباره‌ی موجودی حل می‌شود، نه با پیداکردن ردیف گم‌شده.** `restateBalance` فقط لنگر را جابه‌جا می‌کند: هیچ تراکنشی پاک یا اصلاح نمی‌شود و اختلاف جذب می‌شود. *رد شده:* ساختن یک تراکنش «تعدیل» به‌اندازه‌ی اختلاف — پولی که هیچ‌وقت خرج نشده در فهرست هزینه‌ها و در نمودار دسته‌ها می‌نشیند.
- **موجودی منفی مجاز است و تنها جای اپ است که هست.** کارت اعتباری بدهی نگه می‌دارد؛ نپذیرفتنِ عدد منفی یعنی مجبورکردن کاربر به دروغ‌گفتن درباره‌ی چیزی که خودش می‌داند. به همین دلیل `normalizeDigits` حالا `−` (U+2212) را هم قبول می‌کند: `formatMoney` منفی را با همین کاراکتر چاپ می‌کند و همان رشته به فرم ویرایش برمی‌گردد — یعنی خروجیِ خودِ ماژول باید دوباره خوانده شود.

### حساب روی تراکنش

- **`account_id` اختیاری است و می‌ماند.** پول نقدِ توی جیب هزینه‌ی واقعی است و به هیچ حسابی نمی‌خورد، و هر ردیفی که پیش از این migration نوشته شده هم حسابی ندارد. «بدون حساب» یک جوابِ درست است، نه یک فیلدِ پرنشده.
- **کنترلِ انتخاب حساب تا وقتی کاربر حسابی نساخته، اصلاً رندر نمی‌شود.** فیلدی که همیشه خالی است و جوابی هم برایش وجود ندارد، روی فرمی که قولش ده ثانیه است، از نبودنش بدتر است.
- **یک حساب پیش‌فرض، با ایندکس یکتا در دیتابیس نه با کد.** دو ردیفِ `is_default` یعنی فرم ثبت بی‌سروصدا هرکدام که اول مرتب شد را انتخاب می‌کند. اگر کاربر حساب دومی را پیش‌فرض کند، اولی در همان action پایین می‌آید چون وگرنه insert روی ایندکس خودش می‌خورد.
- **وقتی فقط یک حساب باز هست همان انتخاب می‌شود؛ وقتی چند تا هست، هیچ‌کدام.** حدسِ غلط بدتر از نبودِ حدس است: کاربر دلیلی ندارد به فیلدی که از قبل پر است نگاه کند.
- **حسابِ بسته در فهرست نیست، مگر همان تراکنش رویش باشد.** وگرنه ویرایشِ فروشنده‌ی یک ردیفِ قدیمی، بی‌صدا پول را به حساب دیگری منتقل می‌کرد.
- **هزینه‌ی ثابت هم حساب دارد.** اجاره هر ماه از یک حسابی بیرون می‌رود چه کسی به اپ بگوید چه نگوید؛ بدون این، تنها هزینه‌ای که خودِ اپ ثبت می‌کند تنها هزینه‌ای می‌شد که موجودی را تکان نمی‌دهد. `post_recurring_for_month` فقط برای حمل همین ستون بازنویسی شد.

### صورت‌حساب و حساب

- **هر صورت‌حساب مالِ یک حساب است، و همین تطبیق را هم محدود می‌کند.** ردیفی که به حسابِ *دیگری* خورده، هرقدر هم مبلغ و تاریخش بخواند، نصفه‌ی دیگرِ این صورت‌حساب نیست — دو کارت که یک اشتراک را در یک روز می‌پردازند حالتِ عادی است نه استثنا، و تطبیق‌دادنشان یعنی یکی از آن دو «قبلاً ثبت شده» اعلام و بی‌صدا گم می‌شود. `ledgerForAccount` این را جدا می‌کند و تست دارد.
- **تراکنشِ بی‌حساب همچنان تطبیق داده می‌شود.** آن دقیقاً محتمل‌ترین نصفه‌ی دیگرِ یک ردیف صورت‌حساب است: چیزی که کاربر پیش از داشتن حساب‌ها، یا بدون گفتنِ اینکه از کجا، ثبت کرده. importی که خودش حسابی ندارد هم با همه‌چیز تطبیق داده می‌شود، همان‌طور که هر import پیش از این قابلیت.
- **تراکنش‌های تطبیق‌یافته، حسابشان پر نمی‌شود.** وسوسه‌اش هست — می‌دانیم آن ردیف مالِ این حساب بوده — ولی این یعنی نوشتن روی ردیف‌هایی که کاربر از قبل داشته، بدون اینکه پرسیده باشیم.

### باقی‌مانده و خارج از دامنه

- **انتقال بین دو حسابِ خودِ کاربر هنوز یک مفهوم جدا نیست.** الان به‌صورت یک خروجی از حساب مبدأ و یک ورودی به حساب مقصد ثبت می‌شود، که موجودیِ هر دو را درست نگه می‌دارد ولی در درآمد و هزینه‌ی ماه دوبار شمرده می‌شود. جوابِ درستش یک `type` سومِ `transfer` است که KPIها و نمودار دسته‌ها باید از آن رد شوند — تغییری در سرتاسر داشبورد، نه یک ستون.
- **حساب به ارز غیر از ارز پایه پذیرفته نمی‌شود.** ارز از پروفایل خوانده می‌شود و روی حساب ذخیره؛ تبدیل واقعی نرخِ روزِ هر تراکنش را می‌خواهد که این اپ ندارد — همان دلیلی که `convertMinor` فقط تومان/ریال را می‌شناسد.
- **حسابِ تراکنش‌دار پاک نمی‌شود؛ بسته می‌شود.** کلید FK روی `set null` است، پس پاک‌کردن، تراکنش‌ها را در دفتر نگه می‌داشت ولی بی‌صدا از هر موجودی‌ای که بخشی‌اش بودند بیرون می‌برد. «دیگر ازش استفاده نمی‌کنم» جوابش بستن است؛ پاک‌کردن فقط برای «اشتباه تایپش کردم» و تا وقتی خالی است.

## شناسه‌ی ردیف در فرم‌ها

- **`id` دیگر یک فیلد فرم نیست؛ از خودِ ردیفی که ویرایش می‌شود می‌آید.** هر چهار شیت (منبع درآمد، هزینه‌ی ثابت، هدف، تراکنش) شناسه را در یک `<input type="hidden" {...register("id")} />` نگه می‌داشتند. برای ردیفِ تازه مقدارش `""` است، `""` یک uuid نیست، و هیچ `Field`ی `errors.id` را نشان نمی‌داد — یعنی زدنِ «ذخیره» روی «افزودن منبع درآمد» **بی‌صدا هیچ کاری نمی‌کرد**. این باگ روی main بود و با همین قابلیت پیدا شد، چون شیت حساب‌ها هم از روی همان الگو نوشته شده بود.
- **شیت تراکنش هم عوض شد، با اینکه خراب نبود.** آن یکی همیشه روی یک ردیفِ موجود باز می‌شود، پس شناسه‌اش همیشه uuid واقعی بود؛ ولی تنها بازمانده‌ی یک الگوی خراب، همان چیزی است که دفعه‌ی بعد از رویش کپی می‌شود.
- **`tests/e2e/records.spec.ts` نگهبانش است:** یک منبع درآمد تازه می‌سازد و بعد پاکش می‌کند. باگی که هیچ پیامی نمی‌دهد، فقط با یک تست که واقعاً دکمه را می‌زند گرفته می‌شود.

## نشانگر توسعه‌ی Next

- **`devIndicators.position` به `top-right` رفت، و دلیلش چیدمان است نه سلیقه.** اپ RTL است، پس `end` یعنی سمت چپ — همان‌جایی که دکمه‌ی شناور «ثبت هزینه» و دکمه‌ی حذفِ هر شیت می‌نشیند. نشان‌گرِ dev در گوشه‌ی چپ‌پایین دقیقاً روی آن‌ها می‌افتد و در عرض ۳۷۵ پیکسل کلیک را می‌بلعد؛ Playwright هم همان‌جا گیر کرد. فقط روی dev اثر دارد.

## انتقال بین حساب‌ها

- **یک ردیف، دو حساب — نه دو ردیف.** دفترداری دوطرفه می‌گفت دو ردیف (یکی از مبدأ بیرون، یکی به مقصد تو) و برای یک دفتر کل عمومی حق داشت. اینجا یعنی دو ردیف که باید تا ابد سر مبلغ، تاریخ و حتی وجودِ خودشان با هم توافق کنند: یکی را ویرایش کنی باید آن یکی را هم، یکی را حذف کنی باید آن یکی را هم، و هر فهرستِ اپ هم آخرش باید یاد بگیرد آن دو را یکی نشان دهد. یک ردیف که نام هر دو حساب را دارد نمی‌تواند با خودش اختلاف پیدا کند.
- **هزینه‌اش در دو جای مشخص پرداخت می‌شود و هر دو تست دارند:** تابع `account_balances()` که باید هر دو ستون را ببیند، و `ledgerForAccount` که یک انتقال را به همان یک سمتی تبدیل می‌کند که صورت‌حسابِ آن حساب نشان می‌دهد.
- **انتقال نه درآمد است نه هزینه، و این قاعده در `lib/queries/transactions.ts` نشسته نه در صفحه‌ها.** `monthTotals` و `monthlySeries` تنها جایی‌اند که هر عدد ماه از آن‌ها ساخته می‌شود؛ گذاشتن این فیلتر در هر صفحه یعنی روزی یکی‌اش یادش می‌رود و آن‌وقت خرجِ ماه بی‌صدا دو برابر می‌شود — دقیقاً همان عددی که کل محصول با آن قضاوت می‌شود.
- **در فهرست تراکنش‌ها بی‌رنگ و بی‌علامت نشان داده می‌شود** و زیرش نوشته «از A به B». نه قرمز نه سبز: پول هنوز مالِ خودِ کاربر است، فقط جای دیگری است. جمعِ فیلترشده هم صریحاً می‌گوید «n انتقال در جمع نیست» — جمعی که ردیف‌های روی همان صفحه را بی‌خبر کنار بگذارد، جمعی است که کاربر نمی‌تواند راستی‌آزمایی‌اش کند.
- **گزینه‌ی «انتقال» تا وقتی دو حساب باز نداشته باشی اصلاً نیست.** با یک حساب، انتقال نیست؛ برداشت است.
- **انتقال در شیت ویرایش به هزینه تبدیل نمی‌شود.** برای این کار باید تصمیم گرفت سرِ حسابی که پول به آن رسیده چه می‌آید، و جواب صادقانه این است که پاکش کنی و چیزی که واقعاً اتفاق افتاده را ثبت کنی.

## بروزرسانی ماهانه‌ی حساب

- **نقطه‌ی شروع، خودِ حساب است نه صفحه‌ی import.** هر ردیف حساب یک کلید «بروزرسانی» دارد که مستقیم به `/import?account=<id>` می‌رود، و آنجا انتخابگر حساب اصلاً رندر نمی‌شود: سؤال از قبل جواب داده شده، و یک `<select>` که می‌تواند بی‌صدا جوابش را عوض کند، راهی است برای اینکه صورت‌حساب به اسم حساب اشتباه ثبت شود.
- **حالا به‌ازای هر حساب یک import می‌تواند باز باشد، نه یکی در کل.** قاعده‌ی قبلی («فقط یکی») برای جلوگیری از دوبار ثبت‌کردن یک پول بود؛ حالا که صورت‌حساب به حساب تعلق دارد و تطبیق فقط ردیف‌های همان حساب را نگاه می‌کند، دو گزارش روی دو حساب نمی‌توانند به هم بخورند — و کاربری که اول ماه از او خواسته‌ایم همه‌ی حساب‌هایش را چک کند، زیر قاعده‌ی قبلی مجبور بود دقیقاً یکی‌یکی جلو برود.
- **مانده‌ی پایان دوره از مدل خواسته می‌شود، ولی در فیلدِ خودش و با اسمِ خودش.** تمام قاعده‌های ردیف در پرامپت طولشان را صرف این می‌کنند که بگویند ستون مانده را به‌عنوان مبلغ نخوان؛ پس مانده جداگانه و صریح پرسیده می‌شود، هیچ‌وقت ردیف نیست، و منفی هم می‌تواند باشد. تست E2E هر دو سمتش را می‌گیرد: هیچ مانده‌ای به‌عنوان مبلغِ ردیف ظاهر نشود، و مانده‌ی پایان دوره خوانده شده باشد.
- **عددی که مدل خوانده مثل هر خوانده‌ی دیگری «حدس» است.** در گزارش با علامتِ «از صورت‌حساب خواندم» کنار برچسب می‌نشیند و قابل ویرایش است. اگر صورت‌حساب مانده‌ای چاپ نکرده باشد، فیلد خالی است و از کاربر خواسته می‌شود عددی که در اپ بانکش می‌بیند را بنویسد — همان سؤال، از منبعی بهتر.
- **مرحله‌ی موجودی بعد از ثبت ردیف‌ها می‌آید، نه قبلش.** اختلاف قبل از اضافه‌شدنِ ردیف‌های جامانده معنایی ندارد؛ نشان‌دادنِ یک اختلاف ترسناک که بعد از ثبت خودش حل می‌شود، فقط کاربر را می‌ترساند. گزارش قبل از ثبت فقط *می‌گوید* بانک چه عددی نوشته، مقایسه نمی‌کند.
- **اختلاف باقی‌مانده جذب می‌شود، تراکنشِ «تعدیل» ساخته نمی‌شود.** هرچه بعد از ثبتِ ردیف‌ها باقی می‌ماند — کارمزدی که کسی ریز نکرده، ردیفی که مدل انداخته، خریدی از قبلِ ساخته‌شدنِ حساب — با جلوبردن لنگرِ موجودی جذب می‌شود. ساختن یک تراکنش به‌اندازه‌ی اختلاف یعنی پولی که هیچ‌وقت خرج نشده در فهرست هزینه‌ها و در نمودار دسته‌ها می‌نشیند.
- **`last_reconciled_at` از روی آخرین import محاسبه نمی‌شود، جدا ذخیره می‌شود.** import‌ی که چیزی پیدا نکند هم یک بارِ چک‌کردن است — در واقع بهترین حالت ممکن — و عجیب بود اگر بعدش حساب همچنان به کاربر تذکر می‌داد کاری را بکند که همین الان کرد.
- **مبلغ از خودِ درخواست می‌آید، برخلاف ردیف‌ها که از دیتابیس خوانده می‌شوند.** عمدی است: موجودی چیزی است که کاربر جلوی چشمش دارد و تصحیحش می‌کند. چیزی که نمی‌تواند بکند، عوض‌کردن چیزی است که از قبل ثبت شده.

## یادآوری اول ماه

- **داخل اپ، نه Web Push.** کاری که خواسته می‌شود — پیداکردن پرینت حساب، آپلودش، نگاه‌کردن به چیزی که جا مانده — از روی یک نوتیفیکیشن قابل انجام نیست؛ فقط همین‌جا قابل انجام است. نواری که لحظه‌ی بازکردنِ اپ دیده می‌شود، همان یادآوری است در تنها لحظه‌ای که می‌شود به آن عمل کرد، و نه اجازه‌ی نوتیفیکیشن می‌خواهد، نه service worker (که این پروژه عمداً ندارد)، نه یک job زمان‌بندی‌شده که باید سرِ منطقه‌های زمانی درست عمل کند. *رد شده:* push و ایمیل — هر دو زیرساخت تازه می‌خواهند برای رساندنِ پیامی که مقصدش خودِ همین صفحه است.
- **«چک‌شده» یعنی «از اولِ این ماه چک‌شده»، نه «ظرف سی روز گذشته».** بانک‌ها اول ماه صورت‌حساب می‌دهند؛ پنجره‌ی سی‌روزه هر ماه کمی جلوتر می‌رود تا جایی که یادآوری و صورت‌حساب دیگر با هم نمی‌خوانند.
- **مقایسه در منطقه‌ی زمانی خودِ کاربر انجام می‌شود.** `last_reconciled_at` یک لحظه است؛ حسابی که ساعت ۱ بامدادِ اول ماه به وقت تهران چک شده، به وقت UTC در ماه قبل چک شده. بدون این، یادآوری همان لحظه‌ای که جوابش را می‌گیری دوباره ظاهر می‌شود. تست دارد.
- **حسابی که همین ماه ساخته شده و هیچ‌وقت چک نشده، تذکر نمی‌گیرد.** هنوز اتفاقی رویش نیفتاده و صورت‌حسابی هم وجود ندارد که با آن چک شود.
- **نوار حساب‌ها را اسم می‌برد، نمی‌شمارد.** «خانه و پس‌انداز» چیزی است که کاربر می‌تواند تصورش کند؛ «۲ حساب» یک تکلیف است.
- **نوار تا انجام‌شدن می‌ماند، یک‌بار ظاهر نمی‌شود و نمی‌رود.** یادآوری‌ای که فقط اول ماه یک‌بار دیده شود، همان یادآوری‌ای است که در شلوغیِ اول ماه گم می‌شود.

## تبدیل نوع تراکنش

- **قفلِ «انتقال به هزینه تبدیل نمی‌شود» برداشته شد، چون غلط بود.** استدلال اولیه این بود که تغییر نوع، موجودی حسابِ مقصد را هم بی‌صدا عوض می‌کند. حالتی که ندیده بودم این است که مسیر import خودش همین ردیف‌ها را می‌سازد: بانک «انتقال به پس‌انداز» را یک خروجیِ معمولی چاپ می‌کند، پس به‌عنوان هزینه ثبت می‌شود و تا وقتی کسی نگوید، در هزینه‌ی ماه شمرده می‌شود — یعنی دقیقاً همان دوباره‌شماری‌ای که مفهوم «انتقال» برای جلوگیری از آن ساخته شد. قابلیتی که راهِ درست‌کردنِ خروجیِ خودش را ببندد، نصفه است.
- **مجاز شد ولی ساکت نیست.** موقع تغییر نوع، یک جمله می‌گوید سرِ حسابِ آن‌طرف چه می‌آید — با اسمِ خودِ حساب، چون آن حساب روی صفحه نیست. همان الگوی مرحله‌ی موجودی: کاری که نتیجه‌اش جای دیگری دیده می‌شود، باید پیش از انجام گفته شود.
- **گزینه‌ی «انتقال» فقط با دو حساب باز پیدا می‌شود — مگر ردیف از قبل انتقال باشد.** وگرنه انتقالی که یکی از حساب‌هایش بسته شده، در شیت ویرایشِ خودش نوعِ خودش را نداشت.
- **ردیفِ تبدیل‌شده هنوز به صورت‌حسابش وصل است**، پس آپلود دوباره‌ی همان صورت‌حساب باز هم می‌شناسدش: `ledgerForAccount` انتقال را برای حسابِ مبدأ به یک خروجی تبدیل می‌کند، که همان چیزی است که صورت‌حساب چاپ کرده.

## منشأ تراکنش روی ویرایش پاک می‌شد

- **`source` دیگر موقع ویرایش بازنویسی نمی‌شود.** `saveTransaction` روی update هم `source: "form"` می‌نوشت، یعنی تصحیحِ فروشنده‌ی یک فاکتور — یا تبدیلِ یک خروجیِ import‌شده به انتقال — ردیفی باقی می‌گذاشت که ادعا می‌کرد کاربر دستی تایپش کرده، در حالی که `statement_line_id`اش هنوز به همان صورت‌حسابی اشاره داشت که منکرش شده بود. منشأ یک‌بار موقع ساخته‌شدن ست می‌شود و ویرایش عوضش نمی‌کند.
- **این باگ از قبل روی main بود** و با باز کردنِ تبدیل نوع خودش را نشان داد. مستقیم روی دیتابیس راستی‌آزمایی شد (ویرایشِ یک ردیفِ `statement` از طریق UI، و بعد خواندنِ ستون `source` که سر جایش مانده بود). تستِ دائمی ندارد: رابط کاربری منشأ را برای ردیفِ تأییدشده نشان نمی‌دهد، پس چیزی برای assert کردن روی صفحه نیست، و اضافه‌کردنِ نشانه‌ای به markup فقط برای تست، محصول را به‌خاطر تست عوض‌کردن است.

## دنگ و دونگ

### مدل داده

- **سهمِ هر نفر از یک خرید، جدا از اینکه چه کسی پولش را داده ذخیره می‌شود.** کل فیچر روی همین یک تفکیک ایستاده: هتل را یک نفر با کارت خودش حساب می‌کند و پنج نفر مصرفش می‌کنند. اگر خرید فقط «مبلغ و پرداخت‌کننده» بود، هیچ‌وقت نمی‌شد گفت هرکس چقدر بدهکار است. *رد شده:* نگه‌داشتن فقط یک لیست مشارکت‌کننده و تقسیم در زمان خواندن.
- **سهم‌ها به‌صورت «مبلغ» ذخیره می‌شوند، نه «نسبت» — برخلاف موجودی حساب که در هر خواندن مشتق می‌شود.** تفاوت این است که موجودی یک جواب درست دارد و این ندارد: تقسیم ۱۰٬۰۰۰ بین سه نفر یک واحد باقی‌مانده دارد که باید سر کسی بیفتد، و اگر این تصمیم موقع خواندن گرفته می‌شد، نفری که باقی‌مانده را برمی‌دارد می‌توانست بین دو صفحه عوض شود. یک‌بار تصمیم‌گرفتن و ذخیره‌کردنش همان چیزی است که «سهم تو» را همه‌جا یک عدد نگه می‌دارد.
- **«صندوق دوره» یک عضو است، نه یک حالت خاص.** پول‌ریختن به صندوق یک پرداخت معمولی است و خرج‌کردن از آن یک خرید معمولی، پس هیچ شاخه‌ی جداگانه‌ای در ریاضی تراز لازم نمی‌شود — صندوقی که هرچه گرفته خرج کرده، دقیقاً روی صفر می‌نشیند. *رد شده:* ستون‌های جداگانه‌ی «موجودی صندوق» روی دوره.
- **هر دوره یک واحد پول دارد و بعد از اولین خرید قفل می‌شود.** این اپ هیچ‌جا تبدیل نرخ نمی‌کند (همان تصمیم حساب‌ها)، و دوره‌ای که خریدهایش در سه ارز باشد نه جمع‌شدنی است نه تسویه‌شدنی، مگر با نرخی که از خودمان دربیاوریم. واحد پول را خودِ دوره انتخاب می‌کند نه پروفایل، چون دلیل معمولِ ساختن یک دوره، سفر است. *رد شده:* ارز به ازای هر خرید (کاری که خودِ «دنگ و دونگ» می‌کند).
- **افراد یک دوره، «اسم»اند نه حساب کاربری.** پنج نفر دیگرِ سفر در این اپ حساب ندارند، و شرط‌کردنش یعنی فیچر فقط وقتی کار می‌کند که همه اپ را نصب کنند. *رد شده:* دوره‌ی مشترک بین چند کاربر با دعوت‌نامه — که یک مدل RLS کاملاً تازه می‌خواهد (جدول عضویت، policyهای مبتنی بر عضویت به‌جای `user_id`) و خارج از این برش است.

### قیدها

- **«جمع سهم‌ها باید برابر مبلغ خرید باشد» در دیتابیس تضمین می‌شود، نه فقط در action.** اگر این نامتوازن شود، هر ترازی که از آن مشتق می‌شود غلط است و *بی‌صدا* غلط است: لیست همچنان رندر می‌شود و عددها همچنان شبیه عددند. با constraint trigger و `deferrable initially deferred` چک می‌شود، چون خرید و سهم‌هایش چند دستورند و باید یک‌جا، سرِ commit، سنجیده شوند. تست‌شده روی دیتابیس زنده: سهم ناقص و خریدِ بدون سهم هر دو رد می‌شوند.
- **به همین دلیل، نوشتن خرید فقط از راه RPC `dong_save_expense` ممکن است.** دو round trip جدا یعنی چکِ موخر در پایان اولی شلیک می‌کند — و درست هم می‌گوید، چون بعد از round trip اول داده واقعاً ناسازگار است.
- **کلیدهای خارجیِ اشاره‌کننده به افراد `deferrable initially deferred` شدند.** اول `restrict` بود؛ حذف یک دوره روی cascade خودش گیر می‌کرد: افراد پیش از سهم‌ها پاک می‌شوند و سهم‌ها یک لایه پایین‌تر از خریدها آویزان‌اند، پس چکِ ارجاع وقتی شلیک می‌کند که آن لایه هنوز پاک نشده. `no action` هم کافی نبود — چک را فقط تا پایان همان دستور به تعویق می‌اندازد، نه تا پایان تراکنش. موکول‌کردن به commit هر دو رفتار را درست می‌کند: دوره کامل پاک می‌شود، و حذف یک نفر که در خریدی سهم دارد همچنان با ۲۳۵۰۳ رد می‌شود. **این را تست e2e گرفت، نه بازبینی کد.**

### رابط کاربری

- **صندوق می‌تواند پرداخت‌کننده باشد ولی هیچ‌وقت در تقسیم نمی‌آید.** صندوق چیزی مصرف نمی‌کند؛ آوردنش در فهرست تقسیم یعنی دعوت به ساختن خریدی که خودِ صندوق بابتش بدهکار است، و چنین چیزی معنا ندارد.
- **سهم هر نفر پیش از ذخیره روی صفحه دیده می‌شود، با همان توابعی که action استفاده می‌کند.** کاربر دقیقاً همان عددی را می‌بیند که ذخیره خواهد شد — از جمله اینکه باقی‌مانده‌ی گرد کردن سرِ چه کسی می‌افتد.
- **جدول تسویه حریصانه ساخته می‌شود و حداکثر n−۱ پرداخت می‌دهد.** شش نفر با پنج پرداخت تسویه می‌کنند، نه پانزده‌تا؛ کل دلیل وجود آن صفحه همین است. کمینه‌ی نظری نیست (آن مسئله NP-hard است) ولی پایدار است: تساوی‌ها با شناسه‌ی عضو شکسته می‌شوند، پس یک تراز همیشه یک جدول می‌دهد.
- **هر ردیف جدول تسویه دکمه‌ی «ثبت» دارد که فرم پرداخت را از قبل پر می‌کند.** پیشنهادی که باید دستی دوباره تایپ شود، نصفه است.
- **«دنگ و دونگ» در تب‌بار نیست، ولی روی داشبورد کارت دارد.** تب‌بار چهار قلم دارد و این تصمیم طراحی است؛ ولی نوار کناری که بقیه را حمل می‌کند از ۹۶۰ پیکسل شروع می‌شود، پس گذاشتنِ فیچر فقط آنجا یعنی روی گوشی زیر «تنظیمات» دفن شود. بخش بعدی شرح این اشتباه است.

### باقی‌مانده و خارج از دامنه

- **وصل‌شدن به دفتر شخصی.** خریدی که خودت پولش را داده‌ای، پولی است که واقعاً از حسابت رفته، و تسویه‌ای که می‌گیری پولی است که برمی‌گردد. فعلاً هیچ‌کدام تراکنش نمی‌سازند: این یک تصمیم جداست (کل مبلغ هزینه شمرده شود یا فقط سهم تو؟) و بدون جواب‌دادن به آن، وصل‌کردنشان یعنی هزینه‌ی ماه بی‌صدا اشتباه شود. مدل داده چیزی که این کار را ببندد ندارد و ستون بلااستفاده هم برایش اضافه نشد.
- **چت داخل گروه، QR، ضمیمه‌ی عکس رسید، محاسبه‌گر مالیات، و خروجی PDF/اکسل** — هر پنج تا در اپ اصلی هست و هیچ‌کدام اینجا نیست.

## برنامه‌ی پس‌انداز برای هدف‌ها

### چرا اصلاً

- **هدف بدون برنامه، فقط یک نوار پیشرفت بود.** صفحه‌ی خالی از اول قول داده بود «یک هدف بساز تا بگویم ماهی چقدر باید بگذاری کنار» و هیچ‌جای اپ این جمله را نمی‌گفت: نه مبلغ ماهانه، نه اینکه آن مبلغ از جیب این ماه درمی‌آید یا نه، نه اینکه اگر درنمی‌آید چه باید کرد. کاربر درست گفت که آن هدف عملاً هیچ کاری برایش نکرد.

### ریاضیات

- **ماهِ جاری جزو ماه‌های باقی‌مانده شمرده نمی‌شود.** پول این ماه تا وقتی کسی صفحه‌ی هدف را باز می‌کند معمولاً خرج شده؛ شمردنش یعنی مبلغ ماهانه روی ماهی پخش شود که واقعاً وجود ندارد و کاربر سرِ تاریخ کم بیاورد. خطا عمداً به سمت امن می‌رود: کمی بیشتر بخواه، کمی زودتر برس.
- **مبلغ ماهانه به بالا گرد می‌شود.** گردکردن به پایین یعنی به‌ازای هر ماه یک واحد کسری، و دقیقاً همان نرسیدنی که کل فیچر برای جلوگیری از آن ساخته شده.
- **«چقدر برایت می‌ماند» از ماه‌های واقعی خوانده می‌شود، نه از حرف کاربر — به‌شرط اینکه ماه واقعی‌ای وجود داشته باشد.** میانه‌ی حداکثر سه ماهِ *کامل*، و نه میانگین: یک ماه سفر با سه بلیت هواپیما میانگین را آن‌قدر می‌کشد که کل برنامه غلط شود، و دقیقاً همان ماهی است که یک‌بار اتفاق می‌افتد. ماهِ در جریان کنار گذاشته می‌شود (روز سوم ماه شبیه ثروت است و روز بیست‌وهشتم شبیه فاجعه)، و ماهی که هیچ ثبتی ندارد هم — ماهی که کاربر ثبت نکرده، ماهی نیست که درآمد نداشته. *رد شده:* میانگین شش ماه.
- **تخمینِ جایگزین، هزینه‌های متغیرِ onboarding را هم کم می‌کند، نه فقط هزینه‌های ثابت را.** صفحه‌ی خلاصه‌ی onboarding «درآمد منهای هزینه‌ی ثابت» را نشان می‌دهد، ولی برنامه‌ای که روی آن عدد ساخته شود نرخ پس‌اندازی وعده می‌دهد که خواربار پیش از پایان ماه می‌خوردش.
- **حتی یک ماهِ واقعی بر تخمین مقدم است.** تخمین، حدسِ ماهی است که هنوز کسی از آن رد نشده؛ یک ماه واقعی دست‌کم ماهی است که اتفاق افتاده. و هر جا عدد تخمینی باشد، با همان نشانِ «تخمین» که بقیه‌ی اپ برای حدس‌های مدل استفاده می‌کند علامت می‌خورد.

### تقسیم پول بین هدف‌ها

- **تاریخ و اولویت دو نوع ادعای متفاوت‌اند، پس در دو پاس جدا تقسیم می‌شوند.** هدفِ تاریخ‌دار هر ماه مبلغ مشخصی می‌خواهد وگرنه تاریخش را از دست می‌دهد، پس اول برمی‌دارد. هدفِ بی‌تاریخ نرخ مشخصی نمی‌خواهد، پس نمی‌تواند سر سهم رقابت کند و هرچه از پاس اول ماند به آن می‌رسد.
- **باقی‌مانده کامل به اولین هدفِ بی‌تاریخ می‌رسد، نصف‌نصف بین همه تقسیم نمی‌شود.** تقسیم مساوی منصفانه‌ترین حساب و بدترین توصیه است: هیچ‌چیز تمام نمی‌شود. «اول صندوق اضطراری را پر کن، بعد سراغ بعدی برو» تمام دلیلِ داشتنِ اولویت روی هدف است. هدفی که چیزی نمی‌گیرد، به‌جای یک تاریخِ ساختگی، *اسمِ* هدفی را می‌بیند که پول به آن می‌رسد.
- **هدفی که به آن نمی‌رسد، نامِ هدفِ بی‌تاریخِ جلوتر را می‌بیند، نه نامِ یک هدفِ تاریخ‌دار را.** هدف تاریخ‌دار فقط هزینه‌ی تاریخِ خودش را برداشته و به‌هرحال برمی‌داشت؛ آن چیزی نیست که جلوی این یکی را گرفته.
- **هدفی که کامل تأمین نمی‌شود، هرچه مانده را برمی‌دارد و *می‌گوید* کم دارد — از هدف بعدی قرض نمی‌گیرد.** دو هدف که هرکدام شبیه «در مسیر» به‌نظر برسند در حالی که هیچ‌کدام نیستند، بدترین خروجی ممکن است.
- **تاریخی که گذشته، با پوشش‌دادنِ این ماه «در مسیر» نمی‌شود.** همچنان «گذشته» است؛ پاک‌کردنش یعنی پاک‌کردنِ تنها چیزی که کاربر باید بداند.

### رابط کاربری

- **هر جمله یک عدد دارد و، جایی که خبر بد است، راهِ درست‌کردنش را هم می‌گوید.** «کم می‌آوری» به‌تنهایی شکایت است نه برنامه؛ پس سه راهِ مشخص گفته می‌شود (تاریخ را عقب ببر، مبلغ را کم کن، ماهی همین‌قدر از هزینه‌ها بزن) و برای هدفی که به تاریخش نمی‌رسد، تاریخی که *واقعاً* به آن می‌رسد.
- **عددی که برنامه رویش بنا شده، کنار خودِ برنامه نوشته می‌شود.** برنامه فقط وقتی قابل‌پیروی است که بتوانی فرضِ زیرش را ببینی و بگویی «این غلط است، اجاره‌ام بالا رفته».
- **ترتیب هدف‌ها با دو فلش روی هر کارت قابل تغییر شد.** تا پیش از این اولویت فقط در onboarding نوشته می‌شد و بعدش هیچ‌جا؛ حالا که ترتیب تعیین می‌کند پولِ اضافه‌ی ماه اول به کدام هدف می‌رسد، کاربری که نتواند عوضش کند نمی‌تواند بگوید پول خودش برای چیست. فلش‌ها کنار جمله‌ی برنامه می‌نشینند نه کنار عنوان — آن‌جا ارتفاعی اضافه نمی‌کنند و عنوان عرض کامل را نگه می‌دارد.
- **کارت داشبورد هم مبلغ ماهانه را نشان می‌دهد ولی «می‌رسی یا نه» را نه.** آن سؤال به «چقدر برایت می‌ماند» نیاز دارد و چهار کوئری تازه به داشبورد اضافه می‌کرد؛ مبلغ ماهانه‌ی هر هدف فقط از خودِ هدف و تاریخ امروز درمی‌آید. قضاوتِ کامل سرِ جای خودش، در صفحه‌ی هدف‌ها می‌ماند.

### خارج از دامنه

- **`saved_amount` هنوز دستی است.** هیچ تراکنشی به هدف وصل نمی‌شود، پس «این ماه گذاشتم کنار» را باید خود کاربر بنویسد. وصل‌کردنشان یک تصمیم جدا می‌خواهد (انتقال به پس‌انداز آیا هزینه است؟ کدام حساب پشتِ کدام هدف است؟) و بدون جواب‌دادن به آن، هم موجودی حساب و هم پیشرفت هدف بی‌صدا غلط می‌شوند.

### یک تعریف واحد از «چقدر برایت می‌ماند»

- **صفحه‌ی خلاصه‌ی onboarding حالا همان تابعی را صدا می‌زند که برنامه‌ی پس‌انداز رویش بنا شده، نه یک تفریق دوم که امروز اتفاقی با آن می‌خواند.** پیش از این، خلاصه «درآمد منهای هزینه‌ی ثابت» را نشان می‌داد و برنامه «درآمد منهای هزینه‌ی ثابت منهای تخمین هزینه‌های متغیر» را — یعنی کاربر سرِ ثبت‌نام یک عدد می‌دید و در صفحه‌ی هدف‌ها عددِ کمتری، بی‌آنکه چیزی بگوید کدام درست است. این بدهیِ خودِ برنامه‌ی پس‌انداز بود: جدولِ محاسبه یکی شده بود ولی تعریف نه.
- **ردیف «هزینه‌های متغیر — تخمین خودت» به خلاصه اضافه شد و برچسب آخر از «بعد از هزینه‌های ثابت برایت می‌ماند» به «آخر ماه برایت می‌ماند» تغییر کرد.** عددِ کوچک‌ترِ درست، بدون ردیفی که بگوید از کجا آمد، شبیه اشتباه به‌نظر می‌رسد؛ و برچسب قبلی دقیقاً همان چیزی را توصیف می‌کرد که دیگر حساب نمی‌شود.

## پس‌انداز واقعاً کنار گذاشته می‌شود

### قاعده

- **پس‌انداز هزینه نیست.** پولی که برای یک هدف کنار می‌گذاری هنوز پول توست؛ فقط از حسابی که از آن خرج می‌کنی رفته به حسابی که دست نمی‌زنی. آن روزی هزینه می‌شود که بابت همان هدف خرجش کنی، نه یک روز زودتر. این تصمیم از خود کاربر آمد و کل مدل داده روی آن بنا شد.
- **پس هدف با «انتقال» پر می‌شود و با «هزینه» خالی.** هر دو `goal_id` می‌گیرند. درآمد هیچ‌وقت نمی‌گیرد — پولی که می‌آید پولی نیست که کنار گذاشته شده — و دیتابیس با یک CHECK همین را تضمین می‌کند، نه فقط رابط کاربری.
- **اپ *می‌خواهد* که پول واقعاً جابه‌جا شود.** روی هر هدف یک دکمه‌ی «بریز کنار» هست که انتقال را از حساب روزمره به حساب پس‌انداز می‌سازد، با مبلغی که برنامه گفته. عددی که فقط در یک جعبه تایپ شود، پس‌انداز نیست؛ همان‌جا می‌ماند که از آن خرج می‌کنی و بی‌آنکه بفهمی خرج می‌شود. اگر حساب پس‌اندازی نباشد، اپ اول همان را می‌خواهد و لینکش را می‌دهد.

### مدل داده

- **پیشرفت هدف از دفتر مشتق می‌شود، نه از یک ستون — همان قاعده‌ی موجودی حساب.** تابع `goal_progress()` دقیقاً قرینه‌ی `account_balances()` است. دو جایی که هر دو یک جمع را حساب کنند، بالاخره با هم اختلاف پیدا می‌کنند و آن که کاربر اتفاقاً نگاهش می‌کند، غلط است.
- **`saved_amount` به `opening_saved` تغییر نام داد، نه اینکه سرِ جایش معنی‌اش عوض شود.** حالا همان چیزی است که `accounts.opening_balance` هست: آنچه پیش از آشنایی اپ کنار گذاشته شده بود. تغییر نام عمدی بود تا هر خواندنِ قدیمی *کامپایل نشود* و مجبور شویم نگاهش کنیم؛ ستونی که بی‌صدا معنی‌اش عوض شود، همان راهی است که نوار پیشرفت عددی نشان می‌دهد که هیچ‌کس نمی‌تواند توضیحش دهد. کامپایلر پنج‌تا خواندن پیدا کرد.
- **`opening_saved` تاریخ ندارد، برخلاف `opening_balance_on` روی حساب.** حساب به تاریخ نیاز دارد چون تراکنش‌هایش می‌توانند از خودش قدیمی‌تر باشند؛ هدف نه — یک ردیف فقط وقتی برچسب هدف می‌خورد که کسی عمداً برچسبش بزند، و آن یعنی خواسته که شمرده شود.
- **حذف هدف، پول را حذف نمی‌کند (`on delete set null`).** انتقال واقعاً اتفاق افتاده و موجودی حساب به آن وابسته است؛ فقط برچسبش می‌رود.
- **موجودی هدف می‌تواند منفی شود و می‌گوید.** خرج‌کردنِ بیشتر از آنچه کنار گذاشته شده اتفاقی است که واقعاً می‌افتد؛ صفرکردنش در SQL فقط سردرگمی را جایی می‌برد که زمینه‌ی کمتری برای توضیحش هست. نوار پیشرفت در UI کف صفر دارد (تست دارد)، ولی عدد راستش را می‌گوید.

### رابط کاربری

- **کارت هدف می‌گوید چقدر از آن خرج شده.** بدون این، نوار پیشرفتی که پایین می‌آید شبیه باگ است، در حالی که دقیقاً یعنی هدف کارش را کرده.
- **«این ماه ریختی کنار» جای دکمه را می‌گیرد.** یادآوری‌ای که بعد از انجام‌شدن هم همان را بخواهد، یاد نمی‌آورد؛ اذیت می‌کند.
- **در شیت ویرایش تراکنش یک «بابت کدام هدف؟» هست.** بانک «انتقال به پس‌انداز» را یک خروجی معمولی چاپ می‌کند و مسیر import همان را می‌سازد؛ بدون راهی برای برچسب‌زدنِ بعدی، هر پس‌اندازی که از صورت‌حساب آمده برای همیشه بی‌هدف می‌ماند. همان الگوی «تبدیل نوع تراکنش».
- **«از قبل کنار گذاشته بودی» جای «تا حالا کنار گذاشته‌ای» را گرفت**، با یک جمله زیرش که می‌گوید از این به بعد خودش شمرده می‌شود. برچسب قبلی حالا یک دروغ بود.

## بستنِ هدف، پاک‌کردنش نیست

- **`status` بالاخره از رابط کاربری قابل تغییر شد** («در جریان / فعلاً نه / تمام شد»). ستون از روز اول چهار مقدار داشت و هیچ‌جای اپ چیزی جز `active` نمی‌نوشت، پس هدفی که به آن رسیده بودی تا ابد در فهرست می‌ماند.
- **حذف راهِ درستِ تمام‌کردن نیست.** هم رکوردِ رسیدن را می‌برد، هم — حالا که انتقال‌ها `goal_id` دارند — بی‌صدا برچسبِ هر پرداختی که هدف را پر کرده بود پاک می‌کند. بستن هیچ‌کدام را دست نمی‌زند و فقط جلوی سهم‌برداشتن از ماه بعد را می‌گیرد.
- **هدفی که دستی بسته شده «تمام» است، حتی اگر مبلغش کامل نشده باشد.** کاربر مرجعِ تمام‌شدنِ هدفِ خودش است، نه عددی که چند ماه پیش تایپ کرده.
- **دکمه‌ی «تمامش کن» روی خودِ کارت می‌آید، نه ته شیت ویرایش** — وقتی هدف رسیده ولی هنوز باز است. تنها کاری که آن لحظه لازم است، نباید جست‌وجو بخواهد.
- **ترتیب هدف‌های بسته عوض نمی‌شود.** فرستادنشان به ته فهرست وسوسه‌انگیز بود، ولی `reorderGoal` شماره‌ها را بر اساس همان ترتیبِ کوئری بازنویسی می‌کند؛ دو ترتیب متفاوت یعنی فلش‌ها چیز اشتباهی را جابه‌جا کنند.

## قبض غیرماهانه

- **`frequency` روی `recurring_expenses` از روز اول سه مقدار داشت و فرم همیشه `monthly` می‌نوشت.** یعنی حق بیمه‌ی سالانه دوازده برابر در «هزینه‌ی هر ماه» شمرده می‌شد. حالا فیلدش هست و `lib/cashflow.ts` معادل ماهانه‌اش را درست حساب می‌کند.
- **قبض غیرماهانه خودکار ثبت نمی‌شود، و این را می‌گوید.** `post_recurring_for_month` ماهی را که به آن داده می‌شود ثبت می‌کند و جدول ستونی ندارد که بگوید یک قبض سه‌ماهه *کدام* ماه می‌افتد. `auto_post` برای این ردیف‌ها به اجبار false می‌شود و به‌جای چک‌باکس، یک جمله می‌گوید چرا. تیکی که بماند و کاری نکند، کاربر را به ردیفی معتقد می‌کند که هیچ‌وقت ساخته نمی‌شود. *رد شده:* اضافه‌کردن ستون ماهِ لنگر — یک برش جداست.
- **ردیف فهرست دیگر «ماهانه» را hardcode نمی‌کند.**
- **`due_month` اضافه شد و قبض غیرماهانه حالا خودکار ثبت می‌شود.** جدول می‌گفت هر قبض چه *روزی* می‌افتد ولی نه چه *ماهی*، و بدون آن `post_recurring_for_month` فقط می‌توانست ردیف ماهانه بسازد. حالا سالانه یعنی همان ماه، و سه‌ماهه یعنی همان ماه و هر سه ماه بعدش. حضورِ ستون دقیقاً با frequency گره خورده است (`(frequency = 'monthly') = (due_month is null)`) تا ردیفی وجود نداشته باشد که تولیدکننده بی‌صدا از رویش رد شود. روی دیتابیس راستی‌آزمایی شد، از جمله حالت سختش: سه‌ماهه‌ی لنگرانداخته در دسامبر ماه‌های ۳، ۶، ۹، ۱۲ را می‌دهد — گذر از مرز سال، همان‌جا که modulo ساده می‌شکند.

## هدف در لحظه‌ی خرج‌شدن نام برده می‌شود، و روی ردیف دیده می‌شود

- **«بابت کدام هدف؟» به فرمِ ثبت هم اضافه شد، نه فقط شیت ویرایش.** روزی که چیزی را می‌خری، همان روزی است که می‌دانی بابت چه بوده؛ وادارکردنِ کاربر به اینکه اول ثبت کند و بعد برگردد و برچسب بزند، همان راهی است که این پیوند هیچ‌وقت ساخته نمی‌شود.
- **ردیف تراکنش حالا هدفش را می‌گوید، و اول از همه.** انتقالی که هدفی را پر کرده بود از یک انتقال معمولی قابل تشخیص نبود و خریدی که از پس‌انداز هدف رفته بود از هر خرید دیگری — رابطه فقط از سمت هدف دیده می‌شد. هدف صدرِ خط توضیحات می‌نشیند چون تنها چیزی است که از هیچ‌جای دیگر آن خط قابل استنتاج نیست.
- **انتخابگر، هدف‌های باز را نشان می‌دهد به‌اضافه‌ی همان هدفی که ردیف از قبل نامش را دارد.** بدون نیمه‌ی دوم، بازکردنِ ردیفی که به هدفی بسته‌شده وصل است و زدنِ «ذخیره»، بی‌صدا برچسب را می‌انداخت: انتخابگر گزینه‌ای متناظرش نداشت و به «هیچ‌کدام» برمی‌گشت.
- **کارت تأیید (مسیرهای متن و عکس) هم همین سؤال را می‌پرسد.** اول فقط فرم دستی و شیت ویرایش را درست کرده بودم، یعنی عکس‌گرفتن از فاکتورِ همان چیزی که برایش پس‌انداز می‌کردی هنوز دو قدم داشت — دقیقاً همان درزی که قرار بود بسته شود. هدف مثل حساب، یک انتخاب در سطح کارت است نه به ازای هر ردیف: یک فاکتور یا یک جمله‌ی دیکته‌شده یک عمل خریدن است، هر چند خط که مدل در آن پیدا کند.
- **ردیف درآمد در همان کارت هدف نمی‌گیرد، حتی وقتی کارت هدف دارد.** پولی که می‌آید پولی نیست که کنار گذاشته شده، و دیتابیس هم ردش می‌کند؛ پس در action فیلتر می‌شود نه اینکه به شانس سپرده شود.

## موجودی پس‌انداز با مجموع هدف‌ها سنجیده می‌شود

- **بقیه‌ی صفحه‌ی هدف‌ها درباره‌ی جریان است، این یکی درباره‌ی موجودی.** دو عددی که باید بخوانند و هیچ‌وقت با هم مقایسه نمی‌شوند، همان راهی است که یا پولی بی‌هدف در پس‌انداز می‌ماند یا هدف‌ها بی‌صدا بیشتر از آنچه در حساب هست ادعا می‌کنند.
- **سکوت حالتِ خوب است.** وقتی دو عدد می‌خوانند چیزی رندر نمی‌شود.
- **کاربری که حساب پس‌انداز ندارد ولی هدف‌هایش می‌گویند پولی کنار گذاشته، همان را می‌شنود** — چون کل قاعده‌ی این فیچر این است که آن پول باید جای جدایی باشد.

## بازنویسیِ تاریخچه‌ی git برای پاک‌کردنِ هویتِ شخصی

- **بعد از عمومی‌شدنِ مخزن، سه جا پیدا شد که هویتِ واقعیِ کاربر را نشان می‌دادند: دو کامنتِ کد (`lib/auth.ts`، یک migration) و ایمیلِ نویسنده‌ی هر کامیت.** دوتای اول فقط پیشوندِ ایمیل بودند و در همان کامیتِ اصلاحی رفع شدند؛ سومی — `fxpabarja.ai@gmail.com` به‌عنوان author هر ۳۶ کامیت — بزرگ‌تر بود، چون یک ایمیلِ کامل و کارآمد است، نه یک تکه‌کلمه، و اصلاحش فقط در کامیتِ تازه کافی نیست: metadata گیت برای همیشه در کامیت‌های قدیمی می‌ماند.
- **قبل از هر تغییری، یک `git bundle --all` کامل گرفته شد و verify شد** (`git bundle verify`)، جدا از مخزن اصلی. عملیات هرگز روی خودِ working directory اجرا نشد — یک clone تازه و یکبارمصرف در مسیر جدا ساخته شد، و کارِ اصلی رویش انجام شد.
- **`git-filter-repo` با دو قاعده در یک pass اجرا شد:** `--replace-text` برای رشته‌ی متنی، `--mailmap` برای عوض‌کردنِ ایمیلِ author/committer به فرمتِ استانداردِ noreply گیت‌هاب (`{id}+{username}@users.noreply.github.com`) — که کامیت‌ها را همچنان درست به پروفایل وصل نگه می‌دارد.
- **قبل از push، هر ادعا جداگانه و مستقل تأیید شد:** شمارِ کامیت‌ها (۳۶، بدون تغییر)، عنوانِ هر کامیت (byte-identical با قبل)، diff سطحِ blob بینِ درختِ قدیم و جدید (دقیقاً یک فایل، یک خط — همان چیزی که انتظار می‌رفت)، و یک اسکنِ کاملِ هر blobِ یکتا در کل مخزن (نه هر کامیت جداگانه، برای سرعت) برای رشته‌ی حساس — صفر مورد. بعد از push، همین‌ها دوباره مستقیم از API خودِ گیت‌هاب چک شدند، نه فقط از حالتِ محلی.
- **یک یافته‌ی جدید همین‌جا پیدا شد:** رشته‌ی حساس در سومین فایل هم بود (`supabase/migrations/0004_profile_name_from_provider.sql`) که در ممیزیِ اولیه از قلم افتاده بود — چون آن ممیزی فقط دو فایلِ مشخص را چک کرده بود، نه کلِ مخزن را. این با mismatch شدنِ hash درختِ تاریخچه‌ی بازنویسی‌شده در برابر hash درختِ ثبت‌شده‌ی قبل از عملیات کشف شد، نه با گمان. همین rewrite آن را هم اصلاح کرد، هم در تاریخچه هم در نوکِ فعلیِ `main`.

## دو تست e2e که پاس می‌شدند و کار نمی‌کردند

- **`toHaveCount(0)` وقتی شیت هنوز باز است هیچ‌چیز را ثابت نمی‌کند.** شیت یک modal است، پس Radix بقیه‌ی صفحه را `aria-hidden` می‌کند، `getByRole` اصلاً هیچ ردیفی نمی‌بیند، و شمارش صفر است چه چیزی حذف شده باشد چه نشده. `goals.spec.ts` و `records.spec.ts` هر دو همین را assert می‌کردند: **سبز می‌شدند و ردیفشان را روی حساب جا می‌گذاشتند.** حالا اول بسته‌شدن dialog انتظار کشیده می‌شود، بعد فهرست نگاه می‌شود.
- **پاک‌سازیِ ابتدای تست `if` بود نه `while`،** پس هر اجرا فقط یک ردیفِ جامانده را می‌برد. کنار باگ بالا، جامانده‌ها سریع‌تر از جاروشدن انباشته می‌شدند: چهار «منبع تست E2E» روی حساب پیدا شد.
- **این داده‌ی جامانده عددهای واقعی کاربر را خراب می‌کرد،** نه اینکه فقط کثیف باشد: یک منبع درآمدِ تستیِ ۱۲۳ دلاری در محاسبه‌ی «آخر ماه چقدر برایت می‌ماند» شمرده می‌شد و بعد از پاک‌شدنش مازاد از ۳٬۰۸۸ به ۲٬۹۶۵ آمد.
- **`toHaveCount(before - 1)` به‌جای `toHaveCount(0)`،** تا حذف در حلقه هم قابل assert باشد.

## دو معیار پذیرشی که تست نداشتند

### انزوای کاربران (تست پذیرش ۶)

- **`PLAN.md` گفته بود پایان هر milestone تکرار شود و هیچ‌وقت خودکار نشد.** RLS تنها چیزی است که بین پول دو نفر ایستاده، و برخلاف هر قاعده‌ی دیگری در این پروژه، **بی‌صدا و کامل** شکست می‌خورد. حالا تست دارد.
- **نیمه‌ی API تستِ واقعی است، نه نیمه‌ی UI.** با همان anon key که مرورگر استفاده می‌کند وارد هر دو حساب می‌شود و ردیف‌به‌ردیف چیزهایی را می‌خواهد که مالِ دیگری است — دقیقاً کاری که مهاجمی با یک حساب معتبر می‌کند. نیمه‌ی UI ادعای ضعیف‌تری است (حساب دوم onboarding را تمام نکرده و بیشتر مسیرها ریدایرکت می‌شوند) و فقط چون خودِ معیار هر دو را نام می‌برد نگه داشته شد.
- **probeِ خواندن به ردیف‌های *واقعی* اشاره می‌کند.** پرسیدن از beta درباره‌ی یک id که وجود ندارد، روی دیتابیسی که هیچ policy ندارد هم پاس می‌شود.
- **probeِ نوشتن، مقدار را روی خودش می‌نویسد.** تستی که «نمی‌توانی داده‌ی من را عوض کنی» را می‌سنجد و موقع شکست داده را عوض می‌کند، از نبودِ تست بدتر است: همان اجرایی که باگ را می‌گیرد، همان اجرایی است که خرابی را می‌سازد. حذف به همین دلیل probe نمی‌شود و اصلاً نمی‌شود امنش کرد.
- **راستی‌آزمایی شد که *می‌تواند* شکست بخورد:** با ست‌کردن هر دو حساب روی یک کاربر، هر چهار تست شکست خوردند و نام جدول نشتی را گفتند. تستی که هیچ‌وقت قرمز نشده باشد، چیزی ثابت نمی‌کند — درس همان دو تستی که سبز می‌شدند و کار نمی‌کردند.

### idempotency تراکنش‌های تکرارشونده (تست پذیرش ۷)

- **روی ماهی در گذشته‌ی دور اجرا می‌شود، نه ماه جاری.** فراخوانی برای ماه جاری صفر برمی‌گرداند چون ردیف‌ها از قبل هستند — پاس می‌شود بی‌آنکه چیزی ثابت کند. برای اینکه صفرِ فراخوانی دوم معنا داشته باشد، فراخوانی اول باید واقعاً چیزی بسازد؛ پس `expect(first > 0)` هم assert می‌شود.
- **خودش تمیز می‌کند، حتی وقتی assertion پرت شود** (`try/finally`)، و در پایان چک می‌کند چیزی جا نمانده باشد.
- **این بدهیِ تغییر خودم بود:** `post_recurring_for_month` را برای زمان‌بندی قبض‌های سه‌ماهه و سالانه بازنویسی کردم — تابعی که idempotency‌اش یک معیار پذیرش است — بدون اینکه چیزی آن معیار را بگیرد.

## محدودیتی که قرار بود در UI گفته شود (R9)

- **`PLAN.md` نوشته بود «این محدودیت در UI گفته می‌شود» و فقط یک کامنت در کد بود** — جایی که کاربری که تاریخچه‌اش یک ماه کم دارد نمی‌تواند بخواندش. ماهی که اپ را باز نکرده باشی، اجاره‌اش هیچ‌وقت ساخته نمی‌شود.
- **فقط روی ماهِ گذشته‌ای که واقعاً هیچ ردیف تکرارشونده‌ای ندارد نشان داده می‌شود**، پس یک واقعیت درباره‌ی همان ماه است نه یک سلب‌مسئولیتِ همیشگی که کسی نمی‌خواندش.
- **شمارشش در همان پاسِ `monthTotals` انجام می‌شود، نه با یک کوئری جدا.** کوئری دومِ «اصلاً قبض ثابتی داری؟» فقط وقتی اجرا می‌شود که ماهِ گذشته صفر باشد، پس حالت معمول هزینه‌ای نمی‌دهد.
- **بعداً با فیچرِ «ماه‌های جامانده» جایگزین شد:** گفتنِ محدودیت قدم اول بود، ولی نوارِ فقط-خبری کاری از دست کاربر برنمی‌آورد.

## ماه‌های جامانده: اپ می‌پرسد، بعد تصمیم می‌گیرد

### چرا سؤال، نه حدس

- **اپ نمی‌تواند این را خودش جواب دهد و نباید وانمود کند که می‌تواند.** اینکه اجاره‌ی ژوئن واقعاً پرداخت شد یا نه فقط در سرِ کسی است که پرداختش کرده. حدسِ «بله» ماه را فقیرتر از واقعیت می‌نویسد و حدسِ «نه» ثروتمندتر — و هر دو عددی می‌سازند که هیچ‌وقت کسی تصحیحشان نمی‌کند.
- **«نه» هم اطلاعات است، نه سکوت.** قبضی که دیگر پرداخت نمی‌شود باید از هزینه‌ی ثابتِ هر ماه برداشته شود، وگرنه هر ماه — نه فقط ماه‌های جامانده — غلط حساب می‌شود. برای همین بعد از «پرداخت نشد» سؤال دوم پرسیده می‌شود، و «دیگر نمی‌دهمش» قبض را غیرفعال می‌کند: از هزینه‌های ثابت می‌رود، مازاد بالا می‌رود، و برنامه‌ی هر هدف دوباره حساب می‌شود.
- **یک سؤال در هر صفحه، از قدیمی‌ترین ماه.** اینها سؤال‌های حافظه‌اند: «اجاره‌ی ژوئن پرداخت شد؟» جواب دارد، جدولی با یازده چک‌باکس ندارد. ترتیب هم مهم است: قبضی که از جایی به بعد قطع شده، سرِ همان ماه به آن می‌رسی — همان‌جا که سؤال دوم معنا پیدا می‌کند.

### مدل داده

- **«نه» در جدول `recurring_skipped_months` ثبت می‌شود.** تراکنش، «بله» را ثبت می‌کرد؛ هیچ‌چیز «نه» را ثبت نمی‌کرد، و بدون آن سؤال تا ابد برمی‌گشت.
- **قاعده‌ی «این قبض در این ماه سررسید دارد؟» از `post_recurring_for_month` بیرون کشیده شد و شد `recurring_due_in_month`.** دو کپی از این قاعده یعنی یا اپ درباره‌ی ماهی می‌پرسد که هیچ‌وقت نمی‌ساختش، یا ماهی می‌سازد که هیچ‌وقت درباره‌اش نپرسیده.
- **تأیید یک ماه از همان تولیدکننده‌ی اولِ ماه عبور می‌کند** (با پارامتر `p_only`)، نه از یک insertِ دستی در action. ماهی که کاربر تأیید می‌کند باید مو‌به‌مو همان چیزی باشد که خودکار ساخته می‌شد: همان قاعده‌ی تاریخ، همان حساب، همان ایندکس یکتا پشتش.
- **«دیگر نمی‌دهمش» ردیفِ skip نمی‌سازد.** یابنده فقط قبض‌های فعال را می‌پرسد، پس غیرفعال‌کردن به‌تنهایی همه‌ی ماه‌های باقی‌مانده را از سؤال بیرون می‌برد. قبض حذف نمی‌شود چون ماه‌هایی که *واقعاً* پرداخت شده تراکنش دارند و به آن اشاره می‌کنند.
- **پنجره یک سال است و هیچ‌وقت ماه جاری.** آن‌سوی یک سال، آدم دیگر به‌یاد نمی‌آورد؛ حدس می‌زند — و یک «بله»ی حدسی عددی در دفتر می‌نویسد که هیچ‌چیز تصحیحش نمی‌کند.
- **کفِ «از زمان ساخت پروفایل» حذف شد.** قبض نمی‌تواند پیش از کسی که ساختتش وجود داشته باشد، پس «ماه بعد از ساخت قبض» همیشه از آن جلوتر است. کفِ دوم فقط روی داده‌ای که نمی‌تواند رخ دهد اثر می‌کرد — و وقتی اثر می‌کرد، **بی‌صدا** جواب می‌داد «چیزی برای بررسی نیست»، که برای سؤالی که کاربر منتظرش است بدترین شکست ممکن است. سرِ همین تست زنده گرفته شد.

### باگی که همین کار رویش را کم کرد

- **ماهی که تنها ردیفش یک قبضِ تولیدشده باشد، ماهِ زندگی‌شده نیست.** تأییدِ یک اجاره‌ی جامانده دقیقاً همین شکل را می‌سازد: هزینه‌ی بزرگ، درآمد صفر، و هیچ ردیفی که کاربر خودش ثبت کرده باشد. `observedSurplus` آن را ماهِ واقعی خواند و کلِ برنامه را منفی کرد — «بر پایه‌ی −۸۰۰ که ماه گذشته برایت ماند». همان قاعده‌ای که خودم نوشته بودم («ماهی که کاربر ثبت نکرده، ماهی نیست که درآمد نداشته») را نقض می‌کرد.
- **حالا ملاک «حداقل یک ردیف که خودِ کاربر ثبت کرده» است**، نه «صفر نبودن جمع». ردیف‌های تولیدشده حساب نمی‌شوند.
- **این باگ از قبل هم بود**، فقط دیده نمی‌شد: کاربری که هر ماه اپ را باز می‌کند ولی چیزی ثبت نمی‌کند، ماه‌هایش فقط اجاره دارند و مازادش منفیِ اجاره خوانده می‌شد.

## حذفِ قبض ثابتی که سابقه داشت، بی‌صدا شکست می‌خورد

- **`on delete set null` یکی از جفت را خالی می‌کرد و checkِ دوطرفه را می‌شکست.** `check ((recurring_expense_id is null) = (posted_month is null))` یعنی حذفِ قبض، تراکنش‌های تولیدشده‌اش را نامعتبر می‌کرد، Postgres حذف را رد می‌کرد، و شیت طوری بسته می‌شد که انگار انجام شده — ردیف موقع بارگذاری بعدی سرِ جایش بود.
- **قید از چیزی که محافظت می‌کرد قوی‌تر بود.** آنچه باید برقرار بماند این است که ردیفِ تولیدشده همیشه بگوید برای کدام ماه ساخته شده (ایندکس یکتای idempotency روی همان جفت است). جهت دیگر — ماهی بدون قبض — دقیقاً همان حالتی است که حذفِ قبض به‌جا می‌گذارد، و حالتِ درستی است: آن هزینه واقعاً برای آن ماه ساخته شد، و آن قبض واقعاً رفته.
- **دکمه‌ی حذف نتیجه را دور می‌ریخت.** حالا خطا را نشان می‌دهد. هر دو نیمه لازم بود: بدون قید درست، حذف کار نمی‌کرد؛ بدون نمایش خطا، هر شکستِ دیگری هم بی‌صدا می‌ماند.

## ورود مهمان

- **مهمان یک کاربرِ واقعیِ ناشناس در Supabase است، نه یک حالتِ سمتِ کلاینت.** `signInAnonymously()` ردیفی در `auth.users` با `is_anonymous = true` می‌سازد که نقشش `authenticated` است، پس هر policyـی که از M0 نوشته شده بود بدون یک خط تغییر شاملش می‌شود و داده‌اش دقیقاً مثل بقیه ایزوله است. *رد شده:* نگه‌داشتن داده در `sessionStorage` و دور زدن دیتابیس — یعنی نوشتنِ دوباره‌ی هر کوئری و هر server action در نسخه‌ی حافظه‌ای، و اپی که مهمان می‌بیند دیگر همان اپ نیست.
- **ساختِ حساب یک `updateUser` روی همان کاربر است، نه یک ثبت‌نامِ تازه.** شناسه‌ی کاربر عوض نمی‌شود، پس هر تراکنش و حساب و هدفی که مهمان وارد کرده سرِ جایش می‌ماند. این تنها دلیلی است که می‌شود از کسی خواست بعداً ثبت‌نام کند؛ اگر داده از دست می‌رفت، «حساب بساز» یعنی «از نو شروع کن» و کسی نمی‌کند. *رد شده:* `signUp` و کپی‌کردنِ ردیف‌ها از کاربر مهمان به کاربر جدید.
- **ایمیل و رمز در یک فراخوانی، نه دو تا — و این سلیقه نیست.** اول نوشتم «رمز اول، ایمیل دوم» و دلیل هم آوردم؛ تست نشان داد غلط است. GoTrue پیام `Updating password of an anonymous user without an email or phone is not allowed` می‌دهد، و ایمیلِ معلق (`new_email`) هم شرط را برآورده نمی‌کند — پس دو فراخوانی، به هر ترتیبی، شکست می‌خورد. هر دو حالت را روی کاربرِ واقعی امتحان کردم؛ فقط فراخوانیِ ترکیبی پذیرفته می‌شود. *درس:* ترتیبی که منطقی به نظر می‌رسید در عمل مجاز نبود؛ تست `a guest's email and password have to be set in one call` هست که اگر کسی دوباره جدایشان کرد، بفهمد.
- **ایمیلِ کاربرِ ناشناس `""` است، نه `null`.** `lib/auth.ts` آن را با `user.email || null` نرمال می‌کند؛ رشته‌ی خالی آدرس نیست و هر کدی که بپرسد «ایمیل دارد؟» از آن جوابِ غلط می‌گیرد.
- **تستِ «داده می‌ماند» از مسیر admin می‌رود و نه از نشستِ کاربر.** مسیرِ کاربر ایمیلِ تأیید می‌فرستد و SMTP پیش‌فرضِ Supabase ساعتی چند تا بیشتر نمی‌دهد؛ تستی که سهمیه‌ی ایمیل می‌سوزاند دو بار در ساعت هم نمی‌تواند اجرا شود. ادعای اصلی درباره‌ی شناسه‌ی کاربر و کلیدِ خارجی است و به ایمیل نیازی ندارد.
- **خروج از حساب مهمان یعنی حذفِ کاربر، و همه‌ی مسیرهای خروج از همان `logout()` رد می‌شوند.** وعده‌ای که در صفحه‌ی ورود داده شده باید در تنظیمات و در دکمه‌ی خروجِ ثبت‌نام هم برقرار باشد، و اگر هر کدام جدا تصمیم می‌گرفتند یکی‌شان فراموش می‌کرد. Storage اول پاک می‌شود چون کلید خارجی به `auth.users` ندارد و با حذفِ کاربر یتیم می‌ماند.
- **پاکسازیِ مهمانِ رهاشده شبانه است، ولی در اپ نه در Postgres.** مهمانی که تب را بست هیچ‌وقت خروج نمی‌زند؛ بدون این کار وعده‌ی «موقتی است» فقط برای کسانی درست بود که دکمه را می‌زدند. ملاک `last_sign_in_at` است نه `created_at`، چون مهمانی که یک هفته هر روز برمی‌گردد هنوز دارد استفاده می‌کند.
- **نسخه‌ی اول هرگز کار نمی‌کرد و بی‌صدا هر شب شکست می‌خورد.** تابع SQL از `storage.objects` حذف می‌کرد و Supabase با تریگر `protect_delete` جلویش را می‌گیرد. آزمایشِ اول ۰ برگرداند چون وقتی چیزی برای حذف نیست زودتر برمی‌گردد و هرگز به آن خط نمی‌رسید — اولین مهمانِ واقعی اولین شکست می‌شد. *درس:* تابعی که مسیرِ اصلی‌اش اجرا نشده، تست نشده.
- **تریگر حق داشت و دورزدنش غلط بود.** `storage.allow_delete_query` راهِ فرار دارد، ولی حذفِ ردیف فایلِ پشتِ آن را حذف نمی‌کند — یعنی بایت‌ها به هر حال از حساب بیشتر عمر می‌کردند. پس کار روی همان خطی تقسیم شد که خودِ پلتفرم می‌کشد: Postgres می‌گوید کی، اپ با Storage API حذف می‌کند — همان مسیری که خروجِ خودِ مهمان هم می‌رود. *رد شده:* `set local storage.allow_delete_query = 'true'`.
- **مسیرِ cron در `PUBLIC_PATHS` است، و با `CRON_SECRET` از خودش محافظت می‌کند.** زمان‌بندِ Vercel مرورگر نیست و کوکی ندارد؛ بدون این، middleware به `/login` می‌فرستادش و جاروبِ شبانه بی‌صدا اجرا نمی‌شد. اگر `CRON_SECRET` تنظیم نشده باشد، مسیر کاری نمی‌کند — نه اینکه باز بماند.
- **پیام یک‌باره و نوارِ همیشگی، هر دو.** سؤال این بود که کدام‌یک؛ جواب این است که هیچ‌کدام به‌تنهایی کافی نیست. پیامِ ورودی شرط را قبل از تایپِ چیزی می‌گوید، ولی مدال را می‌بندند تا به اپ برسند و تا تراکنشِ سوم فراموشش کرده‌اند. نوار همان جمله را روی هر صفحه نگه می‌دارد و رنگش همان کهرباییِ «حدس زدم» است، چون معنا یکی است: این موقتی است. سومین لحظه، شیتِ خروج است — پرمخاطره‌ترین نقطه و آخرین جایی که پیشنهادِ ساختِ حساب به درد می‌خورد.
- **سقف پردازش هوشمند برای مهمان ۵ در روز است، نه ۵۰.** سقفِ ۵۰ یک کاربر را محدود می‌کند و ساختِ کاربر مهمان یک کلیک بدون ایمیل است، پس برای هرکس که حاضر باشد خارج و دوباره وارد شود اصلاً سقفی نبود — و هر تماسِ اضافه پولِ واقعی نزد ارائه‌دهنده است. این سوراخ را تنگ می‌کند نه می‌بندد؛ بستنش captcha روی ورود ناشناس است که تنظیمِ پروژه‌ی Supabase است نه کد.
- **«ورود مهمان» نیاز دارد Anonymous Sign-Ins در داشبورد Supabase روشن باشد.** تا وقتی خاموش است دکمه هست و کار نمی‌کند، پس خطای `anonymous_provider_disabled` ترجمه‌ی فارسیِ خودش را دارد و کاربر را به دری که باز است می‌فرستد.

## دو بخش جدا: «حسابداری شخصی» و «دنگ و دونگ»

### چرا جداشان کردیم

- **این دو، دو سؤال متفاوت‌اند.** یکی می‌پرسد «پول من کجا رفت»، دیگری «آخر سفر چه کسی به چه کسی چقدر بدهکار است». روزهایی که آدم سراغشان می‌رود هم یکی نیست. تا وقتی در یک پوسته بودند، هر دو ضرر می‌کردند: داشبوردِ ماهِ شخصی یک کارت سفر گوشه‌اش داشت، صفحه‌ی تنظیمات «دنگ و دونگ» را زیر عنوان «پول» فهرست می‌کرد، و نوار پایینِ موبایل که چهار جا بیشتر ندارد، شش مقصد داشت.
- **صفحه‌ی اول بعد از ورود حالا فقط می‌پرسد کدام بخش.** دو جعبه، و روی هرکدام همان یک عددی که آن بخش درباره‌اش است: مانده‌ی این ماه، و اینکه در پررنگ‌ترین دوره‌ی باز طلبکاری یا بدهکار. پس انتخاب بر اساس چیزی که واقعاً در جریان است انجام می‌شود، نه بر اساس دو برچسب.
- **هیچ عددی از این دو با هم جمع نمی‌شود.** دوره‌ای به یورو و حسابی به تومان جمع‌شدنی نیستند، و «کل دارایی» که بدهی شش نفر به هم را هم تویش حساب کرده باشد، گمراه‌کننده‌ترین عدد کل محصول می‌شد.

### اینکه «کجا هستم» از آدرس خوانده می‌شود، نه ذخیره

- **یک لینک، یک refresh و دکمه‌ی back باید هر سه یک جواب بدهند.** بخشِ به‌خاطرسپرده تنها چیزی است که نمی‌تواند: کاربری که لینک یک دوره را باز می‌کند، در «دنگ و دونگ» است حتی اگر دفعه‌ی قبل حسابداری شخصی را انتخاب کرده باشد. `lib/workspaces.ts` تنها جایی است که این جواب را می‌دهد.
- **مسیرها فهرست شده‌اند، نه حدس زده.** قاعده‌ی «هرچه `/dong` نیست، شخصی است» به `/onboarding` و به صفحه‌ی ۴۰۴ هم یک نوار ناوبری می‌داد و به کسی که روی صفحه‌ای گم‌شده ایستاده می‌گفت وسط حسابداری‌اش است.
- **پوسته کلاینت شد، چون layoutِ سرور آدرس را نمی‌داند.** جایگزینِ ردشده: یک layout جدا برای هر بخش — یعنی دو نسخه از همان پوسته که باید تا ابد مو‌به‌مو یکی بمانند، و یک صفحه‌ی hub که باید از هر دو بیرون بزند.
- **نوار پایین با یک آیتم، نوار نیست.** «دنگ و دونگ» یک صفحه دارد؛ نواری که نمی‌تواند تو را جایی ببرد که نیستی، فقط ۵۶ پیکسل از گوشیِ کاربر را می‌خورد تا همین را بگوید. در عوض سوییچِ بالای صفحه هست، که در هر دو بخش دیده می‌شود و می‌گوید کجا می‌برد.
- **لینکِ پروفایل فقط در بخش شخصی است.** تنظیمات یکی از صفحه‌های همان بخش است، و لینکی که کاربر را بی‌صدا از وسط سفرش بیرون می‌برد دقیقاً همان چیزی است که این جداسازی برای جلوگیری از آن است.

## پولِ دنگ و دونگ، در دفترِ خودت

### مسئله

- **تا حالا این دو نیمه به هم دست نمی‌زدند، و این درباره‌ی پول غلط بود.** وقتی صاحب گوشی شام شش نفر را حساب می‌کند، کلِ فاکتور همان شب از یکی از حساب‌هایش می‌رود؛ وقتی بقیه پس می‌دهند، به یکی از حساب‌هایش برمی‌گردد. اگر این دو حرکت در دفتر نباشند، هر موجودی‌ای که اپ حساب می‌کند دقیقاً به‌اندازه‌ی خرجِ آن سفر غلط است — و بی‌صدا غلط، که تنها نوعِ ماندگارِ غلط است.
- **پس دوره یک حساب دارد، و هر خرید و پرداخت هم می‌تواند حساب خودش را داشته باشد.** سؤال «از کدام حسابت» همان اول دوره پرسیده می‌شود، چون همان‌جاست که کاربر جوابش را می‌داند («این سفر را با کارت فلان حساب می‌کنم») و چون یک‌بار جواب‌دادن است که هر خرید بعدی را به یک لمس می‌رساند. روی خودِ خرید هم قابل عوض‌کردن است، چون یک سفر معمولاً نصف کارت است و نصف نقد.

### کلِ فاکتور می‌رود، نه سهمِ کاربر

- **آنچه ثبت می‌شود همان چیزی است که بانک خواهد گفت.** سهم بقیه هر وقت پس داده شد، به‌عنوان یک ردیف جدا برمی‌گردد. جایگزینِ ردشده: ثبتِ فقط سهمِ خود کاربر — که موجودی حساب را همان شب غلط می‌کرد و صورت‌حسابِ اول ماه را غیرقابل‌تطبیق.
- **برگشتی، درآمد است اما نه از جنس حقوق.** برای همین دو دسته‌ی سیستمیِ تازه ساخته شد — «دنگ و دونگ» برای خروجی و «برگشتی دنگ و دونگ» برای ورودی — وگرنه یک سفر، هم دونات داشبورد را یک قاچِ «بدون دسته» می‌کرد و هم «درآمد این ماه» را با پولی که درآمد نبود باد می‌کرد.
- **خرجِ دیگران آینه نمی‌شود.** کارتِ یک نفر دیگر، دفترِ این کاربر نیست — هرقدر هم از آن فاکتور به گردنش باشد. همین‌طور پرداختی که بین دو نفر دیگر رد و بدل می‌شود.
- **ارز باید یکی باشد.** این اپ هیچ‌جا نرخ تبدیل ندارد؛ دوره‌ای به یورو از حسابی به تومان اجرا نمی‌شود. فرم اصلاً چنین حسابی را پیشنهاد نمی‌دهد، action با پیام فارسی ردش می‌کند، و تریگر با خطا — سه لایه برای یک قاعده، چون تنها راه دیگر اختراعِ یک نرخ بود.

### چرا تریگر، نه همان action

- **یک واقعیت است — «این مبلغ در این تاریخ از این حساب رفت» — و دو جایی که بگویندش بالاخره با هم اختلاف پیدا می‌کنند.** وقتی پیدا کنند، موجودیِ آن حساب یک ماه است که غلط بوده. همان استدلالِ «جمعِ سهم‌ها باید خرید را بپوشاند» که آن هم به‌جای اعتبارسنجی، constraint است.
- **سه رویداد یک sync را لازم دارند**، پس منطق در دو تابع نشسته و تریگرها پوسته‌اند: تغییر خودِ ردیف، و تغییرِ اینکه کدام عضو «من» است. بدون سومی، سفری که کاربر پولش را داده بعد از اینکه بگوید پرداخت‌کننده کس دیگری بوده، همچنان از حسابش کم می‌کرد.
- **ردیفِ آینه از سمت شخصی ویرایش و حذف نمی‌شود.** ویرایشش دفعه‌ی بعد که خریدِ پشتش دست بخورد پاک می‌شد، و حذفش حساب را به‌اندازه‌ی فاکتوری که واقعاً پرداخت شده کم می‌گذاشت. شیتِ ویرایش این ردیف را فقط نشان می‌دهد و در دکمه‌اش می‌گوید کجا می‌شود عوضش کرد؛ action هم همان را رد می‌کند، چون یک دکمه‌ی خاموش قاعده نیست.
- **حذفِ دوره، آینه‌ها را هم می‌برد** (`on delete cascade`). سایه‌ای که از چیزی که ساخته بودش بیشتر عمر کند، ردیفی است که هیچ‌کس نمی‌تواند توضیحش بدهد و هیچ‌کس نمی‌تواند ویرایشش کند، و تا ابد داخل یک موجودی می‌نشیند.

### دو چیز کوچک که سرِ راه درست شد

- **قفلِ واحد پول حالا پرداخت‌ها را هم می‌شمارد.** قبلاً فقط «خرید» را می‌شمرد، پس دوره‌ای که تنها ردیف‌هایش پرداخت بود، می‌شد واحد پولش را از زیر دو تراکنشِ آینه‌شده عوض کرد.
- **`paidBy` و دو سرِ پرداخت حالا بررسی می‌شوند که واقعاً در همین دوره باشند.** کلید خارجی به عضوِ یک دوره‌ی دیگر ایراد نمی‌گرفت؛ همان readی که می‌پرسد «این نفر خودِ کاربر است؟» این را هم جواب می‌دهد.
- **یک ایرادِ قدیمیِ کشف‌شده و دست‌نخورده:** تریگرِ `dong_expenses_covered` از migration 0010، برخلاف جفتش روی `dong_expense_shares`، بررسی نمی‌کند که خرید در همان تراکنش حذف شده باشد. یعنی ساختن و حذف‌کردن یک خرید در *یک* تراکنش، سرِ commit خطا می‌دهد. از مسیر اپ غیرقابل‌دسترسی است (هر درخواست تراکنش خودش را دارد) و موقع تست دستیِ تریگرها پیدا شد؛ عمداً همین‌جا نوشته شده تا دفعه‌ی بعد کسی دو ساعت دنبالش نگردد.

## captcha را روشن نکردیم، و سوراخ را با عدد بستیم

- **captcha در سوپابیس پروژه‌ای است، نه per-endpoint.** یک سوییچ واحد است که `signInWithPassword`، `signUp`، `resetPasswordForEmail` و `signInAnonymously` را با هم پوشش می‌دهد. نمی‌شود فقط جلوی ورودِ مهمان گذاشتش، که تنها جایی است که واقعاً لازمش داریم.
- **روشن‌کردنش کل مجموعه‌ی e2e را از کار می‌انداخت.** هر ۱۳ اسپک وارد می‌شوند — بعضی با فرم، بعضی مستقیم با API — و Playwright نمی‌تواند captcha حل کند. از جمله `isolation.spec.ts` که تنها چیزی است که RLS را می‌سنجد، یعنی تنها چیزی که بین پولِ دو نفر ایستاده. کلیدهای آزمایشیِ hCaptcha هم جواب نمی‌داد: راز، پروژه‌ای است، و گذاشتنِ رازِ آزمایشی یعنی captcha هیچ محافظتی نمی‌کند.
- **معامله‌اش بد بود.** یک ساعت قبلش، همین مجموعه‌ی e2e باگی را گرفت که صفحه‌ی اولِ بعد از ورود را کاملاً از کار انداخته بود و از build و typecheck رد شده بود. از دست دادن آن، در ازای بستنِ سوراخی که چند سِنت در ساعت ارزش دارد، منطقی نبود.
- **پس همان سقف را با دو عدد بستیم:** `rate_limit_anonymous_users` از ۳۰ به ۱۰ در ساعت، و `GUEST_DAILY_CALL_LIMIT` از ۵ به ۳. حاصل‌ضربشان سقفِ واقعیِ سوءاستفاده است: از «۳۰ × ۵ = تا ۱۵۰ پردازش در ساعت» به «۱۰ × ۳ = تا ۳۰». هر دو با یک تغییر برگشت‌پذیرند.
- **کلیدهای hCaptcha ساخته و نگه داشته شدند** (Sitekey در env، Secret در `.env.local`). اگر بعداً سوءاستفاده‌ی واقعی دیده شد، راهش این است: اول کمک‌کنندهٔ ورودِ تست‌ها از رمز به لینکِ ساخته‌شده با admin API منتقل شود تا از مسیرِ captcha رد نشود، بعد captcha روشن شود. ترتیبش مهم است.

## تستِ دودی: چیزی که هیچ دروازه‌ای نمی‌دیدش

### مسئله

- **هیچ‌کدام از پنج مرحله‌ی `verify` یک صفحه را باز نمی‌کند.** typecheck، lint، check:rtl، تست واحد و build — صفحه‌ای که کامپایل می‌شود و بعد *موقع رندر* می‌شکند، از هر پنج‌تا سبز رد می‌شود. این فرضی نیست: صفحه‌ی hub با همین وضعیت به یک preview رفت و «این صفحه بالا نیامد» نشان داد، چون یک Server Component نقشه‌ای از کامپوننت‌ها را از ماژولی با `"use client"` می‌خواند و `undefined` می‌گرفت. build سبز، typecheck سبز، preview سبز.
- **تست E2E موجود این را نمی‌گیرد، چون همه‌ی صفحه‌ها را باز نمی‌کند.** هر spec سراغ فیچر خودش می‌رود؛ صفحه‌ای که هیچ spec‌ای بازش نمی‌کند، تا وقتی کاربر بازش نکند سالم به‌نظر می‌رسد.

### تصمیم

- **یک spec که هر مسیر را یک‌بار باز می‌کند و تقریباً هیچ ادعای دیگری نمی‌کند** (`tests/e2e/smoke.spec.ts`). ارزان است و کامل؛ کامل‌بودنش تمام ارزشش است.
- **سه بررسی به‌ازای هر صفحه، چون صفحه به سه شکلِ نامشابه می‌شکند:** کدِ پاسخ (سروری که اصلاً رد کرده)، عنوانِ خودِ error boundary (Server Componentی که throw کرده — سرور dev کدِ ۲۰۰ می‌دهد و HTMLِ boundary را می‌فرستد، پس فقط نگاه‌کردن به status آن را «قبول» می‌خواند)، و استثنای گرفته‌نشده در مرورگر (کامپوننتی که بعد از hydration می‌شکند و status و markup هر دو سالم می‌مانند).
- **خطاها جمع می‌شوند، نه اینکه سرِ اولی پرتاب شوند.** وقتی چیزِ مشترکی می‌شکند همه‌ی مسیرها می‌شکنند، و فهرستِ کاملشان همین را می‌گوید؛ اولین خطا به‌تنهایی آدم را دنبال یک صفحه می‌فرستد.
- **هیچ ردیفی نمی‌نویسد.** فقط ورود و نگاه‌کردن. همین است که اجازه می‌دهد روی هر pull request به پروژه‌ی واقعی وصل شود، برخلاف بقیه‌ی E2E که ردیف می‌سازند و دستی اجرا می‌شوند.
- **نشانه‌ی هر صفحه با دقت انتخاب شد، نه «یک h1 وجود دارد».** خودِ error boundary هم h1 دارد. برای داشبورد هم h1 به درد نمی‌خورد: در قالب موبایل اصلاً دیده نمی‌شود (در هدری است که از ۹۶۰px به بعد وجود دارد)، پس نشانه‌اش «مانده‌ی این ماه» است که در هر دو حالتِ صفحه هست.

### چرا workflow جدا

- **`verify` هیچ secretی ندارد، و این خاصیت ارزش نگه‌داشتن دارد** — روی pull request از fork اجرا می‌شود و چیزی برای لو‌دادن ندارد. تستِ دودی نمی‌تواند این خاصیت را داشته باشد (با یک حساب واقعی به پروژه‌ی واقعی وصل می‌شود)، پس به‌جای اینکه `verify` را هم با خودش پایین بکشد، جدا نگه داشته شد.
- **سه secret، و عمداً نه چهارتای دیگر:** نه service-role key، نه کلید OpenRouter، نه CRON_SECRET. باز کردن صفحه‌ها به هیچ‌کدام کاری ندارد، و workflowی که اعتبارنامه‌ای را نگه دارد که استفاده نمی‌کند، یعنی آن اعتبارنامه یک‌جای دیگر هم هست.
- **بدون secret، job با اخطار رد می‌شود، نه با شکست.** یک clone تازه نباید به‌خاطر چیزی که از داخل کد قابل درست‌کردن نیست قرمز بماند. در عوض در README و در خودِ workflow نوشته شده که **تیکِ سبزِ یک اجرای رد‌شده یعنی هیچ صفحه‌ای باز نشده** — وگرنه دقیقاً همان توهمِ پوششی ساخته می‌شود که این کار برای از‌بین‌بردنش انجام شد.
- **build واقعی، نه سرور dev.** باگی که این تست برایش نوشته شد درباره‌ی مرزِ سرور/کلاینت است، و آن مرز در بیلد واقعی طور دیگری کشیده می‌شود.

### اینکه واقعاً کار می‌کند، آزموده شد

- **قبل از تحویل، خرابی دوباره ساخته شد:** یک `throw` در hub، build، و اجرا — تست با پیامِ «`/` rendered the error boundary» قرمز شد. بعد برگردانده شد و دوباره سبز شد. تستی که ندیده‌ای شکست بخورد، معلوم نیست چیزی را بگیرد.

### سرصفحه‌ای که روی موبایل وجود نداشت

- **`<h1>داشبورد</h1>` داخل ردیفی بود که فقط از ۹۶۰px به بعد وجود دارد** (`hidden min-[960px]:flex`)، یعنی روی گوشی — همان جایی که این اپ عمدتاً باز می‌شود — صفحه‌ی اولِ کاربر هیچ سرصفحه‌ای نداشت. کسی که با screen reader و با پرش بین سرصفحه‌ها حرکت می‌کند، در داشبورد چیزی برای پریدن پیدا نمی‌کرد.
- **حالا یک سرصفحه است، نه دوتا:** `sr-only` تا ۹۶۰px و `not-sr-only` از آن به بعد. دو تگ جدا — یکی مخفی برای موبایل و یکی دیده‌شدنی برای دسکتاپ — یعنی روی دسکتاپ دو `h1` در درخت دسترس‌پذیری، که مشکل را با یک مشکل دیگر عوض می‌کرد.
- **چیدمان دسکتاپ دست نخورد**، و این با اندازه‌گیری بررسی شد نه با نگاه‌کردن: در ۱۲۸۰px سرصفحه ۵۹×۲۶ در y=33 و دکمه‌ی «ثبت هزینه» ۱۱۶×۴۴ در y=24 — همان یک ردیف قبلی. در ۳۷۵px سرصفحه ۱×۱ است (اعلام می‌شود، دیده نمی‌شود) و دکمه‌ی شناور سرِ جای خودش پایین صفحه.
- **قاعده‌اش در تست دودی ماند، نه فقط در این یک صفحه:** هر مسیری که باز می‌شود باید دست‌کم یک سرصفحه‌ی سطح یک داشته باشد. با برگرداندنِ موقتِ همان markup قدیمی آزموده شد — تست با «`/dashboard` has no level-1 heading» قرمز شد و با برگشتِ اصلاح، سبز.
