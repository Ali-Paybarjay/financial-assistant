# پرامپت ساخت محصول: دستیار مالی (MVP)

> این فایل را به‌صورت کامل داخل Claude Code paste کن، یا آن را در ریشه‌ی پروژه با نام `BRIEF.md` ذخیره کن و به Claude Code بگو: `Read BRIEF.md and follow it.`

---

## ۰. نقش تو و قواعد کار

تو یک senior full-stack engineer هستی که مسئول ساخت کامل یک وب‌اپلیکیشن از صفر تا deploy است. قواعد زیر در تمام مدت پروژه معتبرند:

1. **اول نقشه، بعد کد.** قبل از نوشتن هر خط کد، فایل `PLAN.md` را بساز شامل: architecture، مدل داده، ساختار پوشه‌ها، design token system، و تقسیم کار به milestoneها. بعد از ساختن `PLAN.md` **متوقف شو** و منتظر تأیید من بمان.
2. **کار را milestone به milestone تحویل بده.** انتهای هر milestone: `pnpm typecheck`، `pnpm lint` و `pnpm build` باید بدون خطا پاس شوند. سپس یک گزارش کوتاه بده (چه ساخته شد، چه تصمیمی گرفتی، چه چیزی باز ماند) و **متوقف شو**.
3. **هر تصمیم فنی مهم را در `DECISIONS.md` بنویس** (یک خط: تصمیم + دلیل + جایگزین رد شده).
4. **حدس نزن، بپرس.** اگر جایی از این سند مبهم بود یا با واقعیت کد در تضاد بود، قبل از پیاده‌سازی سؤال بپرس. اما برای تصمیم‌های کوچک و برگشت‌پذیر، تصمیم بگیر و در `DECISIONS.md` ثبت کن.
5. **هیچ فیچری خارج از دامنه‌ی این سند نساز.** بخش «خارج از دامنه» را جدی بگیر.
6. **زبان کد انگلیسی، زبان رابط کاربری فارسی.** نام متغیرها، توابع، جدول‌ها، کامیت‌ها و کامنت‌ها انگلیسی. تمام متن‌های قابل‌مشاهده برای کاربر فارسی.
7. کامیت‌های atomic با پیام‌های conventional (`feat:`, `fix:`, `chore:`).

---

## ۱. محصول در یک پاراگراف

«دستیار مالی» یک وب‌اپلیکیشن مدیریت مالی شخصی برای فارسی‌زبانان مقیم خارج از ایران است. کاربر ثبت‌نام می‌کند، یک onboarding کوتاه را طی می‌کند تا تصویر مالی‌اش (درآمد، هزینه‌های ثابت، اهداف، ریسک‌پذیری) ثبت شود، و بعد از آن هزینه‌های روزمره‌اش را به چهار روش ثبت می‌کند: فرم دستی، تایپ متن آزاد، ضبط صدا، و عکس فاکتور. سه روش آخر با AI به داده‌ی ساختاریافته تبدیل می‌شوند. داشبورد در هر لحظه نشان می‌دهد این ماه چقدر درآمد داشته، چقدر خرج کرده، چقدر باقی مانده و نرخ پس‌اندازش چقدر است.

**جمله‌ی محک محصول:** کاربر باید بتواند در کمتر از ۱۰ ثانیه یک خرید را ثبت کند و در کمتر از ۵ ثانیه بفهمد این ماه چقدر پول برایش مانده. هر تصمیم طراحی که این دو عدد را بدتر کند، اشتباه است.

---

## ۲. مخاطب و تصمیمات locale

| مورد | تصمیم |
|---|---|
| مخاطب | فارسی‌زبانان مقیم کانادا / آمریکا / اروپا |
| زبان رابط | فارسی، RTL کامل |
| ارز پایه | `CAD` پیش‌فرض. کاربر در onboarding یکی از `CAD` / `USD` / `EUR` / `GBP` / `AUD` را انتخاب می‌کند. **در MVP هر کاربر فقط یک ارز دارد** — تبدیل ارز و چندارزی همزمان نداریم. |
| تقویم | میلادی (Gregorian). از تقویم شمسی خبری نیست. |
| فرمت تاریخ | `fa-IR` با `calendar: gregory` — مثال: «۱۲ آگوست ۲۰۲۶». |
| ارقام پول | همیشه ارقام لاتین با جداکننده‌ی هزارگان و `font-variant-numeric: tabular-nums`. مبلغ‌ها داخل متن فارسی باید با `dir="ltr"` و `unicode-bidi: isolate` ایزوله شوند تا جای علامت ارز به هم نریزد. |
| timezone | در `profiles.timezone` ذخیره شود (پیش‌فرض از مرورگر). «ماه جاری» بر اساس timezone کاربر محاسبه شود، نه UTC. |

---

## ۳. استک فنی

```
Framework      Next.js 15 (App Router) + TypeScript (strict)
UI             Tailwind CSS v4 + shadcn/ui (RTL configured)
Database       Supabase Postgres
Auth           Supabase Auth (email + password, و Google OAuth)
Storage        Supabase Storage (باکت‌های خصوصی: receipts, voice-notes)
Server logic   Next.js Route Handlers + Server Actions
Validation     Zod (در مرز هر ورودی: form، API، AI output)
Forms          react-hook-form + zodResolver
State          TanStack Query برای داده‌ی سرور
Charts         Recharts
Package mgr    pnpm
Tests          Vitest (unit) + Playwright (یک happy path E2E)
Deploy         Vercel + Supabase cloud
```

**AI providers:**
- استخراج ساختار از متن و از عکس فاکتور: **Anthropic Claude API** (مدل `claude-sonnet-4-6`). Claude ورودی تصویری را مستقیم می‌پذیرد، پس برای فاکتور به OCR جداگانه نیاز نیست.
- تبدیل صدا به متن (STT): پشت یک interface به نام `SpeechToTextProvider` پیاده‌سازی کن. implementation پیش‌فرض: OpenAI Whisper API (`whisper-1`) چون فارسی را خوب پشتیبانی می‌کند. طوری بنویس که تعویض provider فقط یک فایل را عوض کند.

**قواعد غیرقابل‌مذاکره‌ی امنیتی:**
- هیچ API key ای هرگز به client نمی‌رود. تمام فراخوانی‌های AI فقط در Route Handler سمت سرور.
- `SUPABASE_SERVICE_ROLE_KEY` فقط سمت سرور و فقط جایی که واقعاً لازم است.
- Row Level Security روی **تمام** جدول‌ها فعال باشد.

**متغیرهای محیطی** (یک `.env.example` کامل بساز):
```
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
NEXT_PUBLIC_APP_URL=
```

---

## ۴. دامنه‌ی نسخه‌ی اول

### داخل دامنه

1. ثبت‌نام، ورود، خروج، بازیابی رمز
2. Onboarding چندمرحله‌ای resumable
3. مدیریت منابع درآمد و هزینه‌های ثابت تکرارشونده
4. ثبت تراکنش با ۴ روش: فرم / متن آزاد / ویس / عکس فاکتور
5. لیست تراکنش‌ها با فیلتر، ویرایش و حذف
6. دسته‌بندی هزینه‌ها
7. اهداف مالی (ثبت و نمایش پیشرفت)
8. داشبورد ماهانه
9. تنظیمات و ویرایش پروفایل

### خارج از دامنه — نساز

اتصال به بانک یا Plaid · حساب مشترک/خانوادگی · مدیریت پورتفوی و سرمایه‌گذاری · بودجه‌بندی سقف‌دار per-category · اعلان و ایمیل یادآور · اپ موبایل یا PWA offline · چندارزی همزمان و نرخ تبدیل · گزارش PDF · دستیار چت مالی · پرداخت و اشتراک · پنل ادمین · چند زبانه بودن · دارک‌مود (فقط لایت‌مود در MVP)

اگر فکر کردی یکی از این‌ها لازم است، **اول بپرس**.

---

## ۵. مدل داده

Postgres، همه با `id uuid default gen_random_uuid()`، `created_at`، `updated_at`، و RLS با شرط `auth.uid() = user_id`.

> **قاعده‌ی حیاتی پول:** هیچ مبلغی به‌صورت float ذخیره نشود. تمام مبالغ `bigint` و بر حسب **کوچک‌ترین واحد ارز** (سِنت) هستند. یعنی ۴۵٫۵۰ دلار می‌شود `4550`. یک ماژول `lib/money.ts` بساز که تنها مسیر مجاز تبدیل، جمع، و فرمت‌کردن پول باشد.

```sql
-- پروفایل کاربر (۱:۱ با auth.users)
profiles(
  id uuid PK references auth.users on delete cascade,
  full_name text,
  country_code text,            -- ISO 3166-1 alpha-2
  timezone text,
  base_currency text,           -- CAD | USD | EUR | GBP | AUD
  birth_year int,               -- بازه‌ی معتبر: 1930..(سال جاری - 13)
  employment_status text,       -- employed | self_employed | student | retired | unemployed | other
  risk_score int,               -- 1..10
  risk_label text,              -- conservative | balanced | growth
  monthly_income_estimate bigint,
  has_debt boolean,
  debt_amount bigint,
  emergency_fund_months numeric(4,1),
  onboarding_step int default 0,
  onboarding_completed_at timestamptz
)

-- دسته‌بندی؛ ردیف‌های سیستمی user_id = null دارند
categories(
  id, user_id uuid null, name_fa text, slug text,
  kind text,                    -- expense | income
  icon text, color text,
  is_system boolean default false,
  sort_order int
)

income_sources(
  id, user_id, title text,
  type text,                    -- salary | freelance | business | investment | rental | pension | other
  amount bigint, currency text,
  frequency text,               -- monthly | biweekly | weekly | yearly | one_time
  is_active boolean default true,
  started_on date, ended_on date null
)

recurring_expenses(
  id, user_id, title text, category_id uuid,
  amount bigint, currency text,
  frequency text,               -- monthly | quarterly | yearly
  due_day int,                  -- 1..31
  is_active boolean default true,
  auto_post boolean default true   -- در ابتدای هر ماه خودکار به transactions تبدیل شود؟
)

transactions(
  id, user_id,
  type text,                    -- expense | income
  amount bigint, currency text,
  category_id uuid,
  merchant text, note text,
  occurred_on date not null,
  source text,                  -- form | text | voice | receipt | recurring
  media_asset_id uuid null,
  ai_confidence numeric(3,2) null,
  ai_raw jsonb null,            -- خروجی خام مدل، برای دیباگ
  deleted_at timestamptz null   -- soft delete
)

media_assets(
  id, user_id,
  kind text,                    -- audio | image
  storage_path text, mime_type text, size_bytes int,
  status text,                  -- uploaded | processing | parsed | failed
  transcript text null,         -- برای audio
  extracted jsonb null,
  error_message text null
)

goals(
  id, user_id, title text,
  type text,                    -- emergency_fund | home | travel | debt_payoff | education | investment | other
  target_amount bigint, saved_amount bigint default 0,
  target_date date, priority int, status text  -- active | achieved | paused | cancelled
)

ai_usage_logs(
  id, user_id, feature text, provider text, model text,
  input_tokens int, output_tokens int, cost_cents int,
  latency_ms int, status text, created_at
)
```

**ایندکس‌های لازم:** `transactions(user_id, occurred_on desc)`، `transactions(user_id, category_id)`، `media_assets(user_id, status)`.

**دسته‌بندی‌های پیش‌فرض (seed):** خوراک و سوپرمارکت · رستوران و کافه · مسکن و اجاره · قبوض · حمل‌ونقل · خودرو و سوخت · سلامت و درمان · پوشاک · سرگرمی · اشتراک‌ها · آموزش · هدیه · بیمه · بازپرداخت وام · متفرقه.

---

## ۶. جریان‌های کاربری

```
ثبت‌نام → تأیید ایمیل → Onboarding (۷ گام) → داشبورد
                                    ↑
                        هر گام قابل ذخیره و ادامه است
داشبورد → دکمه‌ی شناور «ثبت هزینه» → انتخاب روش → کارت تأیید → ذخیره → داشبورد به‌روز شده
```

هر کاربری که `onboarding_completed_at` ندارد و به `/dashboard` می‌رود، باید به گام بعدی‌اش redirect شود.

---

## ۷. مشخصات فیچرها

### ۷.۱ احراز هویت

- ثبت‌نام با ایمیل و رمز + ورود با Google
- تأیید ایمیل الزامی
- بازیابی رمز
- Middleware که تمام مسیرهای زیر `/(app)` را محافظت کند
- بعد از logout، cache سمت client پاک شود

### ۷.۲ Onboarding

هفت گام، هر گام یک صفحه با progress bar. **بعد از هر گام داده در دیتابیس ذخیره شود** و `profiles.onboarding_step` به‌روز شود تا کاربر بتواند ادامه دهد. گام‌های ۴ تا ۷ دکمه‌ی «فعلاً رد کن» دارند؛ گام‌های ۱ تا ۳ اجباری‌اند.

| گام | عنوان | فیلدها |
|---|---|---|
| ۱ | آشنایی | نام، کشور محل زندگی (select با پرچم)، ارز پایه، سال تولد، وضعیت اشتغال |
| ۲ | درآمد | متوسط درآمد ماهیانه + افزودن حداقل یک منبع درآمد (عنوان، نوع، مبلغ، تناوب) با امکان افزودن چند ردیف |
| ۳ | هزینه‌های ثابت | لیستی از چیپ‌های پیشنهادی (اجاره، قبوض، اینترنت، موبایل، بیمه، وام، اشتراک‌ها، حمل‌ونقل) که با کلیک یک ردیف با فیلد مبلغ اضافه می‌کنند + امکان ردیف دلخواه |
| ۴ | هزینه‌های متغیر | تخمین ماهانه در ۵ دسته‌ی اصلی، با اسلایدر یا input. اینها به عنوان baseline ذخیره می‌شوند نه تراکنش |
| ۵ | اهداف مالی | حداقل یک هدف: نوع، عنوان، مبلغ هدف، تاریخ هدف، اولویت |
| ۶ | ریسک‌پذیری | ۵ سؤال سناریومحور با ۴ گزینه (مثال: «سرمایه‌ات در یک ماه ۲۰٪ افت می‌کند. چه می‌کنی؟»). امتیاز ۱ تا ۱۰ محاسبه و به برچسب محافظه‌کار/متعادل/رشدمحور تبدیل شود |
| ۷ | وضعیت فعلی | آیا بدهی داری؟ (بله/خیر + مبلغ) · صندوق اضطراری چند ماه هزینه‌ات را پوشش می‌دهد؟ · چند درصد درآمدت را پس‌انداز می‌کنی؟ |

پایان onboarding: یک صفحه‌ی خلاصه («این تصویر مالی توست») با ۳ عدد کلیدی و دکمه‌ی ورود به داشبورد.

**نکته‌ی طراحی:** یک سؤال یا یک گروه سؤال مرتبط در هر صفحه. هرگز ۱۵ فیلد در یک فرم بلند.

### ۷.۳ درآمد و هزینه‌های ثابت

- صفحه‌ی `/income` و `/recurring` با CRUD کامل
- فعال/غیرفعال کردن یک ردیف بدون حذف آن
- برای ردیف‌های `auto_post = true`: یک تابع (Supabase cron یا فراخوانی lazy در اولین ورود هر ماه) که تراکنش‌های تکرارشونده‌ی ماه جاری را با `source = 'recurring'` می‌سازد. **حتماً idempotent باشد** — دو بار اجرا نباید دو تراکنش بسازد. یک unique constraint روی ترکیب `(recurring_expense_id, month)` بگذار.

### ۷.۴ ثبت تراکنش — چهار روش

یک دکمه‌ی شناور در داشبورد، یک modal با چهار تب: **فرم · متن · صدا · عکس**.

> **قاعده‌ی مشترک و حیاتی:** هر سه روش AI هرگز مستقیماً چیزی ذخیره نمی‌کنند. خروجی مدل همیشه در یک **کارت تأیید** با تمام فیلدهای قابل‌ویرایش به کاربر نشان داده می‌شود و فقط با کلیک «ثبت» ذخیره می‌شود. فیلدهایی که مدل با اطمینان پایین حدس زده، به‌صورت بصری متمایز شوند («این را حدس زدم، بررسی کن»).

**الف) فرم دستی** — مبلغ، دسته، تاریخ (پیش‌فرض امروز)، فروشنده، توضیح. کیبورد عددی روی موبایل. باید کمتر از ۱۰ ثانیه طول بکشد.

**ب) متن آزاد** — کاربر می‌نویسد: «امروز ۴۵ دلار خرید از سوپرمارکت و ۱۲ دلار قهوه». به Route Handler `/api/parse/text` می‌رود، Claude با یک prompt سخت‌گیر آن را به آرایه‌ای از تراکنش‌ها تبدیل می‌کند. **از حالت چند-تراکنشی پشتیبانی کن.**

**ج) صدا** — ضبط با `MediaRecorder` (فرمت `audio/webm` یا `audio/mp4` روی iOS — هر دو را تست کن). حداکثر ۶۰ ثانیه. آپلود به باکت خصوصی `voice-notes` → ثبت `media_assets` با `status = uploaded` → STT → transcript → همان pipeline متن. در UI نمایش موج صدا و تایمر. transcript قبل از parse به کاربر نشان داده شود.

**د) عکس فاکتور** — آپلود یا دوربین. سمت client به حداکثر عرض ۱۵۰۰px و کیفیت ۸۰٪ فشرده شود (`browser-image-compression`). حجم نهایی حداکثر ۵ مگابایت. آپلود به باکت `receipts` → ارسال به Claude به‌صورت base64 با prompt استخراج → JSON. **مبلغ کل** را استخراج کن، نه تک‌تک آیتم‌ها. اگر تاریخ روی فاکتور خوانا بود همان، وگرنه امروز.

**قرارداد خروجی AI** — دقیقاً این schema، با Zod در سمت سرور اعتبارسنجی شود و در صورت شکست، یک بار retry شود:

```ts
const ParsedTransaction = z.object({
  type: z.enum(["expense", "income"]),
  amount_minor: z.number().int().positive(),   // بر حسب سنت
  currency: z.string().length(3),
  category_slug: z.string(),                    // باید در لیست دسته‌های کاربر باشد
  merchant: z.string().nullable(),
  note: z.string().nullable(),
  occurred_on: z.string(),                      // YYYY-MM-DD
  confidence: z.number().min(0).max(1),
});
const ParseResult = z.object({
  transactions: z.array(ParsedTransaction).max(10),
  needs_review: z.array(z.string()),            // نام فیلدهای مشکوک
});
```

نکات prompt نویسی برای مدل:
- لیست دسته‌های موجود کاربر و ارز پایه‌اش را در prompt بده و بگو فقط از همین‌ها انتخاب کن.
- بگو خروجی فقط JSON باشد، بدون هیچ متن اضافه یا backtick.
- اگر مبلغ قابل تشخیص نبود، درخواست را reject کن و پیام خطای فارسی روشن برگردان — مبلغ را حدس نزن.
- تاریخ نسبی فارسی («دیروز»، «سه‌شنبه») را با تاریخ امروزِ کاربر که در prompt داده‌ای حل کن.

**کنترل هزینه:** سقف ۵۰ فراخوانی AI برای هر کاربر در هر روز. هر فراخوانی در `ai_usage_logs` ثبت شود. timeout هر فراخوانی ۳۰ ثانیه.

### ۷.۵ لیست تراکنش‌ها

`/transactions` — گروه‌بندی بر اساس روز، فیلتر بر اساس بازه‌ی تاریخ / دسته / نوع، جست‌وجو در فروشنده و توضیح، صفحه‌بندی یا infinite scroll، ویرایش inline، حذف نرم با امکان undo تا ۵ ثانیه. نمایش آیکون کوچک منبع ثبت (فرم/متن/صدا/عکس) روی هر ردیف.

### ۷.۶ داشبورد

مسیر `/dashboard`، با انتخابگر ماه در بالا (پیش‌فرض ماه جاری).

**ردیف اول — چهار کارت:**
1. درآمد این ماه
2. هزینه‌ی این ماه
3. مانده (درآمد منهای هزینه) — رنگ سبز اگر مثبت، قرمز اگر منفی
4. نرخ پس‌انداز: `(درآمد - هزینه) / درآمد` به درصد

هر کارت درصد تغییر نسبت به ماه قبل را هم نشان دهد.

**بقیه‌ی داشبورد:**
- نمودار دونات: هزینه به تفکیک دسته، با نمایش ۵ دسته‌ی برتر و تجمیع بقیه در «سایر»
- نمودار میله‌ای: درآمد و هزینه در ۶ ماه اخیر
- کارت اهداف: نوار پیشرفت هر هدف فعال
- ۱۰ تراکنش اخیر
- دکمه‌ی شناور ثبت هزینه (پایین راست در RTL)

**حالت‌های خالی را جدی بگیر.** کاربر تازه‌وارد داشبورد خالی می‌بیند؛ به‌جای نمودار خالی، یک راهنمای کوتاه با دکمه‌ی «اولین هزینه‌ات را ثبت کن» نشان بده.

### ۷.۷ تنظیمات

ویرایش پروفایل، تغییر ارز پایه (با هشدار که داده‌های قبلی تبدیل نمی‌شوند)، بازپاسخ‌دهی به سؤالات ریسک، مدیریت دسته‌ها، خروج از حساب، حذف حساب.

---

## ۸. جهت‌گیری طراحی

قبل از کدزدن، در `PLAN.md` یک design token system بنویس: ۴ تا ۶ رنگ نام‌گذاری‌شده با hex، مقیاس تایپوگرافی، و «عنصر امضا» — آن یک چیزی که این اپ با آن به یاد آورده می‌شود. سپس آن را نقد کن: اگر هر بخشی از آن همان چیزی است که برای هر اپ مالی دیگری هم تولید می‌کردی، عوضش کن و بگو چرا.

قیدهای ثابت:

- **RTL کامل.** `dir="rtl"` روی `<html>`. از `ms-`/`me-`/`ps-`/`pe-` استفاده کن، نه `ml-`/`mr-`. آیکون‌های جهت‌دار (فلش‌ها) باید mirror شوند.
- **فونت:** بدنه `Vazirmatn`. برای نقش display یک فونت فارسی متمایز انتخاب کن (مثلاً `Morabba` یا `Estedad`) و با خساست از آن استفاده کن. self-host کن، از CDN های تحریم‌خورده استفاده نکن.
- **اعداد پول** با ارقام لاتین، `tabular-nums`، و ایزوله‌ی جهت. این را در یک کامپوننت `<Money />` کپسوله کن تا هیچ‌جا دستی فرمت نشود.
- پالت آرام و مالی. رنگ فقط جایی که معنا دارد: مثبت/منفی، وضعیت هدف. نمودارها نباید رنگین‌کمان باشند.
- **از این سه لوک پرهیز کن** چون امضای طراحی AI هستند و نشان می‌دهند انتخابی صورت نگرفته: پس‌زمینه‌ی کرم گرم با سریف پرکنتراست و لهجه‌ی خاکی-نارنجی؛ پس‌زمینه‌ی تقریباً مشکی با یک سبز اسیدی؛ لی‌اوت روزنامه‌ای با خط‌های مویی و صفر گردی گوشه.
- کف کیفیت بدون اعلام: ریسپانسیو تا موبایل، focus قابل‌مشاهده با کیبورد، احترام به `prefers-reduced-motion`، کنتراست AA.
- **متن رابط:** فعل فعال، جمله‌ی ساده، بدون تعارف. دکمه «ثبت هزینه» نه «ارسال». خطا توضیح می‌دهد چه شد و کاربر چه کند، عذرخواهی نمی‌کند. صفحه‌ی خالی یک دعوت به عمل است.

طراحی موبایل-اول. اکثر ثبت‌های هزینه روی گوشی اتفاق می‌افتد.

---

## ۹. Milestoneها

بعد از هر کدام متوقف شو و گزارش بده.

| # | عنوان | تحویل |
|---|---|---|
| M0 | پایه | `PLAN.md`، اسکلت Next.js، Tailwind + RTL، shadcn، فونت‌ها، migration های Supabase، RLS، seed دسته‌ها، `.env.example`، README |
| M1 | احراز هویت | ثبت‌نام/ورود/خروج/بازیابی رمز، middleware، layout محافظت‌شده |
| M2 | Onboarding | هر ۷ گام، resumable، صفحه‌ی خلاصه |
| M3 | درآمد و هزینه‌ی ثابت | CRUD کامل + job تولید تراکنش‌های تکرارشونده به‌صورت idempotent |
| M4 | تراکنش‌ها | ماژول `money.ts`، فرم دستی، لیست، فیلتر، ویرایش، حذف نرم |
| M5 | داشبورد | چهار کارت، دو نمودار، اهداف، تراکنش‌های اخیر، حالت خالی |
| M6 | ورودی متنی با AI | Route Handler، Zod schema، کارت تأیید، rate limit، لاگ |
| M7 | ورودی صوتی | ضبط، آپلود، STT، اتصال به pipeline متن |
| M8 | عکس فاکتور | فشرده‌سازی، آپلود، استخراج با Claude vision |
| M9 | تنظیمات و صیقل | صفحه‌ی تنظیمات، حالت‌های خطا و بارگذاری، تست E2E، deploy روی Vercel |

اگر جایی مجبور شدی بین «سریع‌تر تمام کن» و «درست تمام کن» انتخاب کنی، درست را انتخاب کن و به من بگو چقدر بیشتر طول می‌کشد.

---

## ۱۰. تعریف «تمام شده»

یک milestone وقتی تمام است که:

- `pnpm typecheck && pnpm lint && pnpm build` بدون خطا و بدون warning جدید پاس شود
- هیچ `any` بدون کامنت توجیهی در کد نباشد
- تمام ورودی‌های کاربر و تمام خروجی‌های AI با Zod اعتبارسنجی شده باشند
- هر عملیات async سه حالت loading، error و empty را در UI داشته باشد
- روی عرض ۳۷۵px بدون شکستن لی‌اوت کار کند
- RLS برای جدول‌های جدید نوشته و تست شده باشد (با یک کاربر دوم تست کن که واقعاً داده‌ی کاربر اول را نمی‌بیند)
- هیچ کلید یا secret ای در کد کلاینت نباشد

### تست‌های پذیرش MVP

1. کاربر جدید ثبت‌نام می‌کند، onboarding را وسط راه رها می‌کند، بعداً برمی‌گردد و از همان‌جا ادامه می‌دهد.
2. کاربر می‌نویسد «۴۵ دلار خرید سوپرمارکت و ۱۲ دلار قهوه» و دو تراکنش با دسته‌ی درست در کارت تأیید ظاهر می‌شود.
3. کاربر یک ویس ۱۵ ثانیه‌ای فارسی ضبط می‌کند و تراکنش استخراج می‌شود.
4. کاربر عکس یک فاکتور را آپلود می‌کند و مبلغ کل درست خوانده می‌شود.
5. مجموع کارت «هزینه‌ی این ماه» دقیقاً برابر جمع تراکنش‌های حذف‌نشده‌ی آن ماه است.
6. کاربر دوم هیچ داده‌ای از کاربر اول نمی‌بیند — نه در UI، نه با فراخوانی مستقیم API.
7. اجرای دوباره‌ی job تراکنش‌های تکرارشونده هیچ رکورد تکراری نمی‌سازد.

---

## ۱۱. اولین کاری که باید بکنی

`PLAN.md` را بنویس شامل: تصمیم‌های معماری، ساختار پوشه‌ها، schema نهایی SQL، design token system به همراه نقد خودت روی آن، و ریسک‌های فنی‌ای که می‌بینی. بعد **متوقف شو** و منتظر تأیید من بمان. کد نزن.
