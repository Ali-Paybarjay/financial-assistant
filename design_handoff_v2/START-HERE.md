# از کجا شروع کن

> **Claude Code با این فایل شروع می‌کند: `CLAUDE-CODE-PROMPT.md`**
> همین یک جمله را به Claude Code بده:
>
> ```
> فایل CLAUDE-CODE-PROMPT.md را بخوان و از قدم ۰ شروع کن.
> ```

---

## ترتیب خواندن

| # | فایل | چیست | برای کی |
|---|---|---|---|
| ۱ | **`CLAUDE-CODE-PROMPT.md`** | پرامپت پیاده‌سازی: ۶ قدم، قیدهای سخت، معیار پذیرش هر قدم، فهرست «انجام نده» | Claude Code — **نقطهٔ شروع** |
| ۲ | `spec-envelopes-and-insights.md` | schema، تابع SQL، توابع خالص، و هشت قاعدهٔ بینش با ورودی/خروجی | Claude Code، در قدم‌های ۱ و ۴ |
| ۳ | `BRIEF-design-complete.md` | بریف طراحی: توکن‌ها، دستور بصری، اجزا، ناوبری، جدول صفحه‌به‌صفحه، حالت‌های خالی، لحن متن، دسترس‌پذیری | Claude Code هنگام ساخت UI + تو برای مرور |
| ۴ | `screens/01…17.png` | یک اسکرین‌شات به هر صفحه (۲x) | مرجع بصری هر قدم |
| ۵ | `دستیار مالی - طراحی کامل.dc.html` | طراحی زنده، همهٔ ۱۷ صفحه در یک بورد | مرور با چشم؛ در مرورگر باز می‌شود |
| ۶ | `دستیار مالی - چهار دموی طراحی.dc.html` | چهار قالبِ اولیه + ترکیبشان + بازسازی وضعیت فعلی — تاریخچهٔ تصمیم | آرشیو؛ برای پیاده‌سازی لازم نیست |

---

## نگاشت اسکرین‌شات به قدم

| قدم در پرامپت | اسکرین‌شات‌ها |
|---|---|
| ۰ — پاک‌سازی توکن‌ها | — |
| ۱ — داده و منطق پاکت‌ها | — (فقط تست) |
| ۲ — بورد پاکت‌ها | `01-envelopes` · `05-envelope-detail` · `17-desktop` |
| ۳ — نوار نوشتن | `03-confirm-card` · و نوار پایین در همهٔ صفحه‌ها |
| ۴ — تب جریان | `02-stream` |
| ۵ — اونبوردینگ سقف‌ها و حافظهٔ انتخاب بخش | `16-onboarding-budgets` · `15-onboarding-risk` · `14-hub` · `10-settings` |
| صفحه‌های بدون تغییر ساختاری (مرجع) | `04-transactions` · `06-goals` · `07-accounts` · `08-import` · `09-income` · `11-dong-list` · `12-dong-group` · `13-login` |

---

## دو چیز که قبل از کد باید تصمیم بگیری

۱. **نام تب:** «جریان» یا «دستیار»؟ (در طراحی «جریان» است)
۲. **`TOTAL_STEPS` از ۷ به ۸ می‌رود** چون گام سقف پاکت‌ها اضافه می‌شود. اگر نمی‌خواهی اونبوردینگ بلندتر شود، این گام را از اونبوردینگ بیرون بگذار و به اولین ورودِ بورد منتقل کن.

چهار سؤال باز دیگر آخر `BRIEF-design-complete.md` بخش ۱۲ است.
