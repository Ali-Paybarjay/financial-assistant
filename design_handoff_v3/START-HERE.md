# از کجا شروع کن

> **Claude Code با این فایل شروع می‌کند: `CLAUDE-CODE-INSTALL.md`**
> بستهٔ کد آماده در `handoff/` است و فقط باید جایگزین شود. همین یک جمله را بده:
>
> ```
> فایل CLAUDE-CODE-INSTALL.md را بخوان و از قدم ۰ شروع کن.
> ```
>
> اگر خواستی از صفر بسازد (بدون بستهٔ کد)، به‌جایش `CLAUDE-CODE-PROMPT.md` را بده.

---

## ترتیب خواندن

| # | فایل | چیست | برای کی |
|---|---|---|---|
| ۱ | **`CLAUDE-CODE-INSTALL.md`** | نصب بستهٔ کد آماده: چه فایلی کجا می‌رود و چه سه فایلی دستی ویرایش می‌شود | Claude Code — **نقطهٔ شروع** |
| ۲ | `handoff/` | کد آماده: CSS توکن‌ها (روشن+تیره)، پروایدر تم، انتخابگر تم، کارت مانده، کارت پاکت، نوار نوشتن، منطق سقف‌ها، migration | Claude Code |
| ۳ | `CLAUDE-CODE-PROMPT.md` | نسخهٔ «از صفر بساز» در ۶ قدم (اگر بستهٔ کد را نخواستی) | Claude Code |
| ۴ | `spec-envelopes-and-insights.md` | schema، تابع SQL، هشت قاعدهٔ بینش | قدم‌های ۱ و ۴ |
| ۵ | `BRIEF-design-complete.md` | بریف کامل: توکن‌ها، دستور بصری، جدول صفحه‌ها، تم تیره، لحن متن | تو + Claude Code |
| ۶ | `screens/01…26.png` | اسکرین‌شات هر صفحه (۲x) | مرجع بصری |
| ۷ | `دستیار مالی - طراحی کامل.dc.html` | طراحی زنده، همهٔ ۲۶ صفحه | مرور با چشم |
| ۸ | `دستیار مالی - چهار دموی طراحی.dc.html` | چهار قالب اولیه و ترکیبشان | آرشیو |

---

## نگاشت اسکرین‌شات به قدم

| قدم در پرامپت | اسکرین‌شات‌ها |
|---|---|
| ۰ — پاک‌سازی توکن‌ها | — |
| ۱ — داده و منطق پاکت‌ها | — (فقط تست) |
| ۲ — بورد پاکت‌ها | `01-envelopes` · `05-envelope-detail` · `17-desktop` |
| ۳ — نوار نوشتن | `03-confirm-card` · و نوار پایین در همهٔ صفحه‌ها |
| ۴ — تب جریان | `02-stream` |
| ۵ — اونبوردینگ سقف‌ها و حافظهٔ انتخاب بخش | `16-onboarding-budgets` · `15-onboarding-risk` · `14-hub` · `10-settings` |
| ۶ — تم تیره و انتخاب کاربر | `22-dark-envelopes` · `23-dark-stream` · `24-dark-dong` · `25-theme-picker` |
| دنگ و دونگ (مرجع کامل) | `11-dong-list` · `12-dong-group` · `18-dong-new-group` · `19-dong-expense` · `20-dong-expenses` · `21-dong-payment` |
| صفحه‌های بدون تغییر ساختاری (مرجع) | `04-transactions` · `06-goals` · `07-accounts` · `08-import` · `09-income` · `13-login` |

---

## دو چیز که قبل از کد باید تصمیم بگیری

۱. **نام تب:** «جریان» یا «دستیار»؟ (در طراحی «جریان» است)
۲. **`TOTAL_STEPS` از ۷ به ۸ می‌رود** چون گام سقف پاکت‌ها اضافه می‌شود. اگر نمی‌خواهی اونبوردینگ بلندتر شود، این گام را از اونبوردینگ بیرون بگذار و به اولین ورودِ بورد منتقل کن.
۳. **پیش‌فرض تم:** «مثل سیستم» گذاشته‌ام. اگر می‌خواهی همه اول‌بار روشن ببینند، بگو تا پیش‌فرض `light` شود.

چهار سؤال باز دیگر آخر `BRIEF-design-complete.md` بخش ۱۲ است.
