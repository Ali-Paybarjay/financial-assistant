# مجموعه‌ی اسکیل‌های پروژه‌ی «دستیار مالی»

### درسنامه‌ی دوره‌ی فارغ‌التحصیلی مشاور هوش مصنوعی
**واحد: طراحی محیط دانشی عامل — مطالعه‌ی موردی یک پروژه‌ی کامل**

---

## فهرست

۱. صورت مسئله — پروژه‌ای که می‌سازیم
۲. چرا انتخاب اسکیل یک تصمیم مشاوره‌ای است
۳. سه منبع اسکیل و تفاوتشان
۴. جدول کامل اسکیل‌های این پروژه
۵. توضیح تفصیلی — گروه یک: بنیان فنی
۶. توضیح تفصیلی — گروه دو: کیفیت و طراحی
۷. توضیح تفصیلی — گروه سه: هوش مصنوعی
۸. توضیح تفصیلی — گروه چهار: انتشار و بازبینی
۹. اسکیل‌هایی که **نباید** نصب کرد — و چرا
۱۰. حفره‌ها: جایی که اسکیل وجود ندارد
۱۱. نگاشت Milestone به اسکیل
۱۲. دستورهای نصب
۱۳. جمع‌بندی درسی

---

## ۱. صورت مسئله — پروژه‌ای که می‌سازیم

پیش از هر بحثی درباره‌ی اسکیل، باید بدانیم چه می‌سازیم. این ترتیب اتفاقی نیست؛ در بخش ۲ خواهیم دید چرا شروع از سند پروژه به‌جای فهرست اسکیل‌ها، اولین اصل حرفه‌ای این کار است.

### محصول

یک وب‌اپلیکیشن مدیریت مالی شخصی برای فارسی‌زبانان مقیم خارج از ایران. کاربر ثبت‌نام می‌کند، یک onboarding هفت‌مرحله‌ای را طی می‌کند، و سپس هزینه‌هایش را به **چهار روش** ثبت می‌کند: فرم دستی، متن آزاد، ضبط صدا، و عکس فاکتور. سه روش آخر با مدل زبانی به داده‌ی ساختاریافته تبدیل می‌شوند.

### استک فنی

| لایه | انتخاب |
|---|---|
| چارچوب | Next.js 15 (App Router) + TypeScript strict |
| رابط کاربری | Tailwind CSS v4 + shadcn/ui با پیکربندی RTL |
| دیتابیس | Supabase Postgres با Row Level Security |
| احراز هویت | Supabase Auth (ایمیل + Google OAuth) |
| ذخیره‌سازی | Supabase Storage (باکت‌های خصوصی) |
| اعتبارسنجی | Zod در مرز هر ورودی |
| فرم | react-hook-form + zodResolver |
| داده‌ی سرور | TanStack Query |
| نمودار | Recharts |
| هوش مصنوعی | Anthropic Claude API (متن + تصویر) · Whisper (گفتار به متن) |
| تست | Vitest + Playwright |
| استقرار | Vercel |

### قیدهای غیرمعمول

سه قید این پروژه را از یک CRUD معمولی متمایز می‌کند و مستقیماً روی انتخاب اسکیل اثر می‌گذارد:

**قید اول — امنیت داده‌ی مالی.** RLS روی تمام جدول‌ها، هیچ کلید API در سمت کلاینت، و `SUPABASE_SERVICE_ROLE_KEY` فقط جایی که واقعاً لازم است.

**قید دوم — دقت پول.** هیچ مبلغی به‌صورت float ذخیره نمی‌شود. همه `bigint` بر حسب سِنت. یک ماژول `lib/money.ts` تنها مسیر مجاز تبدیل، جمع و فرمت است.

**قید سوم — بومی‌سازی راست‌به‌چپ.** رابط کاملاً فارسی و RTL، اما مبالغ با ارقام لاتین و ایزوله‌ی جهت (`dir="ltr"` و `unicode-bidi: isolate`) تا علامت ارز جابه‌جا نشود.

### ده Milestone

`M0` پایه · `M1` احراز هویت · `M2` Onboarding · `M3` درآمد و هزینه‌ی ثابت · `M4` تراکنش‌ها · `M5` داشبورد · `M6` ورودی متنی AI · `M7` ورودی صوتی · `M8` عکس فاکتور · `M9` تنظیمات و استقرار

---

## ۲. چرا انتخاب اسکیل یک تصمیم مشاوره‌ای است

### ۲.۱ اسکیل چیست

اسکیل یک بسته‌ی دانش تخصصی است که **به‌صورت شرطی** وارد حافظه‌ی فعال عامل می‌شود — یعنی فقط وقتی موضوعش پیش بیاید.

> پرامپت سیستمی مثل چیزی است که یک متخصص در حافظه‌ی فعالش نگه می‌دارد: همیشه حاضر، ولی محدود. اسکیل مثل قفسه‌ی مرجع پشت سر اوست: وقتی موضوعی پیش می‌آید، دست دراز می‌کند و جلد مربوطه را برمی‌دارد.
>
> **جایی که تشبیه می‌شکند:** متخصص انسانی از پیش می‌داند کدام کتاب را ندارد. عامل چنین چیزی نمی‌داند — او فقط توضیح یک‌خطی هر اسکیل را می‌بیند و بر همان اساس تصمیم به بارگذاری می‌گیرد.

### ۲.۲ سه دلیل وجودی

**اقتصاد context.** دانش کامل یک کتابخانه‌ی UI شاید ده‌ها هزار توکن باشد. اگر همیشه حاضر باشد، در هر درخواست — حتی درخواستی درباره‌ی دیتابیس — هزینه‌اش را می‌دهید.

**تازگی دانش.** دانش پایه‌ی مدل تا مقطع مشخصی از زمان است. چارچوب‌ها سریع‌تر حرکت می‌کنند. خطای این نوع **بی‌صدا** است: عامل با اطمینان کامل یک API منسوخ می‌نویسد، نه خطا می‌دهد نه تردید نشان می‌دهد. مثال زنده در همین پروژه را در بخش ۷ می‌بینید.

**تبدیل سلیقه به قاعده.** بدون اسکیل، عامل در هر جلسه ممکن است تصمیم متفاوتی بگیرد. در پروژه‌ای که ده Milestone و چند هفته طول می‌کشد، این یعنی کدی بدون انسجام درونی.

### ۲.۳ چرا «هرچه بیشتر بهتر» غلط است

پرتکرارترین خطای این واحد درسی: دانشجو بعد از آشنایی با اسکیل، سی‌تا نصب می‌کند. سه هزینه دارد:

**رقیق شدن توجه.** هر اسکیل بارگذاری‌شده بخشی از context را می‌گیرد و با فایل‌های واقعی پروژه رقابت می‌کند.

**تناقض.** دو اسکیل درباره‌ی یک موضوع، دو توصیه می‌دهند. عامل ممکن است در یک فایل الگوی A و در فایل بعدی الگوی B را به کار ببرد.

**اعتماد کاذب.** بدترین هزینه. یک اسکیل بی‌کیفیت که با اطمینان حرف می‌زند، از نبودِ اسکیل بدتر است، چون عامل را از رجوع به مستندات رسمی بازمی‌دارد.

> **قاعده‌ی طلایی:** برای هر نیاز، **یک** اسکیل، و ترجیحاً رسمی‌ترین. اگر دو اسکیل برای یک نیاز داشتید، یکی را حذف کنید — نه اینکه امیدوار باشید عامل خودش بهترین را انتخاب کند.

### ۲.۴ سنجش اعتبار — چهار سیگنال، نه یک عدد

پیش از توصیه‌ی هر اسکیل، این چهار سیگنال را با هم بخوانید:

| سیگنال | چه می‌گوید | دام |
|---|---|---|
| تعداد نصب | چند نفر امتحانش کرده‌اند | نصب ≠ رضایت |
| ستاره‌ی مخزن | اعتبار عمومی مخزن | ⚠️ بزرگ‌ترین دام — پایین را بخوانید |
| هویت نویسنده | آیا مرجع آن حوزه است | رسمی > مرجع شناخته‌شده > ناشناس |
| تازگی commit | آیا نگهداری می‌شود | مخزن شش‌ماه راکد، دانش کهنه دارد |

**دام ستاره.** فرض کنید اسکیلی در مخزنی با ۴۵ هزار ستاره می‌یابید. سیگنال قوی به نظر می‌رسد — اما مخزن یک **awesome-list** است: مجموعه‌ای گردآوری‌شده از صدها اسکیل با نویسندگان متفاوت. آن ستاره‌ها برای **کار گردآوری** داده شده‌اند، نه برای کیفیت آن یک فایلی که شما می‌خواهید.

در بخش ۵ دقیقاً با چنین موردی روبه‌رو می‌شویم و می‌بینید چطور با آن رفتار می‌کنیم.

**⚠️ درباره‌ی اعداد این سند:** تمام آمار نصب و ستاره مربوط به زمان نگارش است و تغییر می‌کند. آن‌ها را مثالِ روش بخوانید، نه توصیه‌ی ابدی.

---

## ۳. سه منبع اسکیل و تفاوتشان

اسکیل‌های این پروژه از سه جای متفاوت می‌آیند. تفکیک آن‌ها مهم است چون سطح اعتماد و نحوه‌ی نصبشان فرق دارد.

| منبع | نحوه‌ی دستیابی | سطح اعتماد |
|---|---|---|
| **الف. اسکیل‌های داخلی Claude Code** | همراه ابزار می‌آیند، نصب نمی‌خواهند | بالاترین — رسمی Anthropic |
| **ب. افزونه‌ی رسمی Vercel** | با `/plugin` از مارکت‌پلیس نصب می‌شود | بسیار بالا — رسمی خودِ محصول |
| **ج. رجیستری عمومی اسکیل** | `npx skills add <owner/repo@skill>` | **متغیر** — باید سنجیده شود |

**درس این تفکیک:** بار سنجش اعتبار تقریباً تماماً روی گروه ج است. برای گروه الف و ب، صرفاً باید تصمیم بگیرید کدامشان به این پروژه ربط دارد — که کار ساده‌تری است.

نکته‌ی مهم درباره‌ی گروه ب: افزونه‌ی Vercel ده‌ها اسکیل دارد. نصب افزونه یعنی همه‌ی آن‌ها در دسترس‌اند، ولی هرکدام فقط وقتی بارگذاری می‌شود که موضوعش پیش بیاید. پس نصب افزونه هزینه‌ی context ندارد؛ اما این دلیل نمی‌شود که ندانید کدام‌هایش برای شما بی‌ربط است. در بخش ۹ آن‌ها را نام می‌بریم.

---

## ۴. جدول کامل اسکیل‌های این پروژه

بیست اسکیل، به ترتیب اهمیت برای این پروژه‌ی خاص:

| # | اسکیل | منبع | Milestone | نقش در پروژه |
|---|---|---|---|---|
| ۱ | `vercel:nextjs` | ب | M0–M9 | ستون فقرات معماری |
| ۲ | `mattpocock/skills@codebase-design` | ج | M0, M4 | ساختار پوشه و مرز ماژول‌ها |
| ۳ | `planetscale/database-skills@postgres` | ج | M0, M3 | schema، ایندکس، قید یکتایی |
| ۴ | `vercel:vercel-functions` | ب | M3, M6–M8 | Route Handler، timeout، cron |
| ۵ | `claude-api` | الف | M6, M8 | شناسه‌ی مدل، vision، JSON، هزینه |
| ۶ | `vercel:shadcn` | ب | M0–M9 | کامپوننت و RTL |
| ۷ | `frontend-design` | الف | M0 | design token و «عنصر امضا» |
| ۸ | `addyosmani/web-quality-skills@accessibility` | ج | M2, M5, M9 | کف کیفیت دسترس‌پذیری |
| ۹ | `vercel:react-best-practices` | ب | M1–M9 | کیفیت کامپوننت و hook |
| ۱۰ | `web-design-guidelines` | الف | M9 | ممیزی نهایی رابط |
| ۱۱ | `dataviz` | الف | M5 | نمودار دونات و میله‌ای |
| ۱۲ | `sickn33/agentic-awesome-skills@nextjs-supabase-auth` | ج | M1 | تنها گزینه‌ی Supabase Auth |
| ۱۳ | `vercel:env-vars` | ب | M0, M9 | مرز کلیدهای محرمانه |
| ۱۴ | `vercel:deployments-cicd` | ب | M9 | استقرار و rollback |
| ۱۵ | `/security-review` | الف | M1, M6, M9 | ممیزی امنیتی |
| ۱۶ | `/code-review` | الف | انتهای هر M | بازبینی منطقی diff |
| ۱۷ | `vercel:vercel-firewall` | ب | M6 | rate limit سطح پلتفرم |
| ۱۸ | `ui-ux-pro-max` | ج | M2, M5 | الگوهای onboarding و داشبورد |
| ۱۹ | `vercel:verification` | ب | M9 | تأیید جریان انتها-به-انتها |
| ۲۰ | `/simplify` | الف | انتهای هر M | حذف پیچیدگی انباشته |

از این بیست مورد، **فقط چهار مورد** از رجیستری عمومی نصب می‌شوند (شماره‌های ۲، ۳، ۸، ۱۲). بقیه یا داخلی‌اند یا با یک افزونه‌ی رسمی می‌آیند.

این نسبت، خودش یک درس است: **اگر فهرست پیشنهادی شما پانزده نصب از رجیستری عمومی دارد، احتمالاً مرحله‌ی ممیزی پوشش موجود را انجام نداده‌اید.**

---

## ۵. گروه یک: بنیان فنی

### ۱ — `vercel:nextjs`

**منبع:** افزونه‌ی رسمی Vercel · **Milestone:** M0 تا M9

**چه می‌کند:** راهنمای رسمی App Router — روتینگ، Server Component، Server Action، میان‌افزار، لایه‌ها، استراتژی‌های رندر.

**کاربرد در این پروژه:**

**۱. مرز Server/Client — امنیتی‌ترین تصمیم پروژه.** سند می‌گوید «هیچ API key ای به client نمی‌رود». مرز فنی این قاعده، تشخیص درست Server Component از Client Component است. اگر کامپوننتی که کلید Anthropic از آن عبور می‌کند اشتباهاً `"use client"` بگیرد، کلید در باندل مرورگر می‌نشیند و هر بازدیدکننده‌ای می‌تواند ببیندش.

**۲. Server Action یا Route Handler؟** سند هر دو را خواسته. قاعده‌ی عملی: mutation فرم‌ها (ثبت تراکنش دستی، ویرایش پروفایل) با Server Action، اما `/api/parse/text` که timeout سی‌ثانیه‌ای و پاسخ ساختاریافته دارد با Route Handler.

**۳. میان‌افزار محافظت مسیر.** بند ۷.۱ سند: محافظت از تمام مسیرهای زیر `(app)`. این اسکیل الگوی `matcher` را می‌دهد و — مهم‌تر — توضیح می‌دهد چرا نباید authorization سنگین در میان‌افزار انجام شود.

**۴. Route Group.** ساختار `(auth)` و `(app)` که سند به‌طور ضمنی خواسته.

**چرا رسمی مهم است:** نسخه‌های اخیر Next.js تغییرات breaking دارند — مثل async شدن `params` و `searchParams`. یک اسکیل community قدیمی این را اشتباه می‌گوید و شما ساعت‌ها دنبال باگی می‌گردید که وجود ندارد.

---

### ۲ — `mattpocock/skills@codebase-design`

**منبع:** رجیستری · **نصب:** `npx skills add mattpocock/skills@codebase-design -g -y`
**اعتبار:** ۴۱۲ هزار نصب · مخزن ۲۲۰ هزار ستاره · نویسنده Matt Pocock، از شناخته‌شده‌ترین مراجع TypeScript
**Milestone:** M0 (ساختار اولیه) و M4 (ماژول پول)

**چه می‌کند:** طراحی ساختار پوشه، مرز ماژول‌ها، و الگوی deep module — ماژولی با رابط باریک و درون قدرتمند.

**کاربرد در این پروژه:**

سند یک قاعده‌ی معماری دارد که در عمل راحت شکسته می‌شود:

> «یک ماژول `lib/money.ts` بساز که **تنها مسیر مجاز** تبدیل، جمع، و فرمت‌کردن پول باشد.»

مشکل این است که سه هفته بعد، در یک کامپوننت جایی نوشته می‌شود `amount / 100` و قاعده مرده است — بی‌سروصدا، بدون خطا، بدون هشدار.

این اسکیل کمک می‌کند ساختار پوشه و مرزهای import را طوری بچینیم که این نقض **از نظر ساختاری سخت** شود، نه اینکه فقط در سند نوشته شده باشد.

همین منطق برای دو مرز دیگر هم به کار می‌آید:

- **`SpeechToTextProvider`** — سند می‌گوید «تعویض provider فقط یک فایل را عوض کند». این یعنی interface باید واقعاً بسته باشد و جزئیات Whisper نباید بیرون بزند.
- **مرز server/client** — که همان‌طور که دیدیم، مستقیماً امنیتی است.

**چرا این و نه یک اسکیل معماری دیگر:** تخصص نویسنده دقیقاً TypeScript در مقیاس بزرگ است، و سند `strict` mode و «هیچ `any` بدون کامنت توجیهی» خواسته.

---

### ۳ — `planetscale/database-skills@postgres`

**منبع:** رجیستری · **نصب:** `npx skills add planetscale/database-skills@postgres -g -y`
**اعتبار:** ۶.۸ هزار نصب · ۶۳۲ ستاره · مخزن رسمی PlanetScale (شرکت دیتابیس)
**Milestone:** M0 (migration ها) و M3 (job تکرارشونده)

**چه می‌کند:** طراحی schema، ایندکس‌گذاری، قیدها، و الگوهای کوئری Postgres.

**کاربرد در این پروژه — سه مورد مشخص:**

**۱. تله‌ی `bigint`.** تصمیم سند برای ذخیره‌ی پول به‌صورت `bigint` درست است، ولی یک پیامد دارد که معمولاً دیر کشف می‌شود: `bigint` وقتی از دیتابیس به JavaScript برمی‌گردد، ممکن است به `string` تبدیل شود، نه `number` — چون `Number.MAX_SAFE_INTEGER` کوچک‌تر از بازه‌ی `bigint` است و کتابخانه‌ها برای جلوگیری از خطای دقت، رشته برمی‌گردانند.

اگر این را در M0 ندانید، در M5 که جمع می‌زنید، `"4550" + "1200"` می‌شود `"45501200"`. کد خطا نمی‌دهد. تست‌ها هم اگر با داده‌ی کوچک نوشته شده باشند ممکن است پاس شوند. این اسکیل الگوهای type mapping را پوشش می‌دهد.

**۲. ترتیب ستون در ایندکس ترکیبی.** سند سه ایندکس خواسته:

```sql
transactions(user_id, occurred_on desc)
transactions(user_id, category_id)
media_assets(user_id, status)
```

نکته‌ی آموزشی: ترتیب ستون‌ها **اهمیت دارد**. ایندکس `(user_id, occurred_on desc)` برای کوئری «تراکنش‌های این کاربر در این ماه، جدید به قدیم» عالی است — چون کوئری از چپ ایندکس شروع می‌کند. ولی برای کوئری «همه‌ی تراکنش‌های این ماه از همه‌ی کاربران» تقریباً بی‌فایده است، چون ستون اول فیلتر نمی‌شود.

این اسکیل کمک می‌کند کوئری‌های داشبورد را با ایندکس‌ها همراستا نگه داریم و با `EXPLAIN` راستی‌آزمایی کنیم.

**۳. Idempotency با قید دیتابیس.** حیاتی‌ترین بند M3:

> «یک unique constraint روی ترکیب `(recurring_expense_id, month)` بگذار.»

چرا این تنها راه درست است؟ چون قفل نرم‌افزاری کافی نیست: دو درخواست همزمان می‌توانند **هر دو** چک «آیا رکورد وجود دارد؟» را پاس کنند و بعد هر دو درج کنند. تنها چیزی که این را می‌گیرد، قیدی است که خود دیتابیس تضمینش می‌کند.

این اسکیل الگوی `ON CONFLICT DO NOTHING` و partial unique index (برای همزیستی با soft delete) را می‌دهد.

**⚠️ محدودیت صادقانه:** این اسکیل لحن PlanetScale دارد. بخش‌های عمومی Postgres آن معتبر است، ولی هرجا از ویژگی‌های اختصاصی PlanetScale حرف زد، نادیده بگیرید. برای RLS چیزی ندارد — آن حفره سر جایش می‌ماند (بخش ۱۰).

---

### ۴ — `vercel:vercel-functions`

**منبع:** افزونه‌ی رسمی Vercel · **Milestone:** M3، M6 تا M8

**چه می‌کند:** پیکربندی و بهینه‌سازی کد سمت سرور روی Vercel — Route Handler، timeout، streaming، Cron Job، مدل اجرا.

**کاربرد در این پروژه:**

**۱. عدد سی ثانیه.** سند نوشته «timeout هر فراخوانی AI سی ثانیه». این عدد فقط وقتی معنا دارد که بدانید سقف پلتفرم چقدر است و چطور per-route تنظیمش کنید.

بدون این دانش، در M8 که عکس فاکتور را به مدل vision می‌فرستید و پردازش چهل ثانیه طول می‌کشد، function در production کشته می‌شود ولی در محیط local کار می‌کند — کلاسیک‌ترین باگ «روی سیستم من که کار می‌کرد».

**۲. Cron Job.** گزینه‌ی اول سند برای job تراکنش‌های تکرارشونده در M3. این اسکیل نحوه‌ی تعریف زمان‌بندی و — مهم‌تر — محدودیت‌های آن را می‌دهد.

**۳. مدل اجرا و cold start.** سند یک «جمله‌ی محک» دارد: «کاربر باید بتواند در کمتر از ده ثانیه یک خرید را ثبت کند». اگر اولین درخواست هر بار چند ثانیه cold start بخورد، این عدد شکسته می‌شود. این اسکیل توضیح می‌دهد مدل اجرای امروز Vercel چطور این را کاهش می‌دهد.

**۴. Streaming.** اگر خواستید کارت تأیید را تدریجی نمایش دهید تا انتظار کوتاه‌تر حس شود.

---

### ۱۲ — `sickn33/agentic-awesome-skills@nextjs-supabase-auth`

**منبع:** رجیستری · **نصب:** `npx skills add sickn33/agentic-awesome-skills@nextjs-supabase-auth -g -y`
**اعتبار:** ۶.۲ هزار نصب · ⚠️ مخزن ۴۵ هزار ستاره دارد **اما یک awesome-list است**
**Milestone:** M1

این مورد را عمداً اینجا آورده‌ام، چون بهترین نمونه‌ی آموزشی «دام ستاره» است که در بخش ۲.۴ معرفی شد.

**وضعیت واقعی جست‌وجو:** جست‌وجوی تک‌کلمه‌ای `supabase` در رجیستری **صفر نتیجه** داد. با عبارت ترکیبی، بهترین نتایج اختصاصی Supabase زیر دویست نصب بودند. این اسکیل نزدیک‌ترین گزینه‌ی موجود است.

**قضاوت حرفه‌ای:**

آن ۴۵ هزار ستاره برای کار گردآوری مخزن است، نه برای کیفیت این یک فایل. ممکن است این اسکیل خاص را کسی جز نویسنده‌اش نخوانده باشد. پس چرا پیشنهادش می‌کنم؟

چون **جایگزینی وجود ندارد** و نیاز واقعی است. تصمیم درست در چنین حالتی، «نصب نکن» نیست؛ «نصب کن ولی به آن به چشم مرجع نهایی نگاه نکن» است.

**قاعده‌ی استفاده:** هرچه این اسکیل گفت، در برابر مستندات رسمی Supabase راستی‌آزمایی می‌شود. اگر در M1 معلوم شد بی‌کیفیت است، حذفش می‌کنیم.

**چه کمکی ممکن است بکند:** الگوی `@supabase/ssr` — یعنی سه client متفاوت (مرورگر، Server Component، میان‌افزار) و اینکه چرا نمی‌شود یکی را همه‌جا به کار برد. این پرتکرارترین منبع باگ در ترکیب Supabase Auth با App Router است: session در میان‌افزار refresh می‌شود ولی cookie درست ست نمی‌شود و کاربر به‌طور تصادفی logout می‌شود.

---

## ۶. گروه دو: کیفیت و طراحی

### ۶ — `vercel:shadcn`

**منبع:** افزونه‌ی رسمی Vercel · **Milestone:** M0 تا M9

**چه می‌کند:** نصب و ترکیب کامپوننت‌های shadcn/ui، theming، و یکپارچگی با Tailwind.

**کاربرد در این پروژه:**

**۱. Tailwind v4.** نسخه‌ی چهارم پیکربندی‌اش با نسخه‌ی سوم تفاوت بنیادی دارد (رویکرد CSS-first به‌جای فایل پیکربندی جاوااسکریپتی). ترکیبش با shadcn نکات خاص خودش را دارد و اطلاعات قدیمی اینجا فعالانه ضرر می‌زند.

**۲. RTL — پرتکرارترین منبع باگ این پروژه.** کامپوننت‌های shadcn روی Radix سوارند و بعضی primitive ها — Dropdown، Popover، Select، Sheet — در حالت راست‌به‌چپ باید صریحاً `dir` بگیرند، وگرنه در جهت اشتباه باز می‌شوند.

توجه کنید که این دقیقاً همان کامپوننت‌هایی است که این پروژه پرکاربردترینشان را دارد: مودال چهار-تبه‌ی ثبت تراکنش، منوی ویرایش inline در لیست تراکنش‌ها، و انتخابگر ماه در داشبورد.

**۳. Theming با design token.** سند خواسته اول یک token system بنویسیم. shadcn توکن‌هایش را با CSS variable می‌دهد؛ این اسکیل نشان می‌دهد چطور پالت خودمان را جای پیش‌فرض بگذاریم — تفاوت بین اپی که هویت دارد و اپی که «یک اپ shadcn دیگر» به نظر می‌رسد.

---

### ۷ — `frontend-design`

**منبع:** داخلی Claude Code (از `anthropics/skills`) · **Milestone:** M0

**چه می‌کند:** جهت‌گیری زیبایی‌شناختی، تایپوگرافی، و انتخاب‌هایی که «قالبی» به نظر نرسند.

**کاربرد در این پروژه:**

بند ۸ سند، سخت‌گیرانه‌ترین بخش کل پروژه است:

> «سپس آن را نقد کن: اگر هر بخشی از آن همان چیزی است که برای هر اپ مالی دیگری هم تولید می‌کردی، عوضش کن و بگو چرا.»
>
> «از این سه لوک پرهیز کن چون امضای طراحی AI هستند و نشان می‌دهند انتخابی صورت نگرفته: پس‌زمینه‌ی کرم گرم با سریف پرکنتراست و لهجه‌ی خاکی-نارنجی؛ پس‌زمینه‌ی تقریباً مشکی با یک سبز اسیدی؛ لی‌اوت روزنامه‌ای با خط‌های مویی و صفر گردی گوشه.»

این بند دقیقاً برای همین اسکیل نوشته شده است. بدون آن، عامل به‌طور پیش‌فرض همان چیزی را تولید می‌کند که سند صریحاً منع کرده — چون آن سه لوک، نقاط تعادل آماری خروجی مدل‌اند.

با این اسکیل، برای «عنصر امضا» — آن یک چیزی که اپ با آن به یاد آورده می‌شود — انتخاب‌های مستدل می‌آید و می‌توان دلیل هر انتخاب را نوشت.

---

### ۸ — `addyosmani/web-quality-skills@accessibility`

**منبع:** رجیستری · **نصب:** `npx skills add addyosmani/web-quality-skills@accessibility -g -y`
**اعتبار:** ۴۵ هزار نصب · ۲.۶ هزار ستاره · نویسنده Addy Osmani از تیم Chrome گوگل
**Milestone:** M2، M5، M9 — ولی عملاً از M1

**چه می‌کند:** قواعد دسترس‌پذیری وب — کنتراست، ناوبری با کیبورد، ARIA، حرکت.

**کاربرد در این پروژه:**

سند بندی دارد به نام «کف کیفیت **بدون اعلام**»:

> «ریسپانسیو تا موبایل، focus قابل‌مشاهده با کیبورد، احترام به `prefers-reduced-motion`، کنتراست AA.»

عبارت «بدون اعلام» یعنی نمی‌خواهید هر بار یادآوری کنید. اسکیل دقیقاً همین را حل می‌کند: این قواعد در لحظه‌ی نوشتن UI خودکار وارد می‌شوند.

**چهار مورد مشخص در این پروژه:**

**۱. کارت‌های داشبورد فقط با رنگ معنا می‌دهند.** سند نوشته کارت «مانده» سبز اگر مثبت، قرمز اگر منفی. برای کاربر کوررنگ — که در مردان حدود یک نفر از هر دوازده نفر است — این یعنی **هیچ اطلاعاتی**. راه‌حل: علامت ‎+‎ و ‎−‎ یا آیکون جهت، در کنار رنگ نه به‌جای آن.

**۲. مودال ثبت تراکنش.** نیاز به focus trap دارد، و پس از بستن باید focus به دکمه‌ی شناوری که بازش کرده برگردد. بدون این، کاربر کیبوردی گم می‌شود.

**۳. ضبط صدا در M7.** یک تعامل کاملاً بصری است (نمایش موج صدا و تایمر). باید معادل متنی و قابل‌اعلام برای صفحه‌خوان داشته باشد.

**۴. نمودارها.** بدون متن جایگزین، برای صفحه‌خوان یک تصویر بی‌معنی‌اند. حداقل باید خلاصه‌ی عددی همان نمودار در دسترس باشد.

---

### ۹ — `vercel:react-best-practices`

**منبع:** افزونه‌ی رسمی Vercel · **Milestone:** M1 به بعد

**چه می‌کند:** پس از ویرایش چند کامپوننت، یک چک‌لیست کیفیت اجرا می‌کند: ساختار کامپوننت، استفاده‌ی درست از hook، دسترس‌پذیری، performance، الگوهای TypeScript.

**کاربرد در این پروژه:** در M4 و M5 با TanStack Query سروکار داریم. رایج‌ترین اشتباه، fetch در `useEffect` به‌جای query، و کلیدهای بی‌ثبات cache است.

پیامد ملموس این اشتباه در این پروژه: کاربر هزینه‌ای ثبت می‌کند، به داشبورد برمی‌گردد، و عدد عوض نشده — چون cache باطل نشده. کاربر فکر می‌کند ثبت نشده و دوباره ثبت می‌کند. حالا داده‌ی مالی‌اش غلط است.

---

### ۱۰ — `web-design-guidelines`

**منبع:** داخلی · **Milestone:** M9، و بعد از هر M که رابط تحویل داده

**چه می‌کند:** ممیزی کد UI در برابر Web Interface Guidelines.

**کاربرد در این پروژه:** جزئیاتی که در هیچ سندی نوشته نمی‌شوند ولی تفاوت آماتور و حرفه‌ای‌اند — اندازه‌ی هدف لمسی، حالت‌های hover و active و disabled، رفتار دکمه هنگام ارسال فرم (که اگر دو بار کلیک شود چه می‌شود)، و اینکه آیا ورودی مبلغ روی موبایل کیبورد عددی می‌آورد یا نه.

آن آخری دقیقاً بند ۷.۴ سند است: «کیبورد عددی روی موبایل». پیاده‌سازی درستش `inputmode="decimal"` است، نه `type="number"` — که رفتارهای ناخواسته‌ی دیگری دارد.

---

### ۱۱ — `dataviz`

**منبع:** داخلی · **Milestone:** M5

**چه می‌کند:** روش طراحی نمودار — انتخاب فرم، پالت رنگ قابل‌دسترس، مشخصات علامت‌گذاری، و قواعد تعامل. مستقل از کتابخانه، پس با Recharts سازگار است.

**کاربرد در این پروژه:**

سند یک قید سخت دارد: **«نمودارها نباید رنگین‌کمان باشند.»**

این اسکیل دقیقاً یک روش رنگ‌بندی می‌دهد که هم قابل‌دسترس است هم رنگین‌کمان نیست، به‌علاوه‌ی یک validator برای بررسی کنتراست پالت.

چهار کاربرد مشخص در داشبورد:

- **دونات با «۵ دسته‌ی برتر + سایر»** — الگوی درست تجمیع، و اینکه دسته‌ی «سایر» باید بصری خنثی باشد نه یک رنگ پررنگ.
- **میله‌ای دو-سری درآمد/هزینه در ۶ ماه** — باید در حالت سیاه‌وسفید هم قابل تفکیک باشد.
- **حالت خالی نمودار** — سند صریحاً خواسته «به‌جای نمودار خالی، یک راهنمای کوتاه با دکمه‌ی اولین هزینه‌ات را ثبت کن».
- **چهار کارت KPI با درصد تغییر ماه قبل** — الگوی نمایش تغییر، و اینکه وقتی ماه قبل داده ندارد چه نشان دهیم.

---

### ۱۸ — `ui-ux-pro-max`

**منبع:** رجیستری · **Milestone:** M2 و M5

**چه می‌کند:** کتابخانه‌ی الگو — سبک بصری، پالت، جفت‌فونت، و الگوهای UX برای انواع محصول از جمله داشبورد و فرم چندمرحله‌ای.

**کاربرد در این پروژه:** onboarding هفت‌مرحله‌ای. قاعده‌ای که سند نوشته — «یک سؤال یا یک گروه سؤال مرتبط در هر صفحه، هرگز پانزده فیلد در یک فرم بلند» — یک الگوی شناخته‌شده است و این اسکیل جزئیاتش را دارد: نوار پیشرفت، دکمه‌ی بازگشت، ذخیره‌ی خودکار پس از هر گام، و رفتار دکمه‌ی «فعلاً رد کن» در گام‌های ۴ تا ۷.

**⚠️ هشدار همپوشانی — یک درس عملی:** این اسکیل با `frontend-design` هم‌پوشانی دارد. طبق قاعده‌ی طلایی بخش ۲.۳، باید یکی را حذف کنیم — اما اینجا یک استثنای موجه هست، به شرط تقسیم کار روشن:

- `frontend-design` برای **جهت‌گیری و هویت بصری** (بند ۸ سند)
- `ui-ux-pro-max` برای **الگوهای تعاملی مشخص** (ساختار onboarding)

شرط: هرگز هر دو را همزمان بارگذاری نکنید. اگر دیدید توصیه‌های متناقض می‌آید، `ui-ux-pro-max` را حذف کنید — چون هویت بصری، تصمیم سخت‌تری است و اسکیل رسمی‌تر باید آن را ببرد.

---

## ۷. گروه سه: هوش مصنوعی

### ۵ — `claude-api`

**منبع:** داخلی Claude Code (رسمی Anthropic) · **Milestone:** M6 و M8

**چه می‌کند:** مرجع Claude API — شناسه‌ی مدل‌ها، قیمت‌گذاری، پارامترها، streaming، tool use، شمارش توکن، و prompt caching.

**کاربرد در این پروژه — پنج مورد:**

**۱. شناسه‌ی درست مدل — خطای بی‌صدایی که در بخش ۲.۲ وعده دادم.**

سند این پروژه صریحاً نوشته: مدل `claude-sonnet-4-6`.

نام مدل‌ها با نسخه‌های جدید عوض می‌شوند. یک شناسه‌ی منسوخ، خطای زمان‌اجرا می‌دهد — نه خطای کامپایل، نه هشدار در بازبینی کد. کد از `typecheck` و `lint` و `build` عبور می‌کند و در لحظه‌ی اجرا می‌شکند.

پس **اولین کار در M6** راستی‌آزمایی این شناسه با این اسکیل است، نه نوشتن کد.

> **درس عمومی که فراتر از این پروژه می‌رود:** هر جا سند پروژه یک **شناسه‌ی خارجی نسخه‌دار** ذکر کرده — نام مدل، نسخه‌ی API، نام endpoint — آن را پیش از نوشتن کدِ وابسته، در برابر مرجع رسمی راستی‌آزمایی کنید. این را در فهرست بندهای بحرانی پروژه ثبت کنید.

**۲. Structured output — و چرا روش سند بهینه نیست.**

سند یک schema دقیق Zod تعریف کرده و در نکات prompt نویسی گفته: «بگو خروجی فقط JSON باشد، بدون هیچ متن اضافه یا backtick.»

این کار می‌کند، ولی شکننده است — شما دارید از مدل **خواهش** می‌کنید. روش قابل‌اتکاتر، **tool use** است: مدل را در قالبی قرار می‌دهید که خروجی را به‌صورت فراخوانی ابزاری با schema مشخص بدهد. آنجا ساختار تضمین‌شده‌تر است، نه محصول اطاعت.

این اسکیل الگویش را دارد و کیفیت M6 و M8 را محسوس بالا می‌برد. سیاست retry سند («در صورت شکست یک بار retry شود») هم بار کمتری پیدا می‌کند.

**۳. ورودی تصویری در M8.** سند گفته «ارسال به Claude به‌صورت base64». این اسکیل فرمت دقیق بلوک تصویر، محدودیت اندازه، فرمت‌های پشتیبانی‌شده، و تعداد مجاز تصویر در هر درخواست را می‌دهد.

**۴. شمارش توکن و هزینه.** جدول `ai_usage_logs` سند ستون‌های `input_tokens`، `output_tokens` و `cost_cents` دارد. برای پر کردن درست `cost_cents` به قیمت هر مدل نیاز دارید — که در همین اسکیل است. بدون آن، این ستون یا خالی می‌ماند یا با عدد ساختگی پر می‌شود.

**۵. Prompt caching — بردی که سند به آن اشاره نکرده.**

در هر فراخوانی M6، لیست دسته‌های کاربر و ارز پایه‌اش در prompt تکرار می‌شود. این بخش بین فراخوانی‌های یک کاربر تقریباً ثابت است. اگر آن را cache کنیم، هزینه‌ی هر فراخوانی به‌طور محسوسی پایین می‌آید.

این نمونه‌ای است از جایی که اسکیل نه فقط جلوی خطا را می‌گیرد، بلکه فرصتی را نشان می‌دهد که در سند نبوده. **این دقیقاً همان ارزش افزوده‌ای است که از یک مشاور انتظار می‌رود.**

---

### ۱۷ — `vercel:vercel-firewall`

**منبع:** افزونه‌ی رسمی Vercel · **Milestone:** M6

**چه می‌کند:** امنیت سطح پلتفرم — rate limiting، قواعد سفارشی، مسدودسازی IP، مدیریت بات.

**کاربرد در این پروژه:**

سند نوشته «سقف پنجاه فراخوانی AI برای هر کاربر در هر روز». این را در سطح اپلیکیشن با شمارش در `ai_usage_logs` پیاده می‌کنیم — که درست است و کافی به نظر می‌رسد.

اما یک سناریو باقی می‌ماند: کسی که هنوز login نکرده و endpoint را مستقیم صدا می‌زند. rate limit سطح پلتفرم این را **پیش از رسیدن به کد شما** می‌گیرد — یعنی نه هزینه‌ی AI می‌دهید نه هزینه‌ی compute.

**چرا اولویت پایین‌تر است:** برای MVP، لایه‌ی اپلیکیشنی کافی است. این را می‌آورم تا بدانید موجود است و بتوانید به کارفرما توضیح دهید که چرا فعلاً لازم نیست — که خودش بخشی از کار مشاوره است.

---

## ۸. گروه چهار: انتشار و بازبینی

### ۱۳ — `vercel:env-vars`

**Milestone:** M0 (ساخت `.env.example`) و M9 (استقرار)

**کاربرد در این پروژه:**

قاعده‌ی حیاتی سند: «هیچ API key ای هرگز به client نمی‌رود». مرز فنی این قاعده در Next.js **یک چیز** است: پیشوند `NEXT_PUBLIC_`. هر متغیری با این پیشوند در باندل کلاینت قرار می‌گیرد.

فهرست متغیرهای سند را با این عینک بخوانید:

```
NEXT_PUBLIC_SUPABASE_URL      ← عمومی، درست است
NEXT_PUBLIC_SUPABASE_ANON_KEY ← عمومی، درست است — RLS محافظش است
SUPABASE_SERVICE_ROLE_KEY     ← اگر روزی NEXT_PUBLIC_ بگیرد، تمام RLS دور زده می‌شود
ANTHROPIC_API_KEY             ← هزینه‌ی مالی مستقیم
OPENAI_API_KEY                ← هزینه‌ی مالی مستقیم
```

توجه کنید که `ANON_KEY` عمومی بودنش **عمدی و درست** است — امنیتش از RLS می‌آید نه از مخفی بودن کلید. این تمایز را باید بفهمید، وگرنه یا بی‌جهت نگران می‌شوید یا جای اشتباهی خیالتان راحت است.

اسکیل به‌علاوه دستورات همگام‌سازی محیط local و production را می‌دهد.

---

### ۱۴ — `vercel:deployments-cicd`

**Milestone:** M9

**کاربرد:** preview deployment برای هر برنچ، promote به production، rollback، و خواندن build log وقتی بیلد در Vercel می‌شکند ولی در local پاس می‌شود — که معمولاً یا متغیر محیطی گم‌شده است یا خطای typecheck ای که پیکربندی local نادیده گرفته.

---

### ۱۵ — `/security-review`

**Milestone:** M1، **M6**، M9

**کاربرد:** بازبینی امنیتی تغییرات. سه بند «غیرقابل‌مذاکره»ی سند دقیقاً هدف این ابزارند: نشت کلید، سوءاستفاده از service role، و RLS ناقص.

**نکته‌ی طراحی که معمولاً فراموش می‌شود:** این را در M6 هم اجرا کنید، نه فقط M1 و M9. چرا؟ چون M6 اولین جایی است که **ورودی کنترل‌نشده‌ی کاربر مستقیماً وارد یک prompt می‌شود**. یعنی سطح حمله‌ی تزریق پرامپت باز می‌شود — چیزی که در M1 اصلاً وجود نداشت.

---

### ۱۶ — `/code-review`

**Milestone:** انتهای هر Milestone، پیش از گزارش

**کاربرد:** سند گفته انتهای هر Milestone باید `pnpm typecheck && pnpm lint && pnpm build` پاس شوند. اما اینها فقط خطاهای **نحوی و نوعی** را می‌گیرند.

`/code-review` خطاهای **منطقی** را می‌گیرد. دو مثال واقعی از همین پروژه:

- محاسبه‌ی «نرخ پس‌انداز» با فرمول `(درآمد − هزینه) / درآمد` وقتی درآمد صفر است، تقسیم بر صفر می‌شود. typecheck این را نمی‌بیند.
- فیلتر ماه اگر تراکنش‌های `deleted_at`دار را هم بشمارد، **تست پذیرش شماره ۵ سند** می‌شکند («مجموع کارت هزینه‌ی این ماه دقیقاً برابر جمع تراکنش‌های حذف‌نشده‌ی آن ماه است»).

**پیشنهاد عملی:** انتهای هر Milestone با سطح `high` اجرا کنید.

---

### ۱۹ — `vercel:verification`

**Milestone:** M9، و هر وقت چیزی «کار نمی‌کند»

**کاربرد:** تأیید جریان کامل — مرورگر ← API ← دیتابیس ← پاسخ.

برای تست‌های پذیرش سند ایده‌آل است. مثلاً تست شماره ۲ («کاربر می‌نویسد ۴۵ دلار خرید سوپرمارکت و ۱۲ دلار قهوه و دو تراکنش با دسته‌ی درست ظاهر می‌شود») یک زنجیره‌ی چندلایه است: فرم ← Route Handler ← Claude ← اعتبارسنجی Zod ← کارت تأیید ← ذخیره ← به‌روزرسانی cache. اگر بشکند، باید بدانید کدام حلقه.

---

### ۲۰ — `/simplify`

**Milestone:** انتهای هر Milestone، پس از `/code-review`

**کاربرد:** فقط کیفیت، نه باگ — کد تکراری، انتزاع بی‌مورد، و اینکه آیا منطق در لایه‌ی درست نشسته است.

**چرا در این پروژه مهم‌تر از حد معمول است:** پروژه ده Milestone دارد. بدون این پاکسازی دوره‌ای، منطق پول کم‌کم در کامپوننت‌ها پخش می‌شود و قاعده‌ی `money.ts` — که در بخش ۵ دیدیم چقدر بحرانی است — عملاً می‌میرد.

---

## ۹. اسکیل‌هایی که **نباید** نصب کرد

این بخش به‌اندازه‌ی بخش‌های قبل آموزنده است. توانایی توجیه یک «نه» نشانه‌ی مشاور است؛ فهرست‌کردن هرچه پیدا شد، نشانه‌ی مبتدی.

### ۹.۱ پرنصب اما برای استکِ دیگر — خطرناک‌ترین دسته

| اسکیل | نصب | چرا نه |
|---|---|---|
| `clerk/skills@clerk-nextjs-patterns` | ۳۳.۶ هزار | برای Clerk است. این پروژه Supabase Auth دارد |
| `auth0/agent-skills@auth0-nextjs` | ۱.۶ هزار | برای Auth0 است |
| `prisma/prisma-next@prisma-next-supabase` | ۳۸۸ | نام Supabase دارد ولی برای Prisma است. این پروژه ORM ندارد |
| `antvis/chart-visualization-skills@chart-visualization` | ۵.۷ هزار | معتبر و رسمی، ولی برای کتابخانه‌ی نمودار خودشان. این پروژه Recharts دارد |

**چرا این دسته خطرناک‌ترین است:** چون این اسکیل‌ها **باکیفیت‌اند**. اگر صرفاً بر اساس تعداد نصب توصیه کنید، عامل را با الگوهای یک سرویس کاملاً متفاوت تغذیه کرده‌اید و او با اطمینان کد اشتباه می‌نویسد.

اسکیل Clerk با ۳۳.۶ هزار نصب در جست‌وجوی `nextjs` رتبه‌ی اول را داشت. یک دانشجوی بی‌دقت آن را در فهرست می‌گذارد.

**درس:** پیش از هر توصیه، انتخاب‌های استک را در یک ستون بنویسید و هر نامزد را با آن تطبیق دهید.

---

### ۹.۲ همپوشانی با گزینه‌ی رسمی‌تر

| اسکیل | نصب | چرا نه |
|---|---|---|
| `wshobson/agents@nextjs-app-router-patterns` | ۲۷.۴ هزار | همپوشانی کامل با `vercel:nextjs` که رسمی‌تر و به‌روزتر است |
| `secondsky/claude-skills@tailwind-v4-shadcn` | ۷.۲ هزار | همپوشانی با `vercel:shadcn`. ضمناً نصب بالا با فقط ۲۰۷ ستاره — الگوی مشکوکی که در بخش ۲.۴ گفتیم |
| `mattpocock/skills@code-review` | ۳۴۸ هزار | معتبر، ولی `/code-review` داخلی همین کار را با یکپارچگی بهتر می‌کند |
| `obra/superpowers@requesting-code-review` | ۲۰۲ هزار | مخزن ۲۷۳ هزار ستاره دارد، ولی سنگین و پرحرف است و با ابزار داخلی همپوشانی دارد |

**درس:** بزرگ‌ترین عدد لزوماً برنده نیست. معیار انتخاب به ترتیب: رسمی‌بودن نسبت به محصول، سپس تازگی نگهداری، سپس تناسب با استک.

---

### ۹.۳ بازدهی پایین برای دامنه‌ی این MVP

| اسکیل | چرا نه |
|---|---|
| `alinaqi/maggy@playwright-testing` | سند فقط **یک** happy-path E2E خواسته. هزینه‌ی context بیشتر از فایده |
| `giuseppe-trisciuoglio/developer-kit@zod-validation-utilities` | Zod دانش پایه‌ی خوبی دارد و در مدل حاضر است |
| `mattpocock/skills@setup-ts-deep-modules` | مفید، ولی `codebase-design` از همان نویسنده این را پوشش می‌دهد |

---

### ۹.۴ اسکیل‌های افزونه‌ی Vercel که در این پروژه بی‌کاربردند

نصب افزونه یعنی همه‌ی این‌ها در دسترس‌اند، ولی خوب است بدانید کدام‌ها هرگز فعال نمی‌شوند:

| اسکیل | چرا نه |
|---|---|
| `vercel:ai-sdk` و `vercel:ai-gateway` | سند مستقیماً SDK رسمی Anthropic و OpenAI را خواسته. افزودن لایه‌ی انتزاع در MVP، پیچیدگی بی‌دلیل است.<br>**اما:** اگر روزی خواستید provider ها را راحت عوض کنید یا هزینه را متمرکز کنترل کنید، اینها گزینه‌ی درست‌اند |
| `vercel:vercel-storage` | Blob و Edge Config است. این پروژه Supabase Storage دارد |
| `vercel:next-cache-components` | PPR و کش. اپ تقریباً کاملاً پویا و پشت auth است — کش کردن نه‌تنها بی‌فایده، بلکه **ریسک نشت داده بین کاربران** دارد |
| `vercel:auth` | Clerk و Auth0 و Descope است |
| `vercel:chat-sdk` | «دستیار چت مالی» صراحتاً **خارج از دامنه**ی سند است |
| `vercel:turbopack` | فقط اگر بیلد کند شد یا شکست |

---

### ۹.۵ اسکیل‌های نامرتبط با فاز ساخت

`business-analytics-reporter` (تحلیل CSV فروش کسب‌وکار است، نه مالی شخصی — اسمش گمراه‌کننده است) · `rag-implementation` (RAG نداریم؛ استخراج ساختار از متن، RAG نیست) · `landing-page-design` · `content-marketer` · `growth-funnel` · `pricing-strategy` · `retention-strategy` · `startup-validator` · `generative-engine-optimization`

دسته‌ی آخر همه بازاریابی و استراتژی‌اند. **بعد از** MVP و برای عرضه‌ی محصول به‌کار می‌آیند، نه در فاز ساخت.

---

## ۱۰. حفره‌ها: جایی که اسکیل وجود ندارد

پس از جست‌وجوی کامل رجیستری، پنج نیاز بدون پاسخ ماندند.

**به این جدول با دقت نگاه کنید — این‌ها دقیقاً همان قیدهای بحرانی بخش ۱.۳ هستند:**

| # | حفره | وضعیت جست‌وجو | راه‌حل |
|---|---|---|---|
| ۱ | پالیسی‌های RLS در Supabase | `supabase` تک‌کلمه‌ای: **صفر نتیجه**. بهترین گزینه‌ی اختصاصی: ۱۷۳ نصب | **MCP رسمی Supabase** به‌جای اسکیل |
| ۲ | RTL فارسی + ایزوله‌ی جهت مبالغ | بهترین نتیجه: `i18n-rtl-audit` با **۳ نصب** | اسکیل محلی پروژه |
| ۳ | قاعده‌ی پول (bigint، واحد کوچک، ماژول واحد) | چیزی نبود | اسکیل محلی پروژه |
| ۴ | `MediaRecorder` و سازگاری iOS | `whisper` و `audio transcription`: صفر نتیجه | تست دستی روی دستگاه واقعی |
| ۵ | Recharts و TanStack Query | `tanstack query`: **صفر نتیجه** | مستندات رسمی + `dataviz` |

### درس مرکزی این پروژه

> **حساس‌ترین بخش‌های یک پروژه، معمولاً همان بخش‌هایی هستند که اسکیل ندارند.**
>
> دلیلش ساده است: اسکیل‌ها برای مسائل **عمومی** نوشته می‌شوند، و حساسیت معمولاً از **خاص‌بودن** می‌آید. قاعده‌ی «پول را چنین ذخیره کن» خاص این پروژه است. ظرافت راست‌به‌چپ با اعداد لاتین، خاص این زبان و این نوع داده است.
>
> **پیامد حرفه‌ای:** بخش عمده‌ی ارزشی که شما به‌عنوان مشاور اضافه می‌کنید، در همین حفره‌هاست. نصب اسکیل کاری است که هر کسی می‌تواند بکند. تشخیص اینکه کجا اسکیل وجود ندارد و باید چیز دیگری ساخت — این تخصص شماست.

### ۱۰.۱ چرا Supabase در رجیستری غایب است

این ضدشهودی است: سرویسی با میلیون‌ها کاربر، در اکوسیستم اسکیل تقریباً غایب — در حالی که سرویس‌های احراز هویت رقیب، اسکیل رسمی با ده‌ها هزار نصب دارند.

**دلیل:** اکوسیستم اسکیل جوان و نامتوازن است. شرکت‌هایی که زودتر روی این کانال سرمایه‌گذاری کرده‌اند حضور پررنگی دارند؛ بقیه غایب‌اند.

> **حضور در اکوسیستم اسکیل، تابع استراتژی بازاریابی توسعه‌دهنده است، نه محبوبیت یا کیفیت محصول.**

این را به کارفرما هم منتقل کنید. نبود اسکیل برای یک ابزار، نشانه‌ی نامرغوب بودن آن ابزار نیست.

### ۱۰.۲ چرا برای حفره‌ی ۱، ابزار بهتر از اسکیل است

تست پذیرش شماره ۶ سند: «کاربر دوم هیچ داده‌ای از کاربر اول نمی‌بیند — نه در UI، نه با فراخوانی مستقیم API.»

یک اسکیل می‌تواند به عامل **یاد بدهد** پالیسی RLS چطور نوشته می‌شود. اما نمی‌تواند **اثبات کند** که پالیسی کار می‌کند. برای این تست، عامل باید بتواند واقعاً دو کاربر بسازد و کوئری بزند.

> **درس عمومی:** وقتی نیاز، «دانستن» است، اسکیل بدهید. وقتی نیاز، «انجام دادن و دیدن نتیجه» است، ابزار بدهید. اشتباه گرفتن این دو، منجر به عاملی می‌شود که با اطمینان ادعا می‌کند کاری را انجام داده که هرگز راستی‌آزمایی نشده.

### ۱۰.۳ اسکیل محلی برای حفره‌های ۲ و ۳

حفره‌های ۲ و ۳ عمداً به رجیستری سپرده نمی‌شوند، حتی اگر روزی اسکیلی برایشان پیدا شود. این‌ها **قواعد این پروژه‌اند، نه دانش عمومی**. جایشان `.claude/skills/` داخل خود مخزن است.

سه مزیت:

**۱. با پروژه commit می‌شود.** نفر بعدی که وارد پروژه شود — انسان یا عامل — همان قواعد را می‌گیرد.

**۲. در code review قابل بحث است.** قاعده‌ی معماری‌ای که در فایل نوشته شده، می‌تواند نقد و اصلاح شود. قاعده‌ای که فقط در ذهن مشاور است، نمی‌تواند.

**۳. دقیقاً به اندازه‌ی نیاز است.** اسکیل عمومی مجبور است همه‌ی حالت‌ها را پوشش دهد. اسکیل محلی فقط تصمیم شما را می‌نویسد.

**سه اصل نوشتنش:**

**اصل اول — توضیح مهم‌تر از محتواست.** عامل تصمیم بارگذاری را فقط بر اساس توضیح یک‌خطی می‌گیرد. توضیح باید **محرک‌ها** را نام ببرد: چه کلماتی، چه فایل‌هایی، چه کارهایی باید این اسکیل را فعال کنند.

**اصل دوم — قاعده بنویسید، نه آموزش.** به‌جای «پول در نرم‌افزارهای مالی باید با دقت مدیریت شود»، بنویسید: «مبالغ همیشه `bigint` بر حسب سِنت‌اند؛ تبدیل فقط از `lib/money.ts`؛ نمایش فقط از کامپوننت `<Money />`.»

**اصل سوم — دلیل را هم بنویسید.** قاعده‌ی بدون دلیل، در اولین موقعیت خاص شکسته می‌شود. قاعده‌ی با دلیل، درست تعمیم می‌یابد. یک جمله کافی است.

### ۱۰.۴ سه لایه‌ی تضمین — یک چارچوب عمومی

اسکیل محلی، لایه‌ی میانی است. کل تصویر:

| لایه | مکانیزم | قدرت |
|---|---|---|
| سند | نوشتن قاعده در `BRIEF.md` | ضعیف — به یادآوری وابسته است |
| اسکیل | یادآوری خودکار در لحظه‌ی مرتبط | متوسط — احتمال رعایت را بالا می‌برد |
| **ساختار** | قاعده‌ی lint، مرز import، قید دیتابیس | **قوی — نقض را ناممکن یا آشکار می‌کند** |

دو مثال از همین پروژه:

**قاعده‌ی پول.** لایه ۱: در سند نوشته شده. لایه ۲: اسکیل محلی یادآوری می‌کند. **لایه ۳:** یک قاعده‌ی lint که هر تقسیم بر ۱۰۰ خارج از `lib/money.ts` را خطا اعلام کند.

**Idempotency.** لایه ۲: به عامل یادآوری کنیم پیش از درج، وجود رکورد را چک کند. **لایه ۳:** قید یکتایی روی `(recurring_expense_id, month)` — که دو درخواست همزمان را هم می‌گیرد، در حالی که چک نرم‌افزاری نمی‌گیرد.

> **قاعده‌ی حرفه‌ای:** برای هر قید بحرانی بپرسید «آیا می‌توانم این را از لایه ۲ به لایه ۳ منتقل کنم؟» اسکیل احتمال رعایت را بالا می‌برد؛ ساختار آن را تضمین می‌کند.

---

## ۱۱. نگاشت Milestone به اسکیل

خروجی نهایی یک پروژه‌ی مشاوره، فهرست اسکیل نیست — **نگاشت مرحله به اسکیل** است. کارفرما می‌خواهد بداند در هفته‌ی سوم چه اتفاقی می‌افتد.

| Milestone | اسکیل‌های فعال |
|---|---|
| **M0** پایه | `codebase-design` · `planetscale@postgres` · `vercel:nextjs` · `vercel:shadcn` · `frontend-design` · `vercel:env-vars` |
| **M1** احراز هویت | `nextjs-supabase-auth` · `vercel:nextjs` (میان‌افزار) · **`/security-review`** |
| **M2** Onboarding | `ui-ux-pro-max` · `vercel:shadcn` · `accessibility` |
| **M3** درآمد و هزینه‌ی ثابت | `planetscale@postgres` (قید یکتایی) · `vercel:vercel-functions` (cron) |
| **M4** تراکنش‌ها | `codebase-design` (مرز `money.ts`) · `vercel:react-best-practices` |
| **M5** داشبورد | `dataviz` · `ui-ux-pro-max` · `accessibility` |
| **M6** ورودی متنی AI | `claude-api` · `vercel:vercel-functions` · `vercel:vercel-firewall` · **`/security-review`** |
| **M7** ورودی صوتی | `vercel:vercel-functions` · `accessibility` · ⚠️ حفره‌ی STT |
| **M8** عکس فاکتور | `claude-api` (vision) · `vercel:vercel-functions` |
| **M9** صیقل و استقرار | `accessibility` · `web-design-guidelines` · `vercel:deployments-cicd` · `vercel:verification` · **`/security-review`** |
| **انتهای هر M** | `/code-review high` سپس `/simplify` |

**دو نکته‌ی طراحی این جدول:**

**۱. `/security-review` سه بار می‌آید.** M1 و M9 بدیهی‌اند. M6 آن است که معمولاً فراموش می‌شود — و همان‌طور که در بخش ۸ دیدیم، اولین نقطه‌ی باز شدن سطح حمله‌ی تزریق پرامپت است.

**۲. ردیف آخر عمدی است.** بازبینی و ساده‌سازی در انتهای **هر** Milestone، نه فقط پایان پروژه. در پروژه‌ای ده‌مرحله‌ای، بدون این، قواعد معماری به‌تدریج فرسوده می‌شوند.

---

## ۱۲. دستورهای نصب

### گام ۱ — افزونه‌ی رسمی Vercel

در Claude Code با دستور `/plugin` از مارکت‌پلیس نصب می‌شود. با این کار `vercel:nextjs`، `vercel:shadcn`، `vercel:vercel-functions`، `vercel:env-vars`، `vercel:deployments-cicd`، `vercel:react-best-practices`، `vercel:vercel-firewall` و `vercel:verification` در دسترس قرار می‌گیرند.

### گام ۲ — چهار اسکیل از رجیستری

```bash
npx skills add mattpocock/skills@codebase-design -g -y
npx skills add planetscale/database-skills@postgres -g -y
npx skills add addyosmani/web-quality-skills@accessibility -g -y
npx skills add sickn33/agentic-awesome-skills@nextjs-supabase-auth -g -y
```

سه مورد اول با اطمینان بالا. مورد چهارم با احتیاطی که در بخش ۵ توضیح داده شد.

### گام ۳ — اسکیل‌های داخلی

`claude-api` · `frontend-design` · `dataviz` · `web-design-guidelines` · `/code-review` · `/security-review` · `/simplify` — همراه Claude Code می‌آیند، نصب نمی‌خواهند.

### گام ۴ — MCP رسمی Supabase

برای اجرای واقعی migration ها و تست RLS با کاربر دوم. این تنها راهی است که تست پذیرش شماره ۶ **اثبات** شود نه **ادعا**.

### گام ۵ — دو اسکیل محلی

پس از تأیید `PLAN.md`، در `.claude/skills/` داخل مخزن پروژه:

```
.claude/skills/
├── rtl-money-display/   ← قواعد نمایش پول در متن راست‌به‌چپ
└── money-module/        ← قواعد bigint، واحد کوچک ارز، مرز ماژول
```

---

## ۱۳. جمع‌بندی درسی

### چهار عدد که کل ماجرا را می‌گویند

| عدد | معنا |
|---|---|
| **۲۰** | اسکیل در نقشه‌ی نهایی |
| **۴** | نصب از رجیستری عمومی |
| **۵** | نیاز بحرانی بدون هیچ اسکیل — که ارزش افزوده‌ی مشاور آنجاست |
| **۱۵+** | اسکیل معتبری که آگاهانه رد شد |

نسبت آخر مهم‌ترین است. **گزارشی که چهار اسکیل پیشنهاد می‌دهد و دلیل رد پانزده تای دیگر را می‌نویسد، از گزارشی که بیست اسکیل بی‌تفکیک فهرست می‌کند به‌مراتب حرفه‌ای‌تر است.**

### پنج خطای رایج

**۱. شروع از فهرست اسکیل‌ها به‌جای سند پروژه.**
نشانه: فهرست شما مشکوکانه شبیه نتایج صفحه‌ی اول جست‌وجوست. درمان: قیدهای بحرانی پروژه را **پیش از** هر جست‌وجو بنویسید.

**۲. استناد به یک عدد.**
نشانه: در توصیه‌تان فقط تعداد نصب آمده. درمان: هر چهار سیگنال بخش ۲.۴ را بنویسید، حتی وقتی نتیجه بدیهی است.

**۳. نادیده گرفتن تناسب استک.**
نشانه: اسکیل Clerk در فهرست پروژه‌ای که Supabase Auth دارد. درمان: ستون استک را کنار دستتان بگذارید.

**۴. سکوت درباره‌ی حفره‌ها.**
نشانه: گزارش فقط چیزهایی دارد که پیدا شده. درمان: بخش حفره‌ها را اجباری بدانید. اگر خالی است، کافی نگشته‌اید.

**۵. تحویل فهرست به‌جای فرآیند.**
نشانه: خروجی یک جدول است و تمام. درمان: نگاشت Milestone به اسکیل (بخش ۱۱) را همیشه تولید کنید.

### جمله‌ای که باید با خود ببرید

> کار مشاور هوش مصنوعی، **پر کردن** محیط دانشی عامل نیست؛ **طراحی** آن است.
>
> طراحی یعنی تصمیم درباره‌ی آنچه هست، آنچه نیست، و آنچه باید ساخته شود. در این پروژه، پرارزش‌ترین بخش تحلیل، آن پنج حفره‌ای بود که هیچ اسکیلی برایشان وجود نداشت.
